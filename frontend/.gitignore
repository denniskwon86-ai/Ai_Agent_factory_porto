# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# [E0-1A] Preview 샌드박스용 벤더 자산 — `npm run dev`·`npm run build` 가 만든다.
#   남의 코드 4MB 를 저장소에 넣지 않는다. 없으면 미리보기가 «실행 환경을 불러오지
#   못했습니다» 라고 말하고, predev 가 다시 만든다.
public/preview-vendor/
