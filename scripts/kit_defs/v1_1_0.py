"""KIT-MFG-NONFERROUS-PROCUREMENT 1.1.0 — 현재 생성기가 만드는 판본.

## 1.0.0 과 무엇이 다른가

2026-09-05 갈래가 1.0.0 **파일을 직접 고쳐** 넣었던 변경을, 판본으로 다시 낸 것이다
(머지 39ed6e6af 로 들어와 0ca2a58ad 에서 1.0.0 을 되돌렸다).

| | 내용 |
|---|---|
| 앱 | `APP-06`(판매·납기·매출 영향) · `APP-07`(전사 시나리오·실적 통합) 등재 — 기준회사 추천 앱이 5 → 7 |
| 데이터 | `MDM-07` 에 `cost_center_name` — 원가센터 코드만으로는 사람이 못 읽는다 |
| 샘플 | quick 프로필에 `LOC-P1-FG` 추가 — 완제품 창고가 빠져 있었다 |
| **버그** | `active` 판정이 문자열 `"false"` 를 참으로 봤다 (`if l["active"]` → `str(...).lower() == "true"`) |
| **버그** | BOM 생성이 품목 수를 넘겨 순회해 같은 배합의 `-02` 대체판이 생기고 Resolver 가 거부했다 |
| **버그** | 범위 밖 창고를 조용히 다른 창고로 바꿔치기했다 → 이제 예외를 던진다 |

## ★ 이 판본이 기본인 이유

**버그 세 건이 로직 변경이라 오버레이로 표현되지 않는다.** 값을 덮는 것으로는
`if l["active"]` 를 되돌릴 수 없다. 그래서 1.0.0 을 기본에 두는 P2 구조를 유지할 수
없었고, **현재 생성기가 만드는 것 = 1.1.0** 으로 옮겼다.

1.0.0 은 이제 **재생성되지 않는다.** 그 판본의 정본은 `starter_kits/.../1.0.0/` 의
파일과 `fingerprint.json` 이고, `kit_freeze.verify()` 가 지킨다 — 재생성 가능성이
아니라 **파일 자체**가 판본을 보증한다. [[v1_0_0]] 참고.

고칠 내용이 있으면 **1.2.0 을 만드십시오.**
"""
from . import KitOverlay


class V110(KitOverlay):
    version = "1.1.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.1.0 이다.


OVERLAY = V110()
