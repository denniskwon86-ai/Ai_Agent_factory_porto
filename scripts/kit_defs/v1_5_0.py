"""KIT-* 1.5.0 — 현재 생성기가 만드는 판본.

## 1.4.0 과 무엇이 다른가

1.4.0 이 **매출**에서 산업을 보이게 했다. 이번에는 **생산과 구매**다.

    1.4.0   생산  9% · 발주  1%
    1.5.0   생산 51~77% · 발주 25%

### ① 사는 장면이 없었다

구매가 품목을 균등하게 돌았다. 구매 대상이 144 종(원료 72 · 소모품 72)인데 사업이
아는 원료는 1~5 종이라 **정광 구매가 발주의 1%** 였다.

★ 이 키트의 용도는 **「원료 구매·도입계획에서 경영 영향과 실행 결정까지」**다.
  사는 장면이 데이터에 없으면 그 용도가 성립하지 않는다.

⚠️ 더 나쁜 것은 수급이었다. 동정광을 **20,176 톤 쓰면서 612 톤만 샀다.** 기초재고
  23,202 톤이 받쳐 재고 음수는 나지 않았지만, 그것은 「사는 회사」가 아니라
  **「쌓아둔 것을 쓰는 회사」**다.

### ② ★★★ 배합대로 만들지 않았다

    "input_material_id": recipe[0]["input_material_id"]      ← **BOM 첫 줄만**

「수산화리튬 = Black Mass 5.20 + 황산 0.12 + 소석회 0.05」인데 **Black Mass 만**
나갔다. 부재료는 **사 놓고 쓰지 않았다** — 조합 키트에서 황산 706 톤 구매 ·
**0 톤 소비**.

⚠️ 1.3.0 에서 Black Mass 를 넣으며 「배합이 데이터로 돈다」고 했는데, **주원료만
  돌고 있었다.** 배합비를 현업에 물으면서 정작 그 배합대로 만들지 않고 있었다.

`MFG-02` 는 업무키가 `batch_id` 하나라 배치당 **한 행**이고 `input_material_id` 도
하나뿐이다. 그래서 부재료는 **재고 이동(`MOV-ISS-{batch}-{n}`)으로 내보낸다** —
실제 ERP 도 배치 헤더와 자재 출고를 나눠 쓴다. 스키마와 계약은 그대로다.

### 값

| | 값 | 왜 |
|---|---|---|
| `_PRC_CYCLE, _PRC_MAJOR` | **4, 1** (25%) | ⚠️ **반복 목록으로 하면 안 된다.** 계약 100 건보다 목록이 길면 앞부분만 잘려 **전부 주력**이 된다 — 전지소재(4 종 × 25)가 실제로 발주 100% 가 나왔다. 주기로 하면 개수와 무관하다 |
| `_MAJOR_REPEAT_MFG` | 3 → **30** | **판매 가중과 맞춘다.** 완제품이 2 종이면 배치가 나뉘어 종당 생산이 주는데 판매는 그대로라, 작으면 「만든 것보다 많이 판다」로 기운다 — 전지소재가 실제로 그랬다 |
| `_MAJOR_BATCH_SCALE` | **0.45** | 가중만 올리면 총량이 판매를 크게 웃돈다. **주력은 작은 배치로 자주.** 이 값은 **quick 프로파일이 정했다** — 배치 650 · 판매 300 이라 full(8,000 · 2,500)보다 빠듯하다 |
| `purchase_qty_scale` (사업별) | 정광 2.2 · MHP/BM 5.5 · 황산 4.5 · 석회 2.5 | 정광은 선적 단위로 수천 톤씩, 소석회는 조금씩 산다. **원료마다 거래 단위가 다르다** |

## 되풀이되지 않게

| | |
|---|---|
| `{profile}:산_것보다_많이_쓰지_않는다` | 기초재고를 포함해 본다. **재고 음수 검사가 조정에 가려 못 잡던 것** |
| `{profile}:배합대로_원료가_나간다` | BOM 의 모든 `INPUT` 이 실제 출고에 있는가 |

## 고치지 않은 것

| | 왜 |
|---|---|
| ★ **기초재고가 소비의 1.15 배** | **36 개월치다 — 살 이유를 없앤다.** 0.20 으로 내려 봤다가 되돌렸다. 이 값은 **모든 품목**에 걸리는데 더미 소모품은 구매가 따라오지 않아 **음수 842 행**이 터진다. 제대로 고치려면 **발주량을 소비 예상에서 역산**해야 하고, 그러려면 구매를 생산 뒤로 옮기거나 BOM 으로 예측해야 한다 — 1.6.0 |
| 그래서 구매/소비가 **0.66~0.75** | 1.4.0 의 **0.03** 에서 25 배 나아졌지만 아직 1 을 넘지 못한다. 위와 같은 이유다 |
| 조정 이벤트가 이동의 32% | 볼륨용이다. 다만 **이것이 세 결함을 가렸다** — 과잉 판매도, 미구매도, 부재료 미소비도 재고 음수로 드러나지 않게 만들었다 |
| 재고(`INV-01`) 주력 비중 | 품목 마스터를 그대로 따라간다. 재고는 「무엇을 하는 회사인가」를 덜 말한다 |
| 공동 산출 배분 · 상대가격 | 도메인 검토 대상 |

고칠 내용이 있으면 **1.6.0 을 만드십시오.**
"""
from . import KitOverlay


class V150(KitOverlay):
    version = "1.5.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.5.0 이다.


OVERLAY = V150()
