"""KIT-MFG-NONFERROUS-PROCUREMENT 1.0.0 — 동결된 판본. **재생성되지 않는다.**

## 왜 재생성할 수 없는가

2026-09-11 에 생성기 로직이 바뀌었다(1.1.0). 바뀐 것 중 **셋이 버그 수정**이다.

- `active` 판정이 문자열 `"false"` 를 참으로 봤다
- BOM 생성이 품목 수를 넘겨 순회해 대체판 충돌을 만들었다
- 범위 밖 창고를 조용히 바꿔치기했다

**값이 아니라 로직이라 오버레이로 되돌릴 수 없다.** `company_profile_patches` 같은
덮어쓰기로는 `if l["active"]` 를 복원하지 못한다. 그래서 이 오버레이는 1.0.0 을
만들어 내는 대신 **시도 자체를 막는다** — 「돌렸더니 1.0.0 이 나왔는데 내용이 다른」
상황이 가장 나쁘다.

## 그럼 1.0.0 은 무엇이 보증하나

**파일 자체와 지문이다.**

    starter_kits/KIT-MFG-NONFERROUS-PROCUREMENT/1.0.0/
      .frozen            동결 표식 (2026-09-10 · 검증 PASS 406 건)
      fingerprint.json   175 개 파일의 sha256

`kit_freeze.verify()` 가 이 대장과 대조한다. P1 이 지키려던 것은 「언제나 재생성할 수
있다」가 아니라 **「확정된 것이 확정된 채로 남는다」**였고, 그것은 지켜진다.

⚠️ 이 파일을 고쳐 1.0.0 을 만들 수 있게 하려 들지 마십시오. 만들어진 것이 동결된
  1.0.0 과 다르면 **두 개의 1.0.0** 이 생긴다 — 그것이 이 체계가 막으려는 사고다.
"""
from . import KitOverlay


#: `load("1.0.0")` 이 이것을 보고 거부한다. 오버레이를 내놓지 않는다.
REFUSE = (
    '1.0.0 은 동결된 판본이라 이 생성기로 다시 만들 수 없습니다.\n'
    '  2026-09-11 에 생성기 로직이 바뀌었고(버그 3 건 수정), 값이 아니라 로직이라\n'
    '  오버레이로 되돌릴 수 없습니다.\n'
    '\n'
    '  · 1.0.0 의 정본은 starter_kits/KIT-MFG-NONFERROUS-PROCUREMENT/1.0.0/ 입니다.\n'
    '    내용 확인: python scripts/freeze_starter_kit.py --verify\n'
    '  · 지금 생성기가 만드는 것은 1.1.0 입니다.\n'
    '  · 새로 낼 것이 있으면 1.2.0 을 만드십시오 — scripts/kit_defs/v1_2_0.py'
)

KitOverlay  # noqa: B018  — 판본 정의 모듈임을 드러내려 남긴다
