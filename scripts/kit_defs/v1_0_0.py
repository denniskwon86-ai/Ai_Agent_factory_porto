"""KIT-MFG-NONFERROUS-PROCUREMENT 1.0.0 — 기준 판본.

★★★ **이 오버레이는 비어 있어야 한다.** 1.0.0 은 동결된 판본이고(P1), 여기에 무엇을
  더하면 그 판본이 달라진다. 비어 있다는 것이 「생성기의 기본 정의 = 1.0.0」임을
  보증하고, 그래야 새 구조로 재생성해도 지문이 같다(P2-3).

고칠 내용이 있으면 **1.1.0 을 만드십시오.** 이 파일을 고치는 것은
`starter_kits/.../1.0.0/` 을 직접 고치는 것과 같은 일이다.
"""
from . import KitOverlay


class V100(KitOverlay):
    version = "1.0.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.0.0 이다.


OVERLAY = V100()
