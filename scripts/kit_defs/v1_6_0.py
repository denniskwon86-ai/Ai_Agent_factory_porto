"""KIT-* 1.6.0 — 현재 생성기가 만드는 판본.

## 1.5.0 과 무엇이 다른가

**구매가 「얼마나 쓸지」를 모른 채 발주를 만들고 있었다.**

1.5.0 까지 생성 순서가 이랬다.

    구매 → 물류 → 판매 → 생산 → 재고
    ↑
    여기서 발주량을 정하는데, 소비는 저 뒤 「생산」에서 나온다

그래서 발주량을 `purchase_qty_scale` 이라는 **손으로 맞춘 상수**로 정했다. 사업마다
원료 수가 다르고 판본마다 생산량이 달라져서, **1.5.0 을 내면서만 네 번 고쳤다.**
그러고도 **구매/소비가 0.66~0.75** 에 머물렀다 — 여전히 기초재고로 버티는 데이터다.

### 순서를 바꿨다

    판매 → 생산 → **구매** → 물류 → 재고

    _need = material_need(MFG-02, MDM-05)      ← 얼마나 쓰는지 센다
    PRC-01 = generate_contracts(..., _need)     ← 계약 상한이 그것을 덮는다
    PRC-02 = generate_purchase(..., _need)      ← 발주량이 그것에서 나온다

의존은 그대로다 — 구매는 품목·공급사·지표만 보고, 그 셋은 여전히 앞에 있다.

★ **맞출 상수가 사라졌다.** `purchase_qty_scale` 을 걷어냈다.

### 발주가 전부 입고되지는 않는다

    발주 1,800 건  ·  배송 1,200 건       ← 3 분의 2 만 입고되고 나머지는 OPEN

그만큼 더 주문해야 **실제 입고**가 소비를 댄다(`_BUY_MARGIN / _deliver`). 이것을
빼면 발주는 맞는데 재고가 음수로 간다.

### 기초재고를 주력만 낮췄다

1.15 배는 **36 개월치**다 — 살 이유를 없앤다. 1.5.0 에서 모든 품목을 0.20 으로
내렸다가 되돌렸는데(더미 소모품은 구매가 따라오지 않아 **음수 842 행**), 이제 구매가
소비를 보고 들어오므로 **주력만 0.25** 로 둔다. 더미는 그대로 1.15 다.

| | 값 | |
|---|---|---|
| `_BUY_MARGIN` | 1.25 | 딱 맞춰 사면 **시점이 조금만 어긋나도 음수**가 난다 |
| 기초재고(주력) | 0.25 | 약 9 개월치. 구매가 나머지를 댄다 |
| 기초재고(더미) | 1.15 | 구매가 균등하게 들어오므로 그대로 |

## 고치지 않은 것

| | 왜 |
|---|---|
| 조정 이벤트가 이동의 32% | 볼륨용이다. 다만 **이것이 세 결함을 가렸다** — 과잉 판매·미구매·부재료 미소비가 모두 재고 음수로 드러나지 않게 만들었다 |
| 재고(`INV-01`) 주력 비중 | 품목 마스터를 그대로 따라간다 |
| 공동 산출 배분 · 상대가격 | 도메인 검토 대상 |

고칠 내용이 있으면 **1.7.0 을 만드십시오.**
"""
from . import KitOverlay


class V160(KitOverlay):
    version = "1.6.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.6.0 이다.


OVERLAY = V160()
