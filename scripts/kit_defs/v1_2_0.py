"""KIT-MFG-NONFERROUS-PROCUREMENT 1.2.0 — 현재 생성기가 만드는 판본.

## 1.1.0 과 무엇이 다른가

**「기존 필드에 산업의 의미를 넣는」 층**을 처음 실제로 채웠다. 열을 늘리지 않고
이미 있는 `grade`·`unit_price`·`quantity_uom`·`product_id` 에 그 산업이 실제로 쓰는
값을 넣었다 — 스키마가 그대로라 플랫폼 쪽 변경이 없다.

| | 1.1.0 | 1.2.0 |
|---|---|---|
| **등급** | 실제 품목 12 개가 **전부 `DEMO_STANDARD`**, 더미에만 `G1~G4` | 정광 `CU_25PCT` · 전기동 `LME_GRADE_A` · 황산니켈 `BATTERY_GRADE` |
| **부산물** | 재고로 쌓이기만 하고 **한 톤도 안 팔린다** | 생산에서 **나오고**, 팔린다 — 제련 매출의 15.3% |
| **판매 단위** | 전부 `TON` — **금을 톤으로 판다** | 품목 단위 (금은 `KG`) |
| **공정** | 두 사업이 한 목록. 전기동이 **습식**(침출·결정화)으로, 그나마 **2 공정만** | 제련은 건식 5 공정(배소→용련→전로정련→정제→전해정련), 전지소재는 습식 5 공정 |
| **기초재고** | 공장마다 규칙이 달라 **공정재고 창고가 비어 있었다**. 게다가 유형별 고정값이라 **재고가 음수로 갔다**(1,082 행 · 최저 −5,276) | 유형에 맞는 창고로, 수량은 **실제 소요 기반**. 음수 0 행 |
| **사업 경계** | `scope_node_id` 는 갈렸지만 **품목이 샜다** — 재고 스냅샷의 절반이 남의 사업 창고 | 제련만 뽑으면 전지소재 품목이 **한 행도 없다** |

## ★ 왜 오버레이가 아니라 새 판본인가

[[v1_1_0]] 이 남긴 교훈 그대로다 — **로직 변경은 값을 덮는 것으로 표현되지 않는다.**
`grade` 를 사업 정의에서 가져오는 것, 부산물을 판매 목록에 넣는 것은 값이 아니라
경로의 변경이다. 그래서 1.1.0 을 재생성할 수 없고, 1.1.0 의 정본은
`starter_kits/.../1.1.0/` 의 파일과 `fingerprint.json` 이다.

⚠️ 1.1.0 은 **2026-09-17 까지 대장이 없었다.** 확정 판본이라면서 아무것도 그 내용을
  보증하지 않았고 값을 바꿔도 검증이 통과했다. 같은 날 봉인했다.

## 고치지 않은 것

| | 왜 |
|---|---|
| 부서가 **마지막 사업**에 붙는다 | 부서 편제는 **회사의 경영방침**이지 산업 특성이 아니다 — 현업이 플랫폼에서 정할 자리다 |
| 완제품 상대가격 (`FG-NISO4` > `FG-CATHODE`) | 실제와 반대지만 **재무 데이터 전체가 딸려 움직인다** — 도메인 검토가 먼저다 |
| 램프업이 없다 (36 개월 수율이 평평) | 사업이 아니라 **단계**의 문제 — 회사 프로파일이 맡는다 |
| **원장에 매출이 없다** (`FIN-03` 이 전부 매입 전표) | 1.1.0 부터 그랬다. 부산물만의 문제가 아니라 **재무가 거래와 연결돼 있지 않다** — 고치면 `FIN-01~03` 전체가 움직인다 |

고칠 내용이 있으면 **1.3.0 을 만드십시오.**
"""
from . import KitOverlay


class V120(KitOverlay):
    version = "1.2.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.2.0 이다.


OVERLAY = V120()
