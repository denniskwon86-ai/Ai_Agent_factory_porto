"""KIT-* 1.7.0 — 현재 생성기가 만드는 판본.

## 1.6.0 과 무엇이 다른가

**실사 조정이 한 방향으로만 일어나 재고가 저절로 늘고 있었다.**

    (1.6.0)  조정 1,920 건 · **전부 입고** · 순증 +326 톤
    (1.7.0)  조정 1,920 건 · **양방향** · 순증 ≈ 0

### 왜 이것이 중요한가

이 저장소가 늦게 발견한 결함 셋이 **모두 그 뒤에 숨어 있었다.**

    과잉 판매      만든 것보다 많이 팔았다     (1.4.0 에서 발견)
    미구매        산 것보다 많이 썼다         (1.5.0)
    부재료 미소비   BOM 첫 줄만 출고했다        (1.5.0)

셋 다 **재고 음수로 드러나야 했다.** 그런데 조정이 재고를 계속 채워 잔고가 양수로
남았고, 재고 음수 검사는 통과했다. **볼륨을 채우려고 넣은 것이 검사를 무력화하고
있었다.**

★ `i%2`(부호)와 `i%9`(크기)는 서로소라 **18 주기마다 정확히 상쇄**된다.

### 곁들여 — 스냅샷이 **부호로 방향을 정한다**

    (전)  if movement_type in ("PRODUCTION_ISSUE", "SALES_SHIPMENT"): 출발지
    (후)  if quantity < 0: 출발지

이동 유형이 늘 때마다 그 목록을 고쳐야 했고 **빠뜨리면 조용히 반대쪽에 쌓인다** —
음수 조정을 넣자마자 그렇게 될 뻔했다.

⚠️ 바꾸기 전에 대조했다 — 기존 데이터 30,000 행에서 **두 방식의 판정이 한 건도
  다르지 않다.** 나가는 이동은 전부 음수였기 때문이다.

### ⚠️ 「조정이 이동의 32%」는 낡은 숫자다

1.4.0 기준이었다. **1.5.0 에서 부재료 출고를 넣자 실제 이동이 늘어** 조정이 볼륨을
채울 자리가 줄었다.

    제련      1,920 건 (6.4%)
    전지소재       0 건
    조합          0 건

조정은 **목표 행수(30,000)를 채우는 나머지**라, 실제 업무 이동이 늘면 저절로 준다.

## 되풀이되지 않게

| | |
|---|---|
| `{profile}:조정이_재고를_만들지_않는다` | 순증이 총량의 **5% 이내** |

## 고치지 않은 것

| | 왜 |
|---|---|
| 재고(`INV-01`) 주력 비중 | 품목 마스터를 그대로 따라간다 |
| 공동 산출 배분 · 상대가격 | 도메인 검토 대상 |

고칠 내용이 있으면 **1.8.0 을 만드십시오.**
"""
from . import KitOverlay


class V170(KitOverlay):
    version = "1.7.0"
    # 아무것도 얹지 않는다 — 생성기의 기본 정의가 곧 1.7.0 이다.


OVERLAY = V170()
