# 전수 감사 결과 (2026-07-18) — 버그·성능 개선 목록

> 백엔드(core/nodes/api)·프론트(src) 전체 감사. 모든 항목은 실코드 확인 완료.
> 상태: `OPEN`(미수정) / `FIXED`(수정 완료) / `PARTIAL`(부분 수정)

## A. 치명 (기능이 실제로 죽어 있거나 잘못 동작)

| # | 위치 | 내용 | 상태 |
|---|---|---|---|
| A1 | `core/llm_gateway.py:465` vs `nodes/vision_qa.py:105` | `_LazyGateway` 프록시가 `aexecute`만 위임 → `gateway.aexecute_vision()` 호출이 AttributeError → except가 `PASS`로 삼킴. **스크린샷이 있어도 Vision QA는 영구 무력화** | **FIXED** (프록시에 위임 추가) |
| A2 | `api/routes/factory_control.py:383` | 메가 start_all이 `shared_ledger: Dict[str,dict]`에 JSON **문자열** 주입 → 서브 스프린트 첫 노드 `model_validate`에서 즉사 → 8개 서브가 "가동됨"으로 보이나 아무것도 안 함 | **FIXED** (파싱 후 dict 래핑 + master_data 문자열 필드 병행 주입) |
| A3 | `core/async_orchestrator.py:53` | PLANNING 아카이빙이 `project_meta.json`까지 이동 → 이후 모든 태스크의 `template_id`가 `default`로 강등 | **FIXED** (아카이브 제외 목록 추가) |
| A4 | `core/async_orchestrator.py:168,227` | 스프린트 루프가 모든 예외를 print로 삼킴 → 실패 이벤트 없음, UI는 영원히 "가동 중" | **FIXED** (`SPRINT_FAILED` 브로드캐스트 + 프론트 수신 처리) |
| A5 | `frontend/src/components/MegaBoardroomPanel.tsx:21` | `/state/latest` 응답 봉투(`{status,data}`)를 통째로 저장 → 서브 프로젝트 상태/진행률 항상 빈 값/0% | **FIXED** (`data.data` 저장 + 중복 분기 제거) |

## B. 주요 버그

| # | 위치 | 내용 | 상태 |
|---|---|---|---|
| B1 | `core/async_orchestrator.py:46,171` | start/resume이 실행 중 태스크 확인 없이 같은 thread_id로 이중 astream 가능 + 완료 태스크 미제거 | **FIXED** (실행 중이면 거부 + done_callback 정리) |
| B2 | `nodes/ui_designer.py` | VisionQA 게이트 피드백을 UIDesigner가 소비하지 않음 → 재설계 깜깜이 + 잔류 피드백이 WBS 재분할 지시로 오염 | **FIXED** (extra_instruction 주입 + 소비 후 큐 비움) |
| B3 | `nodes/execution.py:542` | 리뷰어 JSON 추출이 non-greedy → 피드백에 `}` 포함 시 파싱 실패 → **REWORK가 PASS로 둔갑** | **FIXED** (전체 json.loads 우선 + greedy 폴백) |
| B4 | `nodes/execution.py:713` | ManualWriter가 기본 `code` 모드 호출 → 매뉴얼이 files JSON 블롭 | **FIXED** (`output_mode="document"`) |
| B5 | `frontend/src/store/useFactoryStore.ts:298,318,609` | `checkHotl`/`fetchFeed`/`fetchWBS`가 await 후 프로젝트 전환 재확인 없음 → 이전 프로젝트 HOTL 대기가 새 프로젝트에 주입 | **FIXED** (await 후 pid 재검증 가드) |
| B6 | `frontend/src/store/useFactoryStore.ts:250` | 강제 정지가 `needs_revision`/`current_sprint_task_id`를 남김 → 유령 HOTL 배너 + 죽은 태스크 resume 가능 | **FIXED** (finally에서 함께 초기화) |
| B7 | `api/routes/factory_control.py:864` | resimulate가 `ProjectState`에 없는 `sim_*` 키 주입(`extra='forbid'`) | **FIXED** (`sim_cycle_count`/`sim_modified_params`/`sim_base_cycle` 정식 필드 추가) |
| B8 | `nodes/execution.py:296` | 생성 .py 전체 이어붙여 `ast.parse` → `from __future__` 위양성 빌드 실패 | **FIXED** (파일별 개별 검사 + 오류에 파일명 표기) |
| B9 | `frontend/src/components/PreviewPanel.tsx:596,504` | message 핸들러 origin/source 미검증(스푸핑 가능) + heal 실패 시 alert 폭풍. `allow-same-origin` 샌드박스 | **PARTIAL** (event.source 검증 + alert 1회 제한 + 문서탭 iframe sandbox 축소. 메인 프리뷰 `allow-same-origin`은 localStorage 앱 프리뷰 정상 동작 위해 유지 — 사유 주석) |
| B10 | `frontend/src/components/HOTLInput.tsx:31` | 질문 재생성 시 id만 같으면 선택 상태 stale → '선택 없음'으로 직렬화 | **FIXED** (지문에 옵션 라벨 포함 + stale 라벨 폐기) |
| B11 | `core/agent_graph.py route_from_pm` | (수정 중 발견) PRD 게이트 피드백이 PM 재작성이 아닌 UIDesigner로 직행 → 기획 피드백이 UI 단계 지시로 오염 | **FIXED** (needs_revision 시 Master_PM 자기루프 + PM이 피드백 소비/큐 비움) |
| B12 | `frontend/src/store/useFactoryStore.ts:239` | (수정 중 발견) 강제 정지 API URL이 `/api/projects/...`로 잘못됨 → **항상 404**, finally의 UI 초기화가 실패를 가림(백엔드 스프린트는 계속 돔) | **FIXED** (`/api/v1/factory/...`로 교정) |

## C. 성능/비용

| # | 위치 | 내용 | 상태 |
|---|---|---|---|
| C1 | `nodes/execution.py:172` | Micro-Swarm이 개발 노드마다 풀컨텍스트 LLM 3회 병렬(3배 토큰 + 429 유발) | **FIXED** (평시 1회, 재작업/재시도 시에만 3회 승격) |
| C2 | `core/supervisor_daemon.py:18` | 모든 NODE_COMPLETED마다 Flash 1콜(총 호출 ~2배) | **FIXED** (Master_PM/PMO/Reviewer/QA/Supervisor 화이트리스트) |
| C3 | `config.py:72` | `MODEL_CONTEXT_LIMITS` 미참조 → 한도 초과 프롬프트가 항상 400 실패, 폴백 왕복 낭비. truncation 검사 데드코드 | **FIXED** (`_clip_prompt`: 1차 모델 한도 상시 클리핑 + Flash 최후 재시도 시 6k 강제 축소, 데드코드 제거) |
| C4 | `nodes/execution.py:648` 등 | async 안 동기 블로킹(QA pytest/npm ~2분, 렌더체크 60s, 스모크 45s, 회귀 git, 상태 저장, 아카이브, ZIP) → 이벤트 루프 동결 | **FIXED** (`asyncio.to_thread` 래핑: 테스트러너/렌더체크/스모크/회귀검사/_save_latest_state/아카이브/ZIP) |
| C5 | `pipeline_state.db` (74MB) | 체크포인트 무한 증식·삭제 프로젝트 스레드 미정리·동일 id 재생성 시 옛 체크포인트 재개 위험 | **FIXED** (delete_project 시 `_purge_checkpoints`: 스레드 GLOB 삭제 + VACUUM, 베스트 에포트) |
| C6 | `api/routes/factory_control.py:1086,1178` | AI-recommend가 요청마다 `LLMGateway()` 생성(동기 models.list 포함) | **FIXED** (지연초기화 싱글턴 재사용) |
| C7 | `frontend` store/Timeline | `logs` 무제한 누적 + SSE마다 재정렬/이중 set() + 3초 상시 tick + PreviewPanel 전체 구독 | **FIXED** (logs 500 상한, SSE 이벤트당 set 1회 통합, Timeline useMemo+하단 근접 시에만 자동스크롤, ControlPanel tick 가동 중에만, PreviewPanel 탭 콘텐츠 useMemo, MegaBoardroom 폴러 마운트 1회+병렬 조회+변경 시에만 set) |
| C8 | `core/broadcaster.py:22` | 클라이언트별 재직렬화 + 비정상 종료 시 큐 누수 + NODE_COMPLETED에 MB급 상태 동봉 | **FIXED** (broadcast 1회 직렬화, finally 큐 회수, 유한 큐(500)+가득 시 최고(最古) 이벤트 드롭. 상태 동봉 축소는 프리뷰 실시간 갱신이 의존해 보류) |

## 정상 확인(문제 없음)
- 경로 조작 방어(`_safe_id`) 전 라우트 적용 확인 / 파일 I/O UTF-8 인코딩 일관 / debate·scoring 호출 수는 이미 최소화(1라운드·1비평가) / SSE 프로젝트 격리 필터 정상 / ServerLogPopup은 폴링 아님(마운트 1회).

## 수정 검증 (2026-07-18)
- 그래프 컴파일·전 모듈 import 정상, HOTL 게이트 5곳 유지.
- pytest: 신규 회귀 0건 (잔여 실패 7건은 감사 전부터 있던 구버전 기대값의 낡은 테스트 — ENV-2 베이스라인 대상).
- 프론트 `tsc --noEmit` 통과.
