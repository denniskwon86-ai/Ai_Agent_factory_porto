# ==========================================
# V5.0 AI Factory Studio - Core Dependencies
# ==========================================

# 1. Core Workflow Engine
langgraph>=0.0.26
langgraph-checkpoint-sqlite>=1.0.0

# 2. LLM Providers & LangChain Core (Hybrid Engine)
langchain-core>=0.1.52
langchain-google-genai>=1.0.3
langchain-groq>=0.1.3
langchain-cerebras>=0.5.0   # 3차 폴백(무료 티어) — CEREBRAS_API_KEY 미설정 시 게이트웨이가 자동 비활성

# 3. Utilities & Configuration
python-dotenv>=1.0.1
PyYAML>=6.0.1
filelock>=3.14.0
truststore>=0.9.0   # 사내망 SSL검사 환경에서 OS 신뢰저장소 사용(CERTIFICATE_VERIFY_FAILED 방지)

# 4. WBS & Dashboard Management
pandas>=2.2.0
openpyxl>=3.1.2
streamlit>=1.32.0

# 5. V5.0 API Server Prep (Phase 2 확장 대비용)
fastapi>=0.110.0
uvicorn>=0.29.0