# T3 정산 — 2026-09-12 (동결본 `e3a41768a`)

> 자동 기록이 아니다. 사람이 읽고 판단하라고 쓴다.
> 실행: 동결 worktree `/c/t3wt` · `.env`·`data/`·`library/` 씨앗 복사 · `pytest tests/`

## 결과

    7,120 passed · 5 skipped · **6 failed** · 2,143초(35분 43초)

    직전 T3(2026-09-08 · `3b9ab8520`)   6,735 passed / 3 failed / 978초
    증감                                **+385건** · 시간 2.2배

⚠️ 시간이 2.2배가 된 것은 시험이 385건 는 것만으로는 설명되지 않는다(+5.7%인데 +119%다).
  **원인을 안 밝혔다** — 다음 T3 에서 느린 시험을 `--durations` 로 봐야 한다.

## 실패 6건 — 대조군으로 갈랐다

지난 T3 에서 3건이 전부 «동결본 씨앗 탓»이었으므로, 이번엔 **main 트리 대조군**을 먼저 돌렸다.

    test_calculation_api.py  5건   main 에서도 실패   → **진짜 결함**
    test_starter_package_catalog.py  1건   main 에서는 통과 → 동결본 artifact

### ⚠️ 진짜 결함 5건 — **내 탓이다**

`evidence_basis` 를 필수로 만들면서 **라우트를 하나만 고쳤다.**

    api/routes/decision_control.py        고쳤다
    api/routes/calculation_control.py     **놓쳤다** — 두 경로가 422 로 죽고 있었다

★ 왜 놓쳤나: `decision_case.create(` 로 grep 했는데 이 시험들은 **HTTP 라우트를 통해**
  만든다. 호출부를 코어 함수 이름으로만 찾으면 라우트 경유 경로가 안 보인다.

고친 방향: 두 경로의 증거는 **서버가 계산에서 파생한 것**이므로 근거 «종류» 도 서버가
안다. 사용자에게 물으면 틀리게 답할 수 있고 그 순간 Q4 의 답이 거짓이 된다.
→ `BASIS_SIMULATION` 을 서버가 싣는다. 커밋 `b7e3982de`. 80건 통과.

★★★ **T3 가 정확히 이걸 잡으라고 있는 것이다.** 집중 회귀 388건은 통과했는데
  전체에서 5건이 나왔다 — 「부분 초록은 통합 초록이 아니다」의 실례다.

### ⚠️ artifact 1건 — 그런데 «그 안에 진짜 결함»이 있다

`test_starter_package_file_index_matches_every_stored_asset` 이 동결본에서만 실패했다.
시계로 추적했다:

    17:03:51   worktree 생성
    17:04:01   `AFS-DEMO-MATERIALS-GROUP.json` **과 `manifest.json` 이 함께 수정됨**  ← T3 도중
    17:40:00   T3 종료

★★★ **시험 도중 «추적 파일»이 고쳐졌다.** `git diff` 는 main 에서 아무것도 안 보여 주는데
  동결본 파일과 내용이 다르다(크기 1,117 vs 1,112 · CRLF 수는 같다 — 줄바꿈이 아니다).

⚠️ 이것이 뜻하는 것: **어떤 시험이(또는 그 시험이 부르는 코어가) 저장소의 추적 파일을
  쓴다.** 그러면 manifest 검사는 **실행 순서에 따라 결과가 달라진다** — 단독으로 돌리면
  통과하고 전체에서 돌리면 실패한다. 지금이 정확히 그 모양이다.

    ⚠️ 이것을 「동결본 탓」으로만 적고 넘기면 안 된다. 동결본이 드러낸 것이지
      동결본이 «만든» 문제가 아니다. 담당 레인(업무키트)이 봐야 한다.

## 다음 T3 를 위한 메모

    ⭕ 씨앗 복사(`.env`·`data/`·`library/`)는 **이번에 효과가 있었다** — 지난번 3건 중
      두 원인(OPENROUTER_API_KEY 없음 · 자산 checksum)이 재발하지 않았다
    ⚠️ `--durations=20` 을 붙일 것 — 35분의 이유를 모른다
    ⚠️ 추적 파일을 쓰는 시험을 찾을 것 — 위 finding
