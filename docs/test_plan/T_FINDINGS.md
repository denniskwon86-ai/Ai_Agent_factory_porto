# T 트랙 결과 — 「이어지는가」가 아니라 「믿을 수 있는가」

> 자동 생성: `tests/probe_t_trust.py`. **고쳐 쓰지 말 것** — 다시 돌리면 덮인다.
> X 트랙과 다르다 — 여기는 **운영 DB 의 실제 자료**를 읽어서 센다(`mode=ro`, 쓰기 0).

    항목 16건 · 충족/막힘 14건 · **미달 2건** · **샘 0건** · 탐침 오류 0건

| 트랙 | 무엇을 봤나 | 결과 | 근거 |
|---|---|---|---|
| T-1 | 원천 등록부에 다섯 칸이 있는가 | ⚠️ 미달 | 1행 중 빈 칸: 소유자 1행(owner_department) |
| T-1 | 관측값에 다섯 칸이 있는가 | OK 충족 | 240행 · 다섯 칸 모두 채워짐 |
| T-1 | 격리 적재본에 다섯 칸이 있는가 | OK 충족 | 240행 · 다섯 칸 모두 채워짐 · 대체: 갱신일←payload_json.vintage_date(240행 · Pink Sheet 는 값별 발표일을 주지 않는다 — 지어내지 않고 비웠다) |
| T-2 | value_kind 에 기본값이 없는가 | OK 막힘 | put_fact(value_kind=…) 에 기본값이 없다 — 호출자가 반드시 고른다 |
| T-2 | 닫힌 목록 밖 성격을 넣는다 | OK 막힘 | value_kind 는 ACTUAL/PLAN/FORECAST/SCENARIO 중 하나여야 합니다(받은 값: 'REAL_ISH'). 실제·계획·예측·시나리오를 섞지 않는 것이 이 모델의 첫 번째 규칙입니다. |
| T-2 | 시나리오 결과를 실적으로 넣는다 | OK 막힘 | ACTUAL 값에는 scenario_id 를 붙일 수 없습니다 — 시나리오 결과가 실적·계획으로 섞여 들어가는 경로입니다. |
| T-2 | 가정 없는 시나리오 값을 넣는다 | OK 막힘 | SCENARIO 값에는 scenario_id 가 필요합니다 — 어느 가정에서 나온 숫자인지 모르면 재현할 수 없습니다. |
| T-2 | 시연 계약에 공표 통계를 넣는다 | OK 막힘 | EXT-01 계약은 «SYNTHETIC» 를 담는다고 선언했는데 이 원천은 «PUBLIC_DISCLOSED» 를 냅니다. 억지로 넣지 않습니다 — 계약을 고쳐 승인하거나 다른 계약을 쓰십시오. |
| T-2 | 성격을 선언 안 한 계약에 넣는다 | OK 막힘 | EXT-99 계약이 자료 성격을 선언하지 않았습니다 — 무엇이 담기는지 말하지 않는 계약에는 넣지 않습니다. |
| T-2 | 실측 — 지금 DB 에 섞인 행이 있는가 | OK 충족 | ACTUAL 6행(scenario_id 0) · PLAN 14행(scenario_id 0) |
| T-3 | 같은 입력이 같은 지문을 내는가 | OK 충족 | 순서 무관 a6b6be51870c · 값이 바뀌면 달라짐(a6b6be51→202eb9b4) |
| T-3 | 같은 계산을 두 번 돌린다 | OK 충족 | 두 번 같음(지문 1a0e6179fd56) · 가정을 바꾸면 달라짐 |
| T-3 | 과거 발표값(vintage)으로 되돌린다 | OK 충족 | 2016-01 발표값 4471.79 (최신 11785.25) — 다른 값이 나온다 |
| T-3 | 원장 해시 사슬이 이어져 있는가 | OK 충족 | 169건 연속 · prev_hash 가 전부 앞 사건의 event_hash 와 일치 |
| T-4 | 결정에 데이터·가정·산식·승인이 다 있는가 | ⚠️ 미달 | 결정 4건 중 네 가지 완비 2건 · 빠진 것: dec_d0097c5a04→['데이터', '가정']; dec_68ea74f1b4→['데이터', '가정'] |
| T-4 | 승인자가 사람 명의인가 | OK 충족 | 결정 4건 전부 사람 명의 · 결정자 ['hikwon@lsmnm.com', 'hikwon_20@lsmnm.com'] |

---

## 판정

### ⚠️ 미달 — «비어 있다» 이지 «고장났다» 가 아니다

★ T-1·T-4 는 **측정**이다. 빈 칸은 제품 결함일 수도 있고 **아직 안 채운 것**일 수도 있다. 둘을 섞어 부르지 않는다.

- **T-1 원천 등록부에 다섯 칸이 있는가** → 1행 중 빈 칸: 소유자 1행(owner_department)
- **T-4 결정에 데이터·가정·산식·승인이 다 있는가** → 결정 4건 중 네 가지 완비 2건 · 빠진 것: dec_d0097c5a04→['데이터', '가정']; dec_68ea74f1b4→['데이터', '가정']


⚠️ 이 결과가 «주장하지 않는» 것: 화면이 이 다섯 칸·네 가지를 **실제로 보여 주는지**는 여기서 알 수 없다(Codex 레인). 그리고 T-1 의 「다섯 가지」를 어느 열로 볼지는 이 탐침의 `FIVE` 표가 정한 것이다 — 열 이름이 바뀌면 표도 바꿔야 한다.
