# X-6 · E 트랙 결과 — 비용은 «측정되는가»

> 자동 생성: `tests/probe_e_economy.py`. **고쳐 쓰지 말 것** — 다시 돌리면 덮인다.
> ★ 이 트랙의 수용 기준은 「싸다」가 아니라 **「잴 수 있다」** 다 — 얼마가 적정한지는 사람이 정한다.
> 자료: `data/llm_call_log.jsonl` 을 **제품이 읽는 함수로** 읽었다(`telemetry_control._read_records`·`aggregate`).

    항목 9건 · 측정됨 8건 · **미측정 1건** · **샘 0건** · 탐침 오류 0건

| 트랙 | 무엇을 봤나 | 결과 | 근거 |
|---|---|---|---|
| E-1 | 비용이 실제로 숫자로 나오는가 | OK 측정됨 | 호출 2,323건 · 비용 $19.83 · 산정됨 2,274건 / 미산정 49건(2.1%) · 토큰 입력 30,240,093 / 출력 6,947,755 |
| E-1 | «모름» 을 0(공짜)으로 접지 않는가 | OK 측정됨 | 무료 491 · 캐시 108 · 유료 1675 · **모름 49** 로 갈라 센다 — 「모름」을 0 으로 접지 않는다 |
| E-1 | 비용을 일 단위로 나눌 수 있는가 | OK 측정됨 | 프로젝트 29개로 나뉜다 · 상위: CRM002 $6.73, CRM003 $3.24, TEST001 $2.35, live-walk-03 $0.96 |
| E-1 | «승인된 결과물 1건당» 을 낼 수 있는가 | ⚠️ 미측정 | 승인 발간물 2건이지만 **호출과 잇는 키가 없다** — 호출 로그는 `project_id` 로, 발간물은 `source_id`(결정 안건)로 묶인다. 둘을 잇는 열이 어느 쪽에도 없어 «1건당 비용» 을 못 낸다 |
| X-6 | 작업 등급별 모델 정책이 선언돼 있는가 | OK 측정됨 | 등급 ['pro', 'flash'] · 우선순위 판정(store)이 «누가 이겼는지» 를 함께 돌려준다: {'pro': ['google/gemini-2.5-flash', 'google/gemini-2.5-flash-lite'], 'flash': ['google/gemini-2.5-flash-lite',  |
| X-6 | 「pro」가 «비싼 이름» 을 뜻하는가 | OK 측정됨 | pro 의 첫 모델은 `google/gemini-2.5-flash` 다 — 「pro」가 «비싼 이름» 을 뜻하지 않는다(유료 판정 True). flash 첫 모델 `google/gemini-2.5-flash-lite` |
| E-2 | 고성능 등급이 모든 곳에 쓰이는가 | OK 측정됨 | 고성능 등급 1573/2323 (68%) · 저성능 750 · 강등된 호출 114건 — 등급이 실제로 갈리고 강등도 일어난다 |
| E-2 | 단계마다 모델 조합이 다른가 | OK 측정됨 | 단계 10종 · 서로 다른 모델 조합 9가지 · 상위: CODE_REVIEW 791회, TECH_SPEC 532회, CLARIFICATION 87회 |
| E-3 | 회차 비교가 가능한가 | OK 측정됨 | 회차 짝 4묶음 · 나중 회차가 더 싼 경우 1 — CRM: CRM002 $6.73(411회) → CRM003 $3.24(227회) / live-walk: live-walk-01 $0.01(7회) → live-walk-03 $0.96(103회) / test_a1_unitconv_canary: test_a |

---

## 판정

### ⚠️ 미측정 — 「잴 수 없다」는 E 트랙의 실패 조건이다

- **E-1 «승인된 결과물 1건당» 을 낼 수 있는가** → 승인 발간물 2건이지만 **호출과 잇는 키가 없다** — 호출 로그는 `project_id` 로, 발간물은 `source_id`(결정 안건)로 묶인다. 둘을 잇는 열이 어느 쪽에도 없어 «1건당 비용» 을 못 낸다


⚠️ 이 결과가 «주장하지 않는» 것

- **비용이 적정한지 판정하지 않았다.** 「$19.83 이 싼가」는 사람이 정한다.
- E-3 은 «짝이 있다» 까지만이다. 회차 이름이 «같은 일을 두 번» 이라는 보장이 없으므로, 싸진 것을 축적 효과라고 부르지 않았다.
- 이 로그는 **개발 중 호출**이 섞여 있다. 운영 단가로 읽으면 안 된다.
