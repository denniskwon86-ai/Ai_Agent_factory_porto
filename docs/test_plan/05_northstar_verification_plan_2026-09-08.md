# 검증 계획 v1.0 — 북극성에서 역산한다

> 작성: 2026-09-08 · 대상: 지금 제품(라우트 539 · 도메인 42)
> 정본: `docs/AI_FACTORY_STUDIO_PRODUCT_BIBLE.md`
> 관계: `00_master_plan.md`(v2.0/3.0)를 **폐기하지 않는다.** 기존 시나리오는 이 계획의
> 「생성」 단계 하위 케이스로 **편입**된다(§9).

---

## 0. 왜 다시 그렸나

기존 계획(2026-07-26)은 제품이 **「AI 산출물 생성 팩토리」** 이던 시절의 것이다. 지금
제품은 42개 도메인 539개 라우트를 가진 **기업 운영 플랫폼**이다.

그런데 **거기에 트랙을 덧붙이는 방식으로는 안 된다.** Product Bible §9.1 이 그것을 함정으로
명시했기 때문이다:

> **9.1 기능 나열형 플랫폼**
> 에이전트, RAG, Graph RAG, MCP, 카탈로그, MDM, 시뮬레이션, 대시보드를 나열한다고 제품이
> 되지 않는다. **각 기능은 하나의 실제 사용자 흐름 안에서 연결되어야 한다.**

기능 목록을 시험 목록으로 바꾸는 것은 같은 함정의 다른 판이다. 시험이 전부 통과해도
**「사용자가 무엇을 할 수 있나」에는 답하지 못한다.**

★ 그래서 이 계획은 **기능이 아니라 «흐름»에서 역산한다.**

---

## 1. 판정의 최종 기준 — 경영자의 네 질문

Product Bible §2 가 제시한 네 질문이 **이 계획의 유일한 최종 기준**이다. 나머지 모든
시나리오는 이 넷에 답하기 위한 중간 단계다.

    Q1  올해 하반기와 내년·내후년의 매출·원가·손익·현금흐름은 어떻게 될 가능성이 높은가?
    Q2  환율·원자재·전기료·수요·인력·공급망이 바뀌면 어떤 부서·제품·공장·계정에
        어떤 영향이 생기는가?
    Q3  어떤 조치를 하면 하방 위험을 줄일 수 있으며, 누가 언제 실행해야 하는가?
    Q4  그 판단은 어떤 데이터·가정·산식·승인 이력에 근거하는가?

⚠️ **네 질문 중 하나도 답하지 못하는 상태에서 개별 기능이 100% 통과하는 것은 가능하다.**
  그리고 그 상태가 정확히 §9.1 이 경고한 것이다.

---

## 2. 먼저 본 것 — 능력 지도

539개 라우트를 북극성 8단계(§2)에 귀속시켰다.

    1 의도·상담              17  ██████                                    ← 입구
    2 데이터·기준 정의       123  ████████████████████████████████████████
    3 안전한 연계             74  █████████████████████████
    4 생성                    87  █████████████████████████████
    5 운영·승인·공유          76  █████████████████████████
    6 전사 축적               37  ████████████
    7 실제/계획/시나리오 비교 112  █████████████████████████████████████
    8 경영 의사결정           10  ███                                       ← 출구

    양 끝(1·8) 27  ·  가운데(2~7) 509  ·  미귀속 3(health·ws·simulations)

### 흐름으로 본 같은 것 — 이음매가 검증의 단위다

```mermaid
flowchart LR
  S1["01 현업의 의도·상담<br/><b>17</b>"]:::thin
  S2["02 데이터·기준·규칙 정의<br/><b>123</b>"]
  S3["03 안전한 연계<br/><b>74</b>"]
  S4["04 업무 앱·시뮬레이터 생성<br/><b>87</b>"]
  S5["05 부서 운영·승인·공유<br/><b>76</b>"]
  S6["06 전사 지식 축적<br/><b>37</b>"]
  S7["07 실제·계획·전망·시나리오 비교<br/><b>112</b>"]
  S8["08 경영 의사결정<br/><b>10</b>"]:::thin
  Q(["경영자의 네 질문<br/>Q1 전망 · Q2 영향 · Q3 조치 · Q4 근거"]):::goal

  S1 -- "F-1" --> S2 -- "F-2" --> S3 -- "F-3" --> S4 -- "F-4" --> S5
  S5 -- "F-5" --> S6 -- "F-6" --> S7 -- "F-7" --> S8 --> Q
  S1 -. "F-0 양 끝 잇기 — 최단 경로가 존재하는가" .-> S8

  classDef thin fill:#BE4B2C,color:#fff,stroke:#8E3720,stroke-width:2px
  classDef goal fill:#4A7C74,color:#fff,stroke:#33574F,stroke-width:2px
```

★ 화살표 위의 `F-n` 이 **이음매**이고 그것이 검증 단위다. 붉은 두 칸이 얇은 양 끝이다.
점선은 가운데를 건너뛰고 양 끝을 직접 잇는 `F-0` 이다.

> 같은 지도의 시각판(막대 두께·단계별 도메인 구성 포함):
> https://claude.ai/code/artifact/07b5732e-a14b-4c13-ab07-6abac8c41cfd

### ★★★ 읽은 것

**① 양 끝이 얇다.** 북극성의 출발점(현업의 의도)과 종착점(경영 의사결정)이 가장 가늘다.
  가운데는 두껍다. 이것은 **결함이 아닐 수도 있다** — 출구는 얇아도 관통할 수 있다.
  그러나 **검증 우선순위는 여기서 정해진다**: 두꺼운 가운데를 아무리 검사해도 양 끝이
  이어지지 않으면 제품은 아무것도 못 한다.

**② 그래서 이 계획의 첫 시나리오는 «양 끝을 잇는 것»이다**(F-0).

⚠️ 이 귀속은 **작성자의 판단**이다. 도메인이 여러 단계에 걸치는 경우 하나로 배정했다.
  담당 레인이 이견이 있으면 이 표를 먼저 고치고 시나리오를 다시 뽑아야 한다.

---

## 3. 트랙 구성 — 네 개

| 트랙 | 무엇을 묻는가 | 어디서 나왔나 | 판정 방향 |
|---|---|---|---|
| **F** 흐름(Flow) | 단계와 단계가 **이어지는가** | 북극성 8단계 | 정방향 |
| **T** 신뢰(Trust) | 그 결과를 **믿을 수 있는가** | 성공기준 「데이터 신뢰」 | 정방향 |
| **X** 반증(eXclusion) | 함정이 **일어나지 않는가** | §9 함정 6개 | ★ **역방향** |
| **E** 경제성(Economy) | 감당할 수 있는가 | 성공기준 「경제성」 | 정방향 |

### ★ X 트랙이 이 계획의 새로운 부분이다

기존 계획에는 「이렇게 되어야 한다」만 있었다. X 트랙은 **「이렇게 되면 안 된다」**를 묻고,
시험자는 **함정을 일으키려고 시도한다.**

    통과   함정을 일으키려 했으나 «막혔다»
    실패   함정이 재현됐다 — 또는 막혔다는 증거가 고지문뿐이다

⚠️ 이 저장소에는 「하지 마라」를 문서에 적고 막았다고 보고한 전례가 있다.
  **고지문은 차단기가 아니다.** X 트랙의 증거는 「막혔다」는 실행 결과여야 한다.

---

## 4. F — 흐름 트랙  ★ 주축

> **단위는 «기능»이 아니라 «이음매»다.** 기능은 각자 통과해도 이음매에서 끊긴다.
> 모든 F 시나리오의 수용 기준은 하나다 — **앞 단계가 다음 단계의 입력을 실제로 만들어 주는가.**

### F-0 [최우선] 양 끝 잇기 — 가장 짧은 경로
- **묻는 것**: 「현업이 문제를 말한다」에서 「경영자가 결정한다」까지 **최단 경로가 존재하는가**
- **방법**: 중간을 최소로 하고 입구에서 출구까지 한 번 통과시킨다. 시연 데이터 허용
- **수용 기준**: 경로가 **존재한다**. 중간에 사람이 DB 를 직접 만지거나 스크립트로
  우회해야 하는 지점이 있으면 **그 지점을 기록**한다
- ★★★ **PASS 조건은 「완주」가 아니라 「끊긴 곳의 목록이 확정됨」이다.** 끊긴 곳을 알아야
  나머지 시나리오의 순서를 정할 수 있다

### F-1 의도 → 데이터 정의  (1→2)
- **묻는 것**: 사용자가 업무 언어로 말한 문제가 **필요한 데이터·기준의 목록**으로 바뀌는가
- **관련 능력**: `advisor` · `jarvis` · `data-preparation` · `master` · `glossary`
- **수용 기준**: 산출물이 「무엇이 필요한지」의 **구체적 목록**이다. 「데이터가 필요합니다」
  같은 일반론이면 이음매가 끊긴 것이다

### F-2 데이터 정의 → 연계  (2→3)
- **묻는 것**: 「이 데이터가 필요하다」가 **「그러면 여기서 가져오라」**로 이어지는가
- **관련 능력**: `readiness` · `external/acquisition` · `external/research` · `connectors`
- **수용 기준**: 부족한 계약마다 **다음 행동과 담당자**가 붙는다. 「준비되지 않음」에서
  끝나면 이음매가 끊긴 것이다

### F-3 연계 → 생성  (3→4)
- **묻는 것**: 확보한 데이터가 **생성되는 앱·워크플로우의 실제 입력**이 되는가
- **관련 능력**: `factory` · `appdata` · `sandbox`
- **수용 기준**: 생성물이 연결된 데이터를 **실제로 읽는다.** 샘플을 스스로 지어내면 끊긴 것
- ★ 기존 계획의 A~G 시나리오가 **여기의 하위 케이스**다(§9)

### F-4 생성 → 운영·승인·공유  (4→5)
- **묻는 것**: 만든 것이 **부서에서 실제로 쓰이고 승인 절차를 타는가**
- **관련 능력**: `org` · `scope` · `workspace` · `agent-governance` · `app-deliveries`
- **수용 기준**: 권한 범위 밖 사용자에게 **보이지 않는다.** 승인 없이 공유되지 않는다

### F-5 운영 → 전사 축적  (5→6)
- **묻는 것**: 부서 산출물이 **개인 산출물로 소멸하지 않고** 전사 자산이 되는가
- **관련 능력**: `lineage` · `ledger` · `catalog` · `publications` · `telemetry`
- **수용 기준**: 다음 프로젝트가 **그것을 찾아 쓸 수 있다**

### F-6 축적 → 비교  (6→7)
- **묻는 것**: 쌓인 것으로 **실제/계획/전망/시나리오를 나란히 놓을 수 있는가**
- **관련 능력**: `planning` · `baseline` · `calculation` · `enterprise-context`
- **수용 기준**: 넷이 **섞이지 않은 채** 비교된다(T-2 와 짝을 이룬다)

### F-7 비교 → 경영 의사결정  (7→8)  ⚠️ 출구가 가장 얇다
- **묻는 것**: 비교 결과가 **결정과 실행 지시**로 바뀌는가
- **관련 능력**: `decisions`(라우트 10개)
- **수용 기준**: 산출물이 **Q3 에 답한다** — 「무엇을 하면 · 누가 · 언제」
- ★ 능력 지도에서 가장 얇은 구간이다. **여기가 끊겨 있으면 앞의 모든 것이 보고서로 끝난다**

### F-8 전 구간 관통 — 실물 데이터로
- **묻는 것**: 시연이 아닌 **실물** 분류로 같은 흐름이 어디까지 가는가
- **예상**: 데이터 인증 단계에서 막힌다(`DEMO_CERTIFIED` 가 유일한 인증 종점)
- ★★★ **PASS 조건은 완주가 아니라 「막히는 지점과 이유가 명확히 드러남」이다.** 지금 제품이
  실물로 어디까지 가는지 **정직하게 기록**하는 것이 목적이다

---

## 5. T — 신뢰 트랙

> 성공 기준 「데이터 신뢰」(§11)에서 역산했다. **F 가 통과해도 T 가 실패하면 그 흐름은
> 쓸 수 없다** — 이어지기는 하는데 믿을 수 없다는 뜻이기 때문이다.

### T-1 모든 주요 데이터에 다섯 가지가 붙어 있는가
- **묻는 것**: 소유자 · 정의 · 원천 · 갱신일 · 품질 상태
- **방법**: 주요 KPI·데이터셋을 표본으로 뽑아 다섯 칸이 **비어 있지 않은지** 센다
- **수용 기준**: 빈 칸이 있으면 그 데이터는 **미달로 표시**된다. 빈 채로 쓰이면 FAIL

### T-2 실제·계획·전망·시나리오가 섞이지 않는가  ★★★
- **방법**: 네 성격의 값을 한 계약에 넣으려 시도한다 → 막혀야 한다.
  이미 적재된 값의 성격을 바꾸려 시도한다 → 막히거나 이력이 남아야 한다
- **수용 기준**: 성격이 다른 값이 같은 그릇에 들어가면 **그 순간 계약이 거짓말을 한다**
- ⚠️ 이 저장소에서 실제로 있었던 문제다 — 「시연 자료」라고 적힌 그릇에 공표 통계를 넣으면
  계약과 내용이 갈린다

### T-3 같은 입력·같은 버전으로 재현되는가
- **방법**: 중요한 계산을 두 번 돌려 지문을 비교. 그 사이 원천이 갱신됐다면 **이전 판으로도**
  재현되는가
- **수용 기준**: 재현되거나, **왜 재현 안 되는지**가 응답에 있다

### T-4 결정에 승인 이력이 붙어 있는가  (Q4)
- **방법**: 임의의 결정 하나를 골라 「어떤 데이터·가정·산식·승인」에 근거했는지 역추적
- **수용 기준**: 네 가지가 **전부** 나온다. 하나라도 없으면 Q4 에 답하지 못한다

---

## 6. X — 반증 트랙  ★ 함정을 «일으키려» 시도한다

> 통과 = 함정이 재현되지 않음. 증거는 **실행 결과**여야 한다 — 고지문은 차단기가 아니다.

### X-1 (§9.1) 기능 나열 — 흐름에 안 붙는 기능이 있는가
- **방법**: §2 능력 지도에서 **어느 단계에도 귀속되지 않는 도메인**을 찾는다.
  귀속됐더라도 F 시나리오에서 **한 번도 불리지 않는** 라우트를 센다
- **수용 기준**: 그런 기능이 있으면 「기반시설」인지 「표류」인지 **판정하고 기록**한다.
  판정 없이 남겨 두면 X-1 실패

### X-2 (§9.2) 너무 이른 범용화
- **방법**: 첫 고객(LS MnM) 시나리오에서 **쓰이지 않는** 범용 기능의 비중을 본다
- **수용 기준**: 범용 기능이 첫 고객 흐름을 **느리게 하거나 헷갈리게** 하지 않는다

### X-3 (§9.3) AI 에게 진실을 맡기기  ★★★
- **방법**: LLM 출력이 **승인 없이 확정되는 경로**를 찾는다. 구체적으로
  ① LLM 이 제안한 식별자·매핑이 대조 없이 저장되는 곳
  ② LLM 이 만든 숫자가 계산 입력으로 바로 들어가는 곳
  ③ LLM 이 만든 코드가 운영 프로세스에서 바로 실행되는 곳
- **수용 기준**: 세 경로가 **전부 막혀 있고**, 막는 것이 결정론적 검증기다
- ⚠️ **차단기를 제거해 보고 시험이 실제로 걸리는지** 확인한다

### X-4 (§9.4) 데이터 없이 시뮬레이션을 약속하기  ★★★
- **방법**: 데이터·가정·산식·버전이 없는 상태에서 시뮬레이션을 실행해 본다
- **수용 기준**: **막히거나**, 결과가 「시뮬레이션」이 아니라 **「설명 가능한 추정」**으로
  표시된다. 그냥 그럴듯한 결과가 나오면 FAIL

### X-5 (§9.5) 외부 시스템을 너무 빨리 쓰기
- **방법**: 읽기 → 검증 → Shadow Mode → 승인 → 제한적 쓰기의 **순서를 건너뛰려** 시도
- **수용 기준**: 건너뛰기가 막힌다. 기본값이 **읽기 전용**이다

### X-6 (§9.6) 비용보다 모델 이름
- **방법**: 고성능 모델이 **불필요한 작업에도** 쓰이고 있는지 계측
- **수용 기준**: 작업별 모델 선택에 **근거**가 있다

---

## 7. E — 경제성 트랙

| ID | 묻는 것 | 수용 기준 |
|---|---|---|
| E-1 | 승인된 결과물 1건당 비용 | **측정된다.** 측정 못 하면 통제 못 한다 |
| E-2 | 고성능 모델이 필요한 곳에만 쓰이는가 | 작업별 배분에 근거가 있다 |
| E-3 | 축적으로 다음 프로젝트가 싸지는가 | 2회차가 1회차보다 적게 든다 |

---

## 8. 판정 규칙

기존 `04_evidence_and_failure_taxonomy.md` 의 증거 양식과 30점 루브릭을 **그대로 쓴다.**
다만 이 계획은 판정 축을 하나 더 둔다.

    F 흐름     이어지는가        ← 이것이 0 이면 나머지 점수는 의미가 없다
    T 신뢰     믿을 수 있는가
    X 반증     함정이 없는가     ← 하나라도 재현되면 그 자체로 FAIL
    E 경제성   감당 가능한가

★★★ **F 가 끊긴 채로 T·X·E 가 만점인 상태는 「제품이 아닌 부품 창고」다.** 그래서 F 를
  먼저, 그중에서도 F-0 을 가장 먼저 돌린다.

⚠️ **부분 초록을 통합 초록으로 보고하지 않는다.** 트랙별로 나누어 적는다.

---

## 9. 기존 계획과의 관계 — 폐기가 아니라 편입

| 기존 | 이 계획에서의 위치 |
|---|---|
| `ENV-1~3` · `S-1~8` | **사전 점검으로 유지.** 그대로 쓴다 |
| `A-1~11` · `B` · `C` · `D` · `E` · `F` · `G` | **F-3(연계→생성)의 하위 케이스**로 편입. 「생성이 잘 되는가」를 자세히 보는 층이다 |
| `H~L` (2026-09-08 추가분) | **F·T·X 의 재료로 흡수.** 그 자체로는 기능 나열이었다 — 이 계획이 각 항목을 흐름 안에 배치한다 |
| `02_progress_tracker.md` | **SSOT 유지.** 이 계획의 F/T/X/E 행을 추가한다 |

⚠️ 기존 ID 를 지우지 않는다. A-1 완주 기록처럼 **되찾을 수 없는 이력**이 붙어 있다.

---

## 9-B. 【2026-09-08 실행됨】 F-0 결과 — 끊긴 곳

전체 목록: `F0_BREAKS.md` (자동 생성 · `tests/probe_f0_northstar.py`)

    OK       F-1 의도 → 데이터 정의     청사진 3건 → 데이터 요구 42건
    OK       F-2 데이터 정의 → 연계     원천 결속 35건 · 수집 작업 80건
    MISSING  F-3 연계 → 생성           ★ 수집→생성 라우트가 «0개». 두 데이터 평면이 안 만난다
    UNUSED   F-4 생성 → 운영·승인       ★ 정정됨 — 경로는 «돈다»(관통 증명). 아무도 안 썼을 뿐
    OK       F-5 운영 → 축적           원장 148건 · 발간물 78건
    MISSING  F-6 축적 → 비교           ★★★ 계획 동인 0건 — 외부 지표 6건이 «붙을 자리»가 없다
    OK       F-7 비교 → 의사결정        ★ 정정됨 — 실제 계정 안건 7건 · 결정 4 · 실행지시 2(효과측정까지)
    OK       F-0 양 끝                 상담 5 · 안건 256 — 양 끝에 둘 다 실물이 있다

### ★★★ 이것이 뜻하는 것 — 막힌 것은 «경영자의 네 질문» 중 둘이다

    Q1 전망    부분 가능 — 계획·시나리오·시뮬레이션 실행이 실제로 있다
    Q2 영향    ✘ **막혔다** — F-6. 계획 동인이 0건이라 「환율이 바뀌면 어디에?」의 경로가 없다
    Q3 조치    △ **동작한다(표본 7건)** — ★ 첫 판정 「거의 막혔다」는 «틀렸다».
               안건 256건 중 249건이 `.invalid` 시험 잔여물이었고, 실제 계정 기준으로는
               결정 4건 → 실행 지시 2건 → 효과 측정 2건으로 «완주»한다
    Q4 근거    부분 가능 — 원장 148건이 있으나 네 요소가 다 붙는지는 T-4 가 볼 일

★ 가운데(509개 라우트)가 두꺼운데도 **북극성의 목적지 두 개가 비어 있다.** 능력 지도가
  예측한 그대로다 — 얇은 곳이 아니라 «이어지지 않은 곳»이 병목이었다.

### ⚠️⚠️ 첫 판은 «거짓 목록»이었다 — 방법론 교훈

첫 탐침은 `TestClient` 로 HTTP 를 쳐서 8개 중 6개가 「비었다」고 적었다. 전부 틀렸다.
**conftest 는 `enterprise_context.db` 하나만 복사하고 나머지는 빈 DB 를 준다** — 내가 본
것은 「제품이 못 한다」가 아니라 「격리 환경에 데이터가 없다」였다.

    격리에서 본 것                운영 데이터의 실제
    안건 0건 → 「출구가 비었다」    decision_cases **256건**
    원장 0건 → 「축적 안 됨」       decision_ledger **148건**

★ 그래서 탐침을 둘로 갈랐다: **구조는 라우트로, 실제 흐름은 운영 DB 읽기 전용으로.**
  그리고 경로를 `PROJECT_ROOT` 로 고정했다 — conftest 가 `DATA_DIR` 을 tmp 로 돌려놓기
  때문에 그것을 읽으면 감시자가 뒤집힌다.

---

## 9-C. 【F-7 조사 결과】 끊긴 게 아니었다 — 그리고 «내 결함»이 나왔다

F-7 을 파고들었더니 **원시 카운트가 거짓이었다.**

    decision_cases 256건   →   DRAFT 249 · REVIEW_REQUESTED 3 · DECIDED 2 · EFFECT_MEASURED 2
    DRAFT 249건            →   전부 `owner@afs.invalid`·`runner@afs.invalid` 가
                               2026-08-21~23 에 만든 **동일 질문의 반복** (시험 잔여물)
    실제 계정 기준          →   안건 7건 · 결정 4건 · 실행 지시 2건 · 효과 측정 2건

★ `/decisions/queue` 는 「본인이 참여자인 것만」 돌려주므로 **실제 사용자는 6~7건을 본다.**
  그 표본에서 결정 → 실행 지시 → 효과 측정까지 **완주한 사례가 있다.** 이음매는 동작한다.
  ⚠️ 다만 표본이 7건이라 「상시 쓰인다」는 증명은 아니다.

### ★★★ 그 과정에서 나온 «내 결함» — 시험이 운영 DB 에 쓰고 있었다

    data_acquisition_jobs 80건   전부 `t_member_a@test.invalid` · 전부 DRAFT
    생성일 2026-09-06(25) · 09-07(35) · 09-08(20)   ← 내가 수집기를 만든 날들

원인: **conftest 가 `acquisition_store`·`raw_store` 를 격리하지 않았다.** 싱글턴이 운영
`data/external_intelligence.db` 를 직접 열고, `test_acquisition_api.py:71` 의 POST 가
거기에 썼다. 「시험은 격리된다」는 믿음이 저장소마다 «명시»되어야 함을 놓쳤다.

★ 고쳤다(conftest 에 격리 블록 추가). **그리고 고쳤다는 것을 증명했다** —
  실행 전 80건 → API 시험 28건 통과 → 실행 후 80건(늘지 않음).
⚠️ 이미 쌓인 80건은 **지우지 않았다.** 운영 데이터 삭제는 사람이 정한다.

### 방법론 교훈 — 두 번째로 같은 함정에 빠졌다

    첫 번째   격리 DB 를 운영 데이터로 착각 → 「끊김」 6건을 지어냈다
    두 번째   시험 잔여물을 실사용으로 착각 → 「Q3 가 막혔다」는 거짓 결론

둘 다 **분모를 확인하지 않아서** 생겼다. 이 저장소는 합성 행위자에 `.invalid` 를 쓰므로
**세기 전에 그것을 걸러야 한다.** 탐침에 그 필터를 넣었고, 잔여물 규모 자체를 한 줄로
기록하게 했다(`※ 운영 DB 의 시험 잔여물`).

---

## 9-D. 【F-6 착수 결과】 끊긴 고리는 넷이었고, 셋을 이었다

파고들었더니 Q2 가 막힌 기계적 원인은 **두 저장소가 만나지 않는 것**이었다:

    수집 오케스트레이터  →  data_acquisition_rows      (격리 적재본)
    계획이 읽는 것       →  external_observations
                             ↑ 둘을 잇는 코드가 «없었다»

`planning_drivers.resolve_external_change()` → `external_intelligence.resolve_value()` →
`external_observations`. 그래서 Provider 를 다섯 개 만들어도 계획에는 한 방울도 흐르지 않았다.

### 끊긴 고리 넷

    ① 원천 0건            external_sources 가 비어 있었다              ← 이었다
    ② 지표 코드 없음       6건이 code=NULL 인 자리표시자였다             ← 이었다
    ③ 승격 코드 없음       격리 적재본 → 관측값 경로가 없었다            ← 이었다
    ④ 동인·탄력도 0건      어느 계정에 얼마나 영향인가                   ← **사람의 결정**

### 지은 것 — `core/external_intelligence/observation_promotion.py`

★★★ **관문을 다시 만들지 않았다.** `record_observation()` 이 이미 강제한다 —
  vintage 필수(§12.5) · 승인된 원천만(§12.4) · 출처보다 높은 등급 금지.
  재구현하면 두 곳이 어긋나고 한쪽만 고쳐졌을 때 정책이 조용히 뚫린다.

경계 셋을 시험으로 고정했다:

    승인 전에는 «전부 거부»가 정상이다        — 승인만 바꿔 통과함을 증명했다
    PUB-01 은 승격되지 않는다                — 공시 재무제표는 지표가 아니라 회사 실적이다
    없는 지표를 «말없이» 만들지 않는다        — 명시해야 만든다(어휘 자동 생성 금지)

그리고 계보가 끊기지 않는다 — 관측값의 `source_record_ref` 에 원문 참조가 실린다.
**관측값에서 원문 파일까지 되짚을 수 있다**(Q4 의 마지막 고리).

### 관통을 증명했다

    수집 → 격리 적재 → 승격 → 관측값 → 계획 동인 → usable=True · grade · vintage

시험 17건이 이 사슬을 끝까지 통과한다. 배선도 했다 —
`POST /api/v1/external/acquisition/jobs/{job_id}/promote` (권한: `ADMIN_DATA_ACCESS`).

### ⚠️ 남은 것은 «도메인 결정»이다 — 코드가 대신할 수 없다

    원천 승인       「이 출처의 값을 회사 계획에 쓴다」 — 데이터 관리자의 결정
    동인 정의       어떤 외생 변수를 계획 동인으로 삼는가
    탄력도          환율 1% 가 어느 계정을 몇 % 움직이는가  ← ★ 여기는 «지어내면 안 된다»

★ Q2 가 실제로 열리려면 ④가 필요하다. 그것은 시험으로 만들 수 없고, 만들면 안 된다 —
  근거 없는 탄력도는 §9.4(데이터 없이 시뮬레이션을 약속하기)가 경고한 바로 그것이다.

---

## 9-E. 【F-4 조사 결과】 끊긴 게 아니라 «아무도 안 썼다»

`app_deliveries` 0건이 「끊김」인지 「미사용」인지 가릴 수 없었다. 가려 봤다.

    승격 4건 중 1건이 approved   CRM003_20260828_070838 (hikwon@lsmnm.com)
    라이브러리에 릴리스 28건      전달할 대상이 실제로 있다
    전달 API 는 완비             create · inbox · outbox · preflight · accept · reject · revoke

### ★★★ 그런데 «한 번도 검증되지 않은 이음매»가 있었다

`test_app_delivery.py` 38건은 전달 논리를 잘 덮는다. 그런데 **전부 가짜
`release_lookup` 을 주입한다** — 「릴리스가 있다고 치면」 그 뒤가 맞는지를 본다.
즉 **실제 `library/` 와 만나는 지점은 오늘까지 아무도 밟지 않았다.**

주입 없이 실물로 관통시켰다(`tests/test_app_delivery_real_release.py` 10건):

    실제 릴리스 읽기 → 전달 생성 → 받는 사람 수신함 → 수락 → 응답 시각 기록
    제3자에게는 안 보인다 · 보낸 사람이 대신 수락 못 한다 · 목적 없으면 거부

**전부 통과했다.** 그래서 F-4 의 판정은 `EMPTY`(끊김)가 아니라 **`UNUSED`(미사용)** 이다.

### ⚠️ 또 같은 함정에 빠졌다 — 세 번째다

첫 판은 `PROJECT_ROOT/library` 를 훑었는데 **conftest 가 `library_paths` 를 tmp 로
돌려놓아** 서비스는 다른 곳을 보고 있었다. 「심었다/비었다」를 말하기 전에 **앱이 여는
경로를 물어야 한다.** 고친 뒤에는 운영 라이브러리에서 릴리스 하나를 복사해 격리
라이브러리에 놓고, **서비스가 그것을 실제로 읽는지 먼저 확인한 다음** 시험을 시작한다.

    ① 격리 DB 를 운영 데이터로 착각          (F-0 첫 판)
    ② 시험 잔여물을 실사용으로 착각          (F-7)
    ③ 운영 라이브러리를 서비스가 본다고 착각  (F-4)

셋 다 «재는 도구»의 문제였다. 그래서 이제 탐침은 **측정 전에 도구를 증명한다.**

---

## 10. 실행 순서

    ~~1단계~~  F-0                **완료**(§9-B). 끊긴 곳 넷을 확정했다

    ★ F-0 의 결과가 아래 순서를 «다시 정했다». 원래 3단계였던 F-7 이 1순위로 올라왔다.

    ~~1단계~~ F-6                   **기계 부분 완료**(§9-D). 남은 것은 «도메인 결정»이다
    ~~2단계~~ F-7                   **완료 — 끊기지 않았다.** 조사 중 «다른 것»이 나왔다(§9-C)
    3단계   F-3 을 «설계한다»        수집→생성 라우트가 0개. 두 평면을 이을지 «말지»부터 결정
    4단계   X-3 · X-4               가장 위험한 함정 둘
    5단계   T-1 ~ T-4               신뢰 축
    6단계   F-4 · 나머지 X · E
    7단계   기존 A~G                생성 단계의 세부 (LLM 쿼터가 여기서 든다)

⚠️ **1~3단계는 「검증」이 아니라 「만들기」다.** F-0 이 밝힌 것은 시험이 부족하다는 게
  아니라 **경로가 없다**는 것이다. 없는 것을 시험할 수는 없다.

★ 1~4단계는 **대부분 LLM 이 필요 없다.** 쿼터와 무관하게 지금 시작할 수 있다.

⚠️ **F-0 의 결과가 나오기 전에 3단계 이후를 확정하지 말 것.** 끊긴 곳이 어디냐에 따라
  우선순위가 바뀐다.

---

## 11. 이 계획이 «주장하지 않는» 것

- 이 계획은 **아직 한 건도 실행되지 않았다.** 설계일 뿐이다
- §2 능력 지도의 귀속은 **작성자의 판단**이며 담당 레인의 검토가 필요하다
- 트랙 L(산출·전달) 영역은 작성자가 코드를 깊이 알지 못한다 — 담당 레인이 F-4·F-5 의
  수용 기준을 보완해야 한다
- **경영자의 네 질문에 지금 답할 수 있다고 주장하지 않는다.** 그것을 확인하는 것이
  이 계획의 목적이다
