# F-8 — 실물 데이터로 전 구간을 걸었다

> 자동 생성: `tests/probe_f8_real_walk.py`. **고쳐 쓰지 말 것** — 다시 돌리면 덮인다.
> ★★★ **PASS 조건이 다른 트랙과 반대다**(계획 §4 F-8): 「완주」가 아니라 **「막히는 지점과 이유가 명확히 드러남」**이다.
> 그래서 `막힘·설명` 은 **통과**이고, `막힘·침묵` 만 실패다.

    이음매 8개 · 흐름 7 · 막힘·설명 1 · **막힘·침묵 0** · 탐침 오류 0

| 이음매 | 판정 | 사실 |
|---|---|---|
| F-1 의도 → 데이터 정의 | →  흐름 | 상담 5 · 청사진 3 → 데이터 요구 **42건**. 표본: «조직 · 법인(master_org) · 필요도 required · 담당 hq» — 일반론이 아니라 «이름·필요도·담당» 이 붙은 목록이다 |
| F-2 데이터 정의 → 연계 | →  흐름 | 결속 35 · 수집 작업 84. EXT-02 실물이 `READY` 다 — 막히지 않았으므로 다음 행동이 없는 것이 정상이다 |
| F-3 연계 → 생성 | →  흐름 | 실물 240행이 `SOURCE_CERTIFIED` 로 인증되어 준비도 `READY` 다 — 인증판만 읽는 기준선·계산·색인이 이제 이 자료를 «본다». 시연 판 1개와 섞이지 않고 나란히 있다 |
| F-4 생성 → 운영·승인 | ✋ 막힘·설명 | 릴리스 데이터 결속 69건은 있으나 **전달 0건** — 경로는 검증됐고(§9-E) 아직 «안 썼을» 뿐이다 |
| F-5 운영 → 전사 축적 | →  흐름 | 발간물 78건(승인 **2건**) · 원장 175건. 다음 프로젝트가 찾을 수 있는 형태로 남아 있다 |
| F-6 축적 → 비교 | →  흐름 | 관측값 **240건**(실물) · 동인 2건. 시나리오 용도는 열린다(구리 11785.25). ✋ 기준계획은 등급으로 막히고 이유를 말한다 — 「'baseline_plan' 용도는 최소 'gold' 등급이 필요한데 보유한 최상위 등급은 'silver' 」 |
| F-7 비교 → 의사결정 | →  흐름 | 실제 계정 안건 **7건** → 결정 4 → 효과측정 2 · 실행과제 2건. (전체 256건 중 249건은 자동 실행기 소유라 제외했다) |
| **F-8 실물 종합** | →  흐름 | 실물이 **① 수집 → ② 승격(관측값 240건) → ③ 계획 동인 → ④ 시나리오 계산 → ⑤ 업무키트 스냅샷 `SOURCE_CERTIFIED`(1판)** 까지 **끝까지 흐른다.** ⚠️ 다만 이 인증은 «발행 기관이 따로 있는 공표 자료» 전용이다 — **회사 실적(자사 매출·원가·생산)에는 여전히 인증 종점이 없고**, 그것은 실제 Data Owner 가 서명하는 일이라 코드가 열 수 있는 것이 아니다 |

---

## 판정

### F-8 **통과** — 막히는 곳마다 제품이 이유를 말한다

★ 이것이 계획이 요구한 것이다. 「실물이 끝까지 간다」가 아니라 **「어디서 왜 멈추는지 사람이 알 수 있다」**.

## ⚠️ 이 걸음이 «주장하지 않는» 것

- **화면이 이 사실들을 보여 주는지는 모른다.** 특히 F-3 의 「준비도는 `READY` 인데 가리키는 판은 시연」 — 응답이 `data_kind` 를 함께 주지만 화면이 그것을 안 그리면 사용자는 실물이 올라간 줄 안다(Codex 레인).
- F-1·F-5 의 자료 중 일부는 **시연 계보**다. 실물로 표시된 것은 관측값 240건과 EXT-02 스냅샷뿐이다.
