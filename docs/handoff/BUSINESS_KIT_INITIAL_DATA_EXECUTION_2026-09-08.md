# 업무키트·초기 데이터 보강 실행 기록

기록일: 2026-09-08 · 기획 및 개발: 권혁일(Hyuk il Kwon)

## 1. 이번 작업의 목적과 범위

업무키트는 CSV 묶음이 아니라 **데이터 계약·현업 입력·규칙·에이전트 구성·계산·결과 활용**을 묶은 실행 자산이다. 구매·생산·판매의 업무 결과가 같은 회사, 기준일, 기준선, 전사 시나리오에 연결되는 것을 완료 기준으로 한다.

진행 순서는 짧은 업무키트 사용성 보강 → PostgreSQL 전환 → 서비스 서버 → 현업 사용 및 데이터 확장이다. 저장소 전환 준비는 병행하지만 이번 변경으로 DB를 바꾸거나 새 서버를 구매하지 않는다. 샘플과 실제 입력은 같은 제품 경로를 사용하되 **자료의 성격·출처·사용 가능 상태는 구분**한다.

## 2. 정확한 모수

정본: `core/data_preparation/business_kits.py`, 비철금속 패키지 `1.0.0/manifest.json`.

| 구분 | 데이터 계약 수 | 현업이 할 일 | 관련 앱/현재 주의점 |
|---|---:|---|---|
| 공통 기반팩 | 11 | 조직·품목·거래처·창고·BOM·계정·단위 정렬 | 모든 앱의 공통 참조; 조직 파일과 실제 권한 설정은 별개 |
| 구매·원료 도입관리 | 2 | 계약·발주·납기와 도입 지연 검토 | 원료 도입 앱; 환율/가격 모델까지 실행된다고 확대하지 않음 |
| 국제물류·도착관리 | 5 | 예정/실제 사건과 도착 현황 확인 | 물류 사건 앱은 현재 조회 중심 |
| 재고·수급관리 | 2 | 가용량·안전재고·입출고 대조 | 재고·생산 영향 앱 |
| 생산계획·자재영향 | 3 | BOM 소요량·생산 배분·차질 확인 | 자재 제약 계산과 전체 설비 최적화는 구분 |
| 품질·Lot 추적 | 2 | 검사·Lot·근거 문서 조회 | 품질 데이터가 있다고 자동 품질 판정이 완성된 것은 아님 |
| 판매·수요·매출시점 | 1 | 수주·출하·납기와 매출 시점 검토 | 판매 앱; 시점 이동을 이익 증가로 표현하지 않음 |
| 원가·현금·경영전망 | 3 | 원가·채권채무·전표 대조 | 구매원가·현금 앱; 전사 전체 손익 모델과 구분 |
| 공급위험·시나리오·의사결정 | 6 | 외부지표·가정·부서 기여·대안 비교 | 공급 위험 앱은 조회 중심, 전사 통합 앱은 별도 |
| 합계 | 35 | 업무영역 8 + 공통 기반팩 | 실행 앱 설계 7종과 같은 분모가 아님 |

인사·투자·자산 등 기업 전체 업무가 8영역으로 모두 구현됐다는 의미도 아니다. 우선 비철금속 구매→생산→판매→재무 경로를 실제로 사용할 수 있게 만들고, 후속 영역은 같은 6요소 계약으로 확장한다.

## 3. 이번 구현

### 현업 시작 안내와 자료 등록 연결

- 데이터 준비 화면에서 업무영역을 펼치면 **예시 질문 / 준비할 자료 / 대조할 것 / 결과 활용 / 현재 사용 범위**를 제공한다. 공통 기반팩을 포함한 9개 안내가 있다.
- 서버가 돌려준 업무영역과 데이터만 표시한다. 정본 패키지와 개정이 다른 경우 이 안내를 억지로 재사용하지 않는다.
- 데이터 행의 `자료 준비`로 해당 자료의 원천·등록 행에 이동한다. 회사/범위를 대신 선택하거나 파일을 자동 업로드하지 않는다.
- 자료 등록과 품질·대사 검사 이후 준비도를 새로 조회한다. 문구만 초록으로 바꾸지 않는다.
- 적용본을 빠르게 바꿨을 때 이전 응답이 다른 적용본의 데이터로 표시되는 것을 막는다.
- 일반 자료 등록 행의 기술 계약키 표시를 제거한다. 이름 없는 자료는 결손 문구로 표시한다.
- 등록된 행 수를 원천 행 수로 그대로 넘기던 자기 대조를 제거했다. 원천 파일/추출 보고서에서 독립적으로 확인한 행 수를 입력해야 검사 버튼이 활성화된다. 행 수가 같다고 금액·수량·업무 연결 대사까지 통과한 것으로 간주하지 않는다.

### 초기 데이터 읽기 전용 검사기

`python -m scripts.audit_business_kit_initial_data --profile quick` 또는 `--profile full`.

검사 범위: 파일/필수열, 선언된 복합 업무키, 회사별 참조키, 샘플 조직 범위, 합성 표시, 발주↔계약 속성, **부분 선적 누적량**, 단위, 유효 BOM, 제품 산식을 사용한 저장 소요량 대사, 통화별 전표 차대 대사.

검사는 샘플 파일만 읽는다. RAW 등록·품질 인증·실행 승인·라이브 DB 쓰기는 하지 않는다. 정상 코드 0도 `PASS_CHECKED_SCOPE`이지 제품에서의 준비 완료가 아니다. 실패는 1, 검사 자체가 불가능하면 2이며 오류 종류별 표본을 따로 남긴다.

생산 대사는 제품의 `calc_projection.project()`와 `calc_models.material_shortage()`를 사용한다. 재고 0은 대사 함수 실행을 위한 진단 입력일 뿐이며 산출된 생산량·부족량을 시연 결과로 내보내지 않는다. 대체 BOM의 동시 유효, 단위 변환 부재는 임의로 고르거나 합치지 않는다.

## 4. 실측된 데이터 공백 — 무조건 대량 등록하지 않는 이유

대상은 **저장소의 샘플 CSV**이며 운영 DB에 현재 연결된 인증판 전체를 검사한 결과가 아니다.

| 샘플 | 행 수 | 확인 결과 |
|---|---:|---|
| quick | 13,585 | 조직 범위 미등재 3,215행, 소요량 대사 실패 333행, 동시 유효 BOM 충돌 167행 (§8 재측정 정정) |
| full | 155,508 | 계획 시점에 유효 BOM 없음 843행, 소요량 대사 실패 5,157행 |

- quick 조직 파일은 7개 노드이며 `plant-afs-smelting-01`이 없다. 해당 범위를 사용하는 자료와 패키지 조직 구성이 맞지 않는다. 현재 운영 ECM에 이 노드가 없다는 뜻은 아니다.
- full 생산계획의 날짜 범위는 2023-08-01~2026-07-30이다. 2024-01-01 전의 계획이 843행이며 BOM 유효 시작 이전이다. 과거 유효기간을 근거 없이 소급 확장하지 않는다.
- quick 첫 생산행의 저장 소요량은 66.4다. 이전에 적은 67.347은 서로 다른 BOM 판을 구분하기 전 값이며 정답으로 사용하면 안 된다. 해당 행은 동시 유효 BOM 충돌 대상이다. §8에서 BOM 판별과 소요량 불일치를 구분했다.
- 이 불일치를 시험에서 감추거나 검사 기준을 낮추지 않는다. 기존 샘플·manifest·엑셀을 덮어쓰지 않았다. `build(clean=True)` 재생성도 실행하지 않았다.
- 검사 통과가 아직 보증하지 않는 것: 재고 이동 전체 대사, 공시 총계와의 대사, 실제 소유권/인증판, 생산→판매 배분 근거, 실제 브라우저 실행, 전체 손익/현금 모델.

## 5. 후속 실행 순서와 완료 기준

### K1. 첫 입력 동선 — 이번 보강 다음 확인

1. 실제 서버에서 업무 데이터 준비 → 구매/생산/판매 영역 → 자료 준비 → 해당 원천 등록 위치를 확인한다.
2. 빈 파일, 필수값 누락, 잘못된 단위, 중복 거래의 행 단위 수정 안내를 확인한다.
3. 파일 등록 직후/검사 후 화면의 준비도 갱신을 확인한다.
4. 잘못된 입력이 기업 전체 수치를 바꾸지 않는지 확인한다.

남은 작업: 사용자용 한글 입력 양식/열 매핑·미리보기, 기준정보 이름 선택과 내부 ID 자동 생성 경로 대조. 기존 관리용 CSV만으로 이 조건이 충족됐다고 하지 않는다.

### K2. 서로 맞물리는 초기 업무 데이터

1. **회사·조직 기준 확정:** 샘플 회사 프로필과 조직도를 대조하고 누락 조직/부서 및 공장별 소유권을 정렬한다. 실제 회사·가상 상세·인증 성격을 별도로 유지한다.
2. **BOM·기간 정렬:** 제품/자재/역할/단위/수율/유효기간을 정본으로 선택하고 저장 소요량을 재계산한다. 과거 BOM 근거가 없으면 그 기간은 자료 보강 대상으로 남긴다.
3. **업무 사슬:** 계약→발주행→분할 선적→도착 사건→창고 입출고→생산→수주→전표를 연결한다. 생산→판매는 제품명이 같다는 이유만으로 자동 배분하지 않는다.
4. **결정론과 대사:** 동일 입력 재실행, 수량 보존, 기초+입출고=기말, 차변=대변, 단위/통화 변환, 중복/누락/타 조직의 부정 사례를 검사한다.
5. **검토 후 등록:** 신규 후보 개정에 원본 지문·변환 규칙·변경 목록을 보관한다. 등록 전에는 이전 인증판을 교체하지 않고, 제품 등록·검사 경로를 통해 반영한다. 두 개의 별도 시연/기능 서버나 이중 정본 DB를 만들지 않는다.

출구: 같은 기준일의 구매·생산·판매가 하나의 전사 시나리오에 저장되고, 부서별 기여 및 그 근거를 사람이 확인할 수 있다. 데이터의 양보다 이 조건을 우선한다.

### K3. 공개자료 기반의 현실성

| 자료 | 수집/정렬 단위 | 반드시 남길 근거 | 대사/제한 |
|---|---|---|---|
| DART 최근 10개 결산연도 + 당기 | 법인·연도·보고서·연결/별도·계정·누적/기간 | 접수번호, 원문, 정정공시, 단위, 수집시각 | 손익·재무상태·현금흐름 총계 대조; 비공개 구매/거래 원장은 얻었다고 하지 않음 |
| 환율·금리 | 지표·통화·관측일·빈도 | 제공자·시리즈·공표시각·이용범위 | 일/월 평균과 말일 값 구분; 회사 실제 결제환율과 별도 |
| 금속가·외부지표 | 상품·품위·시장·가격종류·통화/단위 | 출처와 재배포 가능 범위 | 공개 접근과 재배포 권한 구분; 무단 유료 원천 수집 금지 |
| 회사/산업 공개 분석 | 발행자·발행일·대상·문서 근거 | 원문과 인용 범위·주장/사실 구분 | 분석 의견을 거래 실적으로 전환하지 않음 |
| 합성 업무 상세 | 회사·부서·월·거래·업무 사슬 | 연결한 공개 집계, 배분 가정, 생성 seed, 생성 판 | 공개 총계와 대사하되 모든 상세는 SYNTHETIC 표시 |

중요 정정: 현재 `data_kind`는 공개/비공개 축이 아니라 실제/합성 축이고 `data_origin`이 공개 출처를 표현한다. 실제 공개 관측을 합성으로 바꿔 인증을 통과시키지 않는다. 현재 확인된 공백은 **실제 자료를 위한 인증 경로**이며, 외부 수집기 세션과 공동 계약이 필요하다. 수집 완료와 계산에 사용할 수 있는 인증 완료를 분리한다.

### K4. 앱별로 사람이 실제 수행

구매/생산/판매 앱의 준비자료 선택 → 자료 확인 → 가정 입력 → 실제 계산 → 같은 전사 시나리오 반영 → AI 경영비서 설명 → 안건/보고서 순으로 확인한다. 물류/공급위험의 조회 기능을 신규 입력·자동 추천까지 완성된 것처럼 제시하지 않는다. 타 부서 접근, 자료 누락, 기준선 불일치에서도 올바른 다음 행동이 보여야 한다.

### DB 준비 및 이전 경계

이번에는 코드와 파일명만 조사했다. 기존 SQLite 인벤토리의 라이브 DB 열기 기능은 실행하지 않았다.

- 소스의 DB 파일명 리터럴 21종, `data/` 최상위 DB 파일 20개. 실제 운영 저장소 수와 동일하다고 단정하지 않는다.
- 기존 정규식 검사로 열 추가 구문 11곳/10모듈. 모든 DDL·트리거·마이그레이션을 센 값은 아니다.
- 우선 이관 접점: `data_preparation`(판/결속/색인), `master`·ECM(기준/조직), `app_data`(업무 입력), `planning`·전사 시나리오(결과), 원장·보고(추적), 외부 수집 저장소.
- 다음 준비: 저장소별 단일 쓰기 경계, 스키마/트리거 목록, 날짜·Decimal·JSON·유일성·조회 순서의 동등성 시험, RAW 파일과 판 지문 보존, 복원 사본 검증.
- PostgreSQL 전환 시 쓰기를 잠시 멈추고 사본 이관→대사→전환→복구 확인. 이번 턴에서 전환/이중 쓰기/상용 배포를 시작하지 않았다.
- 위 첫 입력·데이터 계약이 잡힌 후 PostgreSQL 및 최대 10명 서비스 준비로 진행한다. 업무키트 전 영역을 모두 완성할 때까지 이전을 무기한 미루지 않는다.

## 6. 검증 및 인수인계

- 정본 샘플에 대한 검사기는 실제 결함을 보고하며 FAIL이다. 이는 새 코드 회귀 실패와 다르다.
- 추가 시험은 정상 대조군, 부분 선적 합계, 타 회사 참조, 복합키 중복, 유효기간/단위/BOM 판 중복, 비정상 수치, 통화별 전표, 빈 파일/목록, 자산 비변경을 검증한다.
- 안내 컴포넌트는 실제 React 서버 렌더 15케이스로 확인했다. 이것은 화면 배치·클릭의 브라우저 실측을 대신하지 않는다.
- 로컬 5173/5174/8000 리스너가 확인되지 않았고 브라우저 도구 초기화가 실패했다. 브라우저 실측은 후속 첫 단계로 남긴다. 운영 서버를 임의로 다시 띄워 DB 초기화를 유발하지 않았다.
- 동시 작업: 수집기 KOSIS 변경은 작업 중 `7a782aa3e`로 커밋되었다. 이 작업에서 해당 경로를 편집하지 않았다. 기존 `data/interaction_log.jsonl` 수정도 그대로 유지했다.
- `.agents/TEAM_BOARD.md`와 과거/공식 문서는 수정하지 않았다. 구현 종료 시점에는 커밋·푸시·인증 승인·기존 데이터 교체가 없었다. 이후 사용자 요청에 따른 Git 마감 범위는 아래 §7을 따른다.

### 최종 검증 결과

- 관련 Python 회귀 **100건 통과**: 신규 검사기, 업무영역 카탈로그, 준비도, 패키지 카탈로그, 앱 API. 최종 수집 모수도 100건으로 별도 확인했다.
- React 안내 렌더 **15케이스 통과**, 원천 행 수 입력값 검증 **11케이스 통과**.
- 최종 `npm run build` 통과(타입 검사 포함). 큰 번들/동적 import 경고는 남아 있으나 이번 기능의 빌드 실패는 아니다.
- `git diff --check` 통과. 원천 행 수 보정 이후 프론트엔드를 다시 빌드해 최종 코드로 확인했다.
- 전체 T3와 실제 브라우저 동선은 실행하지 않았다. 이번 검사기를 라이브 인증 관문의 대체물로 사용하지 않는다.

현재 판정: **모수 및 공백 실측 + 첫 입력 안내·등록 동선 보강 완료. 초기 데이터 정본화/추가 등록과 실제 사용 검증은 다음 단계**다.

## 7. 오늘 작업 마감 및 다음 세션 재개 지침

기록: Codex · 2026-09-08 00:52 KST 이후 마감 작업.

사용자 지시: “인수인계 자료 상세히 작성해서 커밋 푸시하고 오늘은 여기서 정리합시다.” 이번 마감은 작업을 보존하는 것이며 미완료 기능이나 데이터 인증을 완료 처리하는 것이 아니다.

### 커밋할 파일 — 이번 작업 10개 경로

| 파일 | 책임과 변경 내용 |
|---|---|
| `core/data_preparation/kit_sample_audit.py` | 샘플 읽기 전용 검사, 참조·누적량·BOM·전표 대사 |
| `scripts/audit_business_kit_initial_data.py` | quick/full 점검 명령, 상태별 종료코드 |
| `tests/test_kit_sample_audit.py` | 독립 정상 대조군·부정 입력·샘플 비변경 회귀 |
| `frontend/src/content/kitGettingStarted.json` | 공통 기반팩+8업무영역의 설명 정본; 특정 패키지/개정에 한정 |
| `frontend/src/components/KitGettingStarted.tsx` | 준비자료·대조·활용·지원범위 안내 표시 |
| `frontend/src/components/DataReadinessBoard.tsx` | 업무영역 안내, 자료 준비 버튼, 준비도 재조회 |
| `frontend/src/components/DataPrepPanel.tsx` | 해당 자료 등록 위치 이동, 이전 적용본 응답 차단, 독립 원천 행 수 입력 |
| `frontend/src/lib/kitInputValidation.ts` | 원천 행 수 형식/범위 검사, 자기 대조용 기본값 없음 |
| `frontend/scripts/check-kit-getting-started.mjs` | React 렌더 15건+원천 합계 입력 11건 |
| 이 인수인계 문서 | 모수·구현·실측 공백·검증·다음 실행 순서·Git 경계 |

### 원격 보존 경계

- 저장소: `https://github.com/denniskwon86-ai/Ai_Agent_factory_porto.git`
- 작업 브랜치: `claude/data-acquisition-orchestrator-20260905`.
- 마감 시작 HEAD: `5d8509bef`. 원격 fetch 후 해당 브랜치는 아직 원격에 없었으며, 원격 어디에도 게시되지 않은 선행 커밋은 20개였다. 오늘 커밋과 함께 같은 이름의 원격 작업 브랜치로 보존한다.
- 선행 20커밋 범위: OpenDART·ECOS·KOSIS 수집 Provider, 요청 해석/매핑/원문 추적, 격리 적재, API·화면, 정기 갱신, 준비도 수집 제안 및 해당 인수인계. 이미 커밋된 이력이며 이번 파일 변경과 혼동하지 않는다.
- `origin/dev` 관측값은 `c1f39237d8ca3340ae676eb97cf0d98c5e9bbd86`이다. `dev`, archive, 다른 통합 브랜치를 갱신하거나 강제 푸시하지 않는다.
- 푸시는 검토한 **이번 커밋의 정확한 SHA**를 대상으로 한다. 동시에 다른 세션이 로컬 브랜치를 전진시켜도 새 변경을 우연히 포함시키지 않는다.
- 실제 커밋 식별자와 원격 반영 성공 여부는 Git 이력 및 최종 사용자 보고로 확인한다. 문서 작성 시점에 아직 수행하지 않은 푸시를 성공으로 기록하지 않는다.

### 커밋에서 제외하고 보존하는 병행 작업

마감 점검 시 `core/external_intelligence/orchestrator.py`, 신규 `providers/datagokr.py`, `tests/fixtures/datagokr/`에 별도 공공데이터 수집기 작업이 있었다. `data/interaction_log.jsonl`도 기존 미커밋 변경이었다. 이 파일들은 **스테이징·되돌리기·삭제하지 않는다**. 이후 추가되는 같은 세션의 파일도 이번 커밋에 자동 포함하지 않는다.

따라서 마감 후 작업 트리가 완전히 clean이라고 보고하면 안 된다. 다음 세션에서 이 파일들이 남아 있는 것은 오늘 작업의 커밋 누락이 아니라 의도적 범위 분리다.

### 검증 재현 명령

```powershell
# 저장소 루트 — 격리 회귀, 운영 데이터 쓰기 없음
venv/Scripts/python.exe -m pytest tests/test_kit_sample_audit.py tests/test_business_kit_catalog.py tests/test_data_readiness.py tests/test_starter_package_catalog.py tests/test_kit_app_api.py -q -x -p no:warnings

# frontend 디렉터리
npm run build
node scripts/check-kit-getting-started.mjs

# 저장소 루트 — 오류가 있는 현재 샘플은 exit 1이 정상적인 진단 결과
venv/Scripts/python.exe -m scripts.audit_business_kit_initial_data --profile quick
venv/Scripts/python.exe -m scripts.audit_business_kit_initial_data --profile full
```

이 작업의 검증 근거는 §6의 100건/26케이스/제품 빌드다. 다른 세션의 `5d8509bef`에 적힌 T3 6,282건은 그 세션의 선행 변경 근거이지 이번 새 10개 경로의 전체 회귀 근거가 아니다.

### 다음 세션의 첫 행동 — 순서를 바꾸지 말 것

1. 브랜치/원격 SHA와 병행 미커밋 파일을 확인한다. 자동 pull/merge 또는 남의 변경을 정리하는 명령부터 실행하지 않는다.
2. 현재 통합 데이터 설정의 제품 서버를 확인하고 업무 데이터 준비 화면을 검증한다. 별도 데모 전용 정본이나 가짜 준비도 응답을 만들지 않는다.
3. 구매·생산·판매 영역의 `자료 준비` 클릭, 등록 대상 이름, 원천 합계 미입력/잘못된 입력, 정상/실패 등록 후 준비도 갱신, 적용본 전환 응답 순서를 브라우저에서 확인한다.
4. K2를 시작한다. 먼저 quick 조직 프로필의 누락 범위와 full BOM의 과거 기간을 대조하고, 수정 후보의 변경 전후 보고서를 만든다. 과거 유효일을 단순 소급하거나 정합성 검사 기준을 낮추지 않는다.
5. 그 뒤 구매·물류·재고·생산·판매·전표를 하나의 대사 가능한 초기 묶음으로 보강한다. 공개자료 수집기 담당과 인증 경로를 맞춘 뒤 공시 기반 총계와 합성 상세를 연결한다.
6. 첫 입력·업무 데이터 계약이 안정되면 PostgreSQL 및 최대 10명 서비스 준비로 넘어간다. 모든 업무영역을 완성할 때까지 DB 전환을 무기한 미루지는 않는다.

완료로 착각하면 안 되는 잔여: 한글 입력 양식·열 매핑·미리보기, 라이브 인증 경로의 업무 간 대사 적용, 실제 공개자료 인증, 초기 데이터 정본화·등록, 실제 브라우저 사용 검증, PostgreSQL 전환·배포.

## 8. 재개 — 조직·BOM 후보 분리와 원본 보존

작성: Codex · 2026-09-08 · 사유: 사용자 “이어서 진행해주세요”.

### 현재 기준과 작업 경계

- 재개 HEAD: `85a153473`, 작업 브랜치 유지. 사용자가 승인한 이전 푸시는 `744c62373`까지만 완료됐으며, 후속 2커밋을 이번에 자동 푸시하지 않았다.
- 권고 착수 순서 4~6 / G2-B·G2-D에 해당한다. 현업 자료 준비와 같은 회사·기간·단위의 계산 입력을 전진시키는 작업이다. 관문 전체 완료나 데이터 등록 완료가 아니다.
- 기존 `run.py`와 설치 설정을 사용해 제품 서버 8080 및 프론트 5173을 기동했다. 두 HTTP 생존 확인은 성공했다. 데이터 뿌리를 바꾸거나 별도 시연 DB를 만들지 않았다.
- 기동 과정의 기존 설치 조직 upsert·업무표준 확인·체크포인터 초기화는 제품 시작 절차다. “서버 시작까지 DB 무변경”이라고 주장하지 않는다. 이번 후보 생성기 자체는 DB를 열지 않는다.
- 브라우저 도구는 초기화 2회 실패했다. 이후 인증 화면 검사 스크립트 작성이 자동 안전검토에서 거절돼 실행하지 않았다. 과거 전달된 로그인 제한 문구를 이유로 한 차단이며 우회하지 않았다. 실제 로그인·화면 클릭·파일 등록·인증 검사는 미수행이다.
- 컴퓨터 사용 스킬을 확인했으나 도구 연결이 실패해 화면 확인에는 사용하지 못했다. React 렌더 결과를 브라우저 증적으로 바꾸지 않는다.

### 새 코드와 보정 명세

| 파일 | 책임 |
|---|---|
| `core/data_preparation/kit_sample_audit.py` | 동일 바이트 읽기 함수를 공용화; 비정상 Decimal 연산도 진단으로 분류 |
| `core/data_preparation/kit_sample_revision.py` | 합성 샘플에 한정해 조직 상위 계층 보완 및 파생 소요량 보정 후보 생성 |
| `scripts/plan_business_kit_initial_revision.py` | 검토 명세 출력; --report는 기존 파일 덮어쓰기 금지 |
| `tests/test_kit_sample_revision.py` | 독립 기대값, 조직/기간/단위/대체 BOM, 제품 산식 대사, 실제 자산 비변경 회귀 |

원본 지문·계약 지문·변환 규칙 판·제품 모델 판·변경 전후 행·참조 BOM 행 지문을 남긴다. 수정 후보 행의 품질·인증 표시는 `PENDING_VALIDATION / UNVERIFIED_CANDIDATE`다. 원본의 인증 문구를 복사해 승인된 후보로 만들지 않는다.

샘플의 조직 참조는 동일 패키지 full의 FND-01이다. 이것은 **샘플 내부 참조 보완**이지 LS MnM 설치 조직 및 소유권 매핑 확정이 아니다. 후보 상태는 항상 `REVIEW_ONLY`이며, 검사가 통과해도 준비도·실행 권한을 올리지 않는다.

### 실측과 이전 기록 정정

| 범위 | 제안한 변경 | 보정 후보의 남은 문제 |
|---|---|---|
| quick 13,585행 | FND-01 3행 추가(누락 제련공장과 상위 계층), MFG-01 333행 소요량 변경 | 동시 유효 BOM 충돌 167행 |
| full 155,508행 | MFG-01 5,157행 소요량 변경 | 2024-01-01 이전 계획의 유효 BOM 부재 843행 |

1. quick의 “생산 소요량 실패 500행”은 현재 실제 검사에서는 **소요량 불일치 333 + BOM 충돌 167**로 나뉜다. 회귀에 옛 500 수치를 그대로 넣었다가 실패했으며, 원본 파일과 BOM별 행 수로 다시 대조했다. 검사 기준을 완화한 것이 아니라 기존 보고의 분류를 정정한 것이다.
2. quick `FG-CATHODE`·`FG-NISO4`는 각각 `-01`·`-02` 두 BOM이 같은 기간에 유효하다. 두 판을 합쳐 소요량을 계산하거나 임의로 하나를 선택하지 않았다.
3. 유효 BOM이 하나인 `MPS-0000002`는 저장값 2.057 → 후보 2.188이다. Decimal로 자재별 합산/수율/최종 반올림 후, 제품의 실제 투영 및 `material_shortage` 대사에 후보값을 넣어 일치를 확인했다. 진단용 가용재고 0에서 나온 생산량·부족량은 발행하지 않았다.
4. full의 보류 843행은 모두 BOM 시작 전이다. 유효기간을 소급하거나 계획일을 이동하지 않았고, 해당 원본 행을 삭제하지 않았다.
5. 후보를 메모리에 반영한 검사에서 quick 3,715 → 167건, full 6,000 → 843건의 문제로 줄었다. **두 후보 모두 FAIL이며 자동 등록 대상이 아니다.**

로컬 상세 증적(미추적 생성물, DB가 아님):

- `tmp/kit-k1-20260908/quick-candidate.json` — 지문 `8dc383392143b02268552169e81f36c44d79ff50ea4b942c6da222fc742c5b97`
- `tmp/kit-k1-20260908/full-candidate.json` — 지문 `1d69e85bb67d2fa888bef4e9211d799c9d366cec0595853ece56ff26b7ee9a73`

재생성(출력 파일은 새 이름으로 지정):

```powershell
venv/Scripts/python.exe -m scripts.plan_business_kit_initial_revision --profile quick --report tmp/quick-candidate-next.json
venv/Scripts/python.exe -m scripts.plan_business_kit_initial_revision --profile full --report tmp/full-candidate-next.json
```

현재 자료는 두 명령 모두 exit 1이 정상 진단이다. 원천 판독 실패/출력 덮어쓰기 시도는 exit 2다. exit 0도 검사 범위 내 후보 통과일 뿐 인증이 아니다.

### 최종 검증과 남은 작업

- Python 관련 회귀 **127건 통과**, 수집 모수 127건 별도 확인. 신규 27건 + 선행 관련 100건이다.
- React 안내 **15케이스**, 독립 원천 행 수 **11케이스** 통과. 프론트 소스 변경 없음; 전체 제품 빌드는 이번에 다시 하지 않았다.
- 실제 starter kit 전체 파일의 전후 SHA 대조 회귀 통과. 샘플 CSV·manifest·Excel 변경 없음. `git diff --check` 통과.
- 전체 T3, 실제 화면 점검, 파일 등록·인증, 시나리오 계산 실행은 미수행이다. 시연 데이터 준비 완료라고 보고하지 않는다.
- 병행 World Bank 수집기 작업(`providers/worldbank.py`, mapping/raw_store/관련 라우트 등)과 기존 로그 변경은 편집·스테이징·복구하지 않았다. 커밋·푸시 없음.

다음 순서:

1. 인증된 실제 화면 검증을 위한 사용자 재확인을 받고 K1 클릭 동선을 확인한다. 파일 등록은 후보를 실제 적용하기로 정한 다음 수행한다.
2. 생성기와 full 참조를 대조해 quick 중복 BOM이 대체판인지 중복 생성인지 명시적으로 정리한다. 같은 재료라는 이유로 합산하지 않는다.
3. 과거 BOM 근거 없는 843행은 보류를 유지하며, 사용할 검증 기간과 과거 자료 보강을 분리한다.
4. 구매·물류·재고·생산·판매·전표의 업무 사슬 대사와 설치 조직/소유권 연결을 확인한 뒤 후보 개정으로 등록한다. 기존 인증판을 조용히 교체하지 않는다.
5. 첫 입력·데이터 계약이 안정되면 PostgreSQL 및 최대 10명 서비스 준비로 이어간다.

## 9. 경영 홈 해상도 보정 후 재개 — 간편 샘플 BOM 충돌 해소

작성: Codex · 2026-09-08 · 사용자 지시: “자 다음 작업 이어서 진행해주세요”. 재개 HEAD `d56dfcda2`. 앞선 경영 홈 반응형 변경은 별도 `HOME_RESPONSIVE_LAYOUT_2026-09-08.md`에 남겼다. 이번에는 로드맵 권고 4~6 / G2-B·G2-D의 초기 자료 정합성 보강을 진행했다. K1 실제 계정 화면 동선은 여전히 미검증이며, 그 상태를 통과로 바꾸지 않고 독립적인 K2 후보 보정을 진행했다.

### 원인과 변경

`generate_sample_company_starter_kit.py:generate_bom()`은 quick에 완제품이 6종인데 목표 8번을 순회했다. 그 결과 전기동·황산니켈의 같은 배합·같은 유효기간이 각각 `-01`, `-02`로 중복 생성됐다. 전체 샘플에는 동일 업무키의 `-01`이 유일하게 있고, quick의 `-02`는 행/계보/BOM 식별자 외 모든 값이 같다. quick CSV에서 복제 BOM을 참조하는 파일은 MDM-05 자신뿐임도 확인했다.

- 생성기는 목표 개수를 상한으로 사용하도록 수정했다. 함수 단위로만 시험했으며 `build()`/`build(clean=True)`와 전체 재생성은 실행하지 않았다. **기존 생성기의 소요량 산식·기간·타 산출물 삭제 문제까지 해결했다는 뜻은 아니다.**
- 후보 생성기에 명시 옵션 `--resolve-exact-bom-copies`를 추가했다. 기존 기본 명령은 종전처럼 충돌을 보류한다.
- full의 같은 회사·범위·제품에 유일한 BOM이 있고, 후보의 모든 판이 그 내용과 정확히 같을 때만 복제 행을 후보에서 제외한다. 수량·수율·역할·단위·기간뿐 아니라 미지의 추가 열도 비교한다. 실제 대체판 선택/합산 기능이 아니다.
- 복제 ID를 다른 자료가 참조하거나 full 참조가 없거나 여러 판이면 보류한다. 원본 CSV는 삭제하지 않고 보고서의 변경 전 행, 제외 행 번호, 유지 BOM ID, 참조 행 지문으로 추적한다.
- BOM 후보 정리 후 기존 제품 투영·소요량 계산 함수로 생산계획을 다시 대사한다. 실제/공개자료는 이 합성 후보 자동 보정 대상이 아니다.

### 실측

| 프로필 | 후보 변경 | 후보 검사 결과 |
|---|---|---|
| quick | 조직 3행 추가, 동일 BOM 복제 5행 제외, 생산계획 500행 소요량 보정 | 기존 진단 3,715건 → 검사 범위 내 0건, 보류 0건 |
| full | 생산계획 5,157행 소요량 보정 | 과거 유효 BOM 없는 843행 보류 유지, FAIL |

전기동 첫 계획의 독립 대조: 생산 10톤 × 투입계수 3.3 ÷ 수율 0.98 = **33.673톤**(최종 3자리 반올림). 원본 66.4는 복제판을 합치고 RETURN까지 더하며 수율을 반영하지 않은 생성 산식의 값이다. 이전 67.347을 정답으로 되살리지 않았다.

quick의 `PASS_CHECKED_SCOPE`는 현재 검사기가 보는 파일·키·참조·조직·BOM·소요량·전표 대사 범위의 후보 통과다. **전체 업무 사슬 대사·등록·인증·계산 실행·화면 준비 완료가 아니다.** 상태는 `REVIEW_ONLY`이며 후보 생성기는 DB를 열지 않는다. 아직 화면 데이터는 바뀌지 않는다.

검토 명세:

- `tmp/kit-k2-bom-20260908/quick-candidate.json` — 지문 `a14abe8faf48271308ec641ec1a80afb6407969946ecc5bcbb56c87f07be2136`, exit 0.
- `tmp/kit-k2-bom-20260908/full-candidate.json` — 지문 `5c4f540528d64ca46bc01451f5d7d58f19f7beb8229a2a2f32baca299e2a4062`, exit 1(843행 보류).

### 검증과 보존

- 후보 회귀 45건(신규 18건 포함) 통과. 관련 검사기·업무영역 카탈로그·패키지·앱 계약/빌더/API를 합친 **137건 통과**, 종료 0; 수집 모수 별도 확인.
- 정확히 같은 복제 대조군, 역할/단위/유효기간/수율/새 업무열 차이, 타 회사·타 범위·중복 full 참조, 복제 ID의 다른 자료 참조, 결정론, 실제 quick/full의 결과를 확인했다.
- starter kit 전체 파일의 SHA 전후 대조 회귀 통과. CSV·manifest·Excel 변경 없음. 전체 T3는 실행하지 않았다.
- 선행 미커밋 후보 코드에 이어 보강했으며 경영 홈 UI, 수집기, 상호작용 로그는 편집하지 않았다. 이번 작업에서 Git 스테이징·커밋·fetch·pull·push 없음.

### 다음 실행 — 수량이 업무 사이에서 보존되는가

1. INV-02의 이동 유형별 부호와 창고를 기준으로 OPENING + 입고 − 출고 ± 조정 = INV-01 말일 재고를 대사한다. `safety_stock_quantity`는 정책량이므로 재고 합에 더하지 않는다. 등록된 창고가 없는 이동·타 조직/단위 혼입은 별도로 표시한다.
2. MFG-02의 `plan_line_id`/`batch_id`와 재고 이동의 `reference_type=BATCH`, `reference_id`를 대조한다. 여러 투입 원료가 필요한 BOM을 첫 원료 하나로 축약한 생성 규칙은 별도 검증 대상으로 남긴다.
3. SLS-01의 `sales_line_id`와 재고 이동의 `reference_type=SALES_ORDER`를 연결한다. 제품명이 같다는 이유로 생산계획/실적을 수주에 임의 배분하지 않는다.
4. 이 검사가 추가로 발견하는 문제까지 변경 명세에 포함하고 설치 조직·소유권 연결을 확인한 후, 기존 제품 등록·검사 경로에 넣을 신규 후보 묶음을 준비한다. 과거 843행은 근거 보강 전까지 그대로 보류한다.

모수는 여전히 **공통 기반팩 + 8개 업무영역, 데이터 계약 35종**이다. 이번에는 그중 조직·BOM·생산 소요량의 한 구간을 전진시켰다. 업무키트 전체가 완료된 것은 아니다.

## 10. K2 수량 사슬 검사 — 단위·창고·기말재고 결함 가시화

작성: Codex · 2026-09-08 · 사용자 “네 그럼 다음 작업 계속 이어서 진행해줘요”. 재개 HEAD `b2e1b5f17`, 브랜치 `claude/data-acquisition-orchestrator-20260905`. 기존 우선순위인 초기 자료 정합성 보강을 이어갔다. 외부 수집기 UI와 일회/주기/변경·정정 수집 정책은 별도 `EXTERNAL_ACQUISITION_UI_REFRESH_PLAN_2026-09-08.md`의 계획이며, 이번에 구현 완료로 바꾸지 않았다.

### 구현 범위

- `core/data_preparation/kit_quantity_audit.py`: 저장소를 열지 않는 순수 파일 행 검사. 이동 유형 6종을 명시하고 회사·조직·품목·창고·단위를 분리한다. 정의되지 않은 이동, 부호 반전, 참조/시점 불일치, 중복 키, 미등록/비활성/타 조직 창고, 비유한 수량을 실패로 보고한다.
- 생산 입출고는 `BATCH → batch_id → plan_line_id`, 판매 출고는 `SALES_ORDER → sales_line_id`, 구매 입고는 `SHIPMENT → LOG-02 → PRC-02` 및 `LOG-05 DELIVERED`로 대조한다. 분할 출고는 합산하며 구매 입고의 날짜·목적 창고도 대조한다. 구매부서의 소유 범위를 입고창고의 보관 범위로 강제하지 않는다.
- 재고는 주어진 이동 원장 안에서 `OPENING + 입고 - 출고 ± 조정`을 스냅숏 시점까지 합산한다. 불출/출고는 음수라는 샘플 계약을 사용한다. 월말 물리 재고는 `unrestricted + quality + blocked`; `safety_stock` 정책량은 더하지 않는다. 미래 이동을 당겨 읽지 않는다.
- 창고·품목 전체 재고인 `lot_id=ALL`만 지원한다. 상세 로트 재고를 전체와 혼합하지 않는다. 공급된 회사/조직별 스냅숏 달력에서 잔고 있는 항목이 사라지는 경우도 실패다. **달력 자체에서 통째로 빠진 기간과 전기 인증 잔액까지 증명하지 않는다.**
- 무효 입출고가 있으면 그 행을 버린 부분합으로 재고 일치를 선언하지 않는다. 이번 버전은 보수적으로 기말재고 수량 비교 전체를 보류한다. 스냅숏의 단위/음수 문제는 그와 별개로 보고한다.
- 기존 `audit_package()`와 `plan_package_revision()` 양쪽에 실제 연결했다. 조직/BOM 후보의 `before/after` 진단과 새 `quantity_flow`를 구분하되, **최종 candidate_check는 어느 검사든 실패하면 FAIL**이다. 후보 규칙 판은 `organization-bom-candidate/2`다.
- CLI 상세 JSON에는 모든 진단을 보존하며 콘솔에는 모수·종류별 개수만 표시한다. 기존 보고서 덮어쓰기 금지는 유지한다.

### 원본/후보 실측

수량 검사는 메모리의 조직/BOM 후보에도 실행했다. 해당 후보 변경은 창고·단위·입출고를 수정하지 않으므로, 수량 진단은 원본과 같다. 아래는 **진단 수**이며, 한 행의 단위와 음수가 함께 걸릴 수 있어 불량 행 수와 같지 않다.

| 새 수량 진단 | quick | full |
|---|---:|---:|
| 단위 불일치 | 1,098 | 28,076 |
| 미등록 창고 참조 | 159 | 0 |
| 창고와 행의 조직 불일치 | 8 | 0 |
| 음수 물리 재고 | 16 | 1,082 |
| 합계 | **1,281** | **29,158** |

단위 진단 세부: quick은 INV-01 126 / INV-02 864 / MFG-02 108, full은 INV-01 17,892 / INV-02 6,718 / MFG-02 3,466이다.

검사 모수 및 결과:

| 항목 | quick | full |
|---|---:|---:|
| 재고 이동 행 | 5,000 | 30,000 |
| 재고 스냅숏 행 | 540 | 55,440 |
| 생산 배치 | 650 | 8,000 |
| 판매 행 | 300 | 2,500 |
| 입력 검사 통과 이동 | 3,969 | 23,282 |
| 대사 일치 생산 입고/불출·판매 출고 합계 항목 | 1,225 | 11,568 |
| 날짜·창고를 포함해 일치한 구매 입고 합계 항목 | 120 | 1,200 |

생산 배치는 불출/입고 각각 1항목이다. 일치 항목만 전체 모수처럼 보고하지 않는다. quick 기말재고 540행 중 단위 오류 126행은 입력 거부, 나머지 414행은 무효 이동으로 수량 대사 보류다. full은 55,440행 중 단위 오류 17,892행 입력 거부, 37,548행 수량 대사 보류다. **기말재고 대사 완료가 아니다.**

원인을 생성 코드와 독립 대조했다:

1. quick MDM-01은 TON 23품목 / EA 6품목 / KG 1품목인데 스냅숏 540행은 전부 TON이다. `generate_movements_and_snapshots()`가 실사 조정과 스냅숏의 단위를 TON으로 고정한다. MFG-02도 입출고에 단일 TON 열을 사용하므로 EA 품목을 첫 투입재로 고르면 물리적 의미가 어긋난다. **EA→TON을 환산하거나 단위명만 덮어쓰지 않았다.**
2. quick의 창고 3종에는 `LOC-P1-FG`가 없는데 생산/판매가 그곳을 사용한다. full MDM-04에는 같은 회사·조직의 해당 창고가 있다. 추후 동일 패키지 참조를 근거로 후보 보완할 수 있다.
3. 생성기의 기초재고는 예정 창고가 없으면 활성 창고 목록에서 임의로 고르되 원래 품목 조직을 유지한다. 이 fallback이 타 조직 창고 8건을 만든다.
4. 기초재고를 충분히 제공한다는 생성기 주석과 달리 실제 월말 음수가 있다. 음수를 0으로 올리거나 이유 없는 조정 입고로 맞추지 않았다.

이전 §9의 quick PASS는 당시 조직/BOM 등 제한된 범위에 대한 기록이다. 그 성과(500계획 소요량 대사, 중복 BOM 정리)는 유지하지만, 이번에 넓힌 범위로는 **quick과 full 모두 FAIL**이다. full의 과거 유효 BOM 부재 843행도 별도 보류 그대로다.

### 증적과 검증

최종 후보 상세(로컬 생성물, DB 아님; 앞선 중간 보고서도 보존):

- `tmp/kit-k2-quantity-20260908/quick-candidate-final.json` — 지문 `286e952ce38934eae89c7c90b874ee6dd9208fe0afa07db80f7cc42420f5653a`.
- `tmp/kit-k2-quantity-20260908/full-candidate-final.json` — 지문 `6ea68738e20f7983f343fcb431f5711baaeaa511bebaad35c22e24970afb4a5a`.
- 두 CLI 모두 진단 FAIL에 따른 exit 1. 실행 오류/판독 불가가 아닌 데이터 진단 결과다.

검증: 수량 검사·기존 검사기·후보 생성·업무영역 카탈로그·패키지 카탈로그·앱 계약/빌더/API **193건 통과, 94.81초, exit 0**. 실제 quick/full 후보와 starter kit 전체 파일 SHA 보존 회귀 포함. 명령:

```powershell
venv/Scripts/python.exe -m pytest -x -p no:warnings tests/test_kit_quantity_audit.py tests/test_kit_sample_audit.py tests/test_kit_sample_revision.py tests/test_business_kit_catalog.py tests/test_starter_package_catalog.py tests/test_kit_app_contract.py tests/test_kit_app_builder.py tests/test_kit_app_api.py
```

치명 분기 변이 **6/6 포착**: 단위 검사 제거, 안전재고 합산, 원천 수량 대신 자기 값 대조, 스냅숏 값 대조 무력화, 회사 없는 참조 조회, 후보 판정에서 수량 실패 제외. 각 변이가 소스 앵커 1곳에 실제 적용됐음을 확인한 뒤 해당 행동 회귀의 실패(exit 1)를 확인했다. 별도 Python 프로세스의 **메모리 안에서만** 변이해 작업 파일은 수정하지 않았다. 정상 코드의 영향 회귀는 변이 이후 실행했다.

전체 T3는 하루 예산과 기존 결과 귀속 HOLD를 유지해 재실행하지 않았다. 이번 193건을 통합 T3 통과라고 보고하지 않는다. 샘플 CSV·manifest·Excel·기존 인증판·운영 DB·화면은 변경하지 않았다. 실행/등록/인증 권한도 올리지 않았다. 기존 `data/interaction_log.jsonl`과 외부 수집기 UI 계획은 보존, 스테이징·커밋·푸시 없음.

### 다음 작업 순서

1. **창고·보관 조직 후보 보완**: full에 실제 있는 같은 회사·조직의 창고만 quick 후보에 연결한다. 임의 창고 fallback은 제거하고 출처와 변경 전후 행을 남긴다. 설치 조직/소유권 자동 변경은 하지 않는다.
2. **단위·투입 모델 보정**: 품목 기본단위를 따라 조정/스냅숏을 생성하되 과거 수치에 단위명만 바꾸지 않는다. TON 산출·EA 투입 등 다중 단위 배치에는 투입/산출 단위 및 변환 근거 계약이 필요하다. 단위가 확정되지 않은 항목은 보류하고 질량 계산에 합산하지 않는다.
3. **수량 시나리오 재구성**: 유효 BOM의 실제 투입재 구성, 기초잔액, 구매 도착, 생산 투입/산출, 판매 납기를 시간순으로 맞춘 신규 후보를 만든다. 부족 사건은 표현하되 실제 출고/기말 물량이 물리적으로 음수가 되도록 장부를 만들지 않는다. 승인된 배치↔판매 연결 없이 제품명으로 역산 배분하지 않는다.
4. 위 후보를 동일 검사로 다시 대사하고, 기존 인증판 보존/새 판 등록 절차와 한글 입력 미리보기로 연결한다. 이후 실제 브라우저 입력·실행을 확인한다. 후보 파일의 초록을 화면 완료로 대신하지 않는다.
5. 외부 수집기 UI는 별도 계획의 요청→출처 추천→수집 미리보기→회사/업무키트 연결→갱신 이력 순서로 이어간다. 일회/주기/변경 감지와 과거자료 정정 검사를 구분하며 자동 수집을 자동 인증/적용으로 취급하지 않는다.

## 11. G2 창고·보관 조직 보정 후보와 생성기 재발 방지

작성: Codex · 2026-09-08 · 사용자 “네 다음 진행해주세요”. 재개 HEAD `b2e1b5f17`, 기존 브랜치 유지. 로드맵 권고 4~6 / G2-B·G2-D의 데이터 매핑·대사 구간을 전진시킨 작업이다. 현업 입력에서 전사 시뮬레이션으로 이어지는 자료 준비에 직접 필요하지만, 등록·인증·브라우저 실행 관문 완료는 아니다.

### 결정과 구현

1. 기존 후보 생성기에 명시 옵션 `--repair-warehouses`를 추가했다. 기본 호출의 보정 범위를 몰래 넓히지 않았다. `propose_revision()`은 입력을 복사한 뒤 창고 후보를 적용하고 기존 수량 검사까지 다시 실행한다. 후보 규칙 판은 `organization-bom-candidate/3`, 창고 규칙은 `same-package-warehouse-and-opening-fallback/1`이다.
2. 누락 창고는 같은 패키지 full의 같은 회사·창고 식별자가 유일하고 활성일 때만 추가한다. 사업장/조직이 참조 행과 일치하고 조직 기준정보에 존재해야 한다. 기존 창고 행은 덮어쓰지 않는다.
3. 기초재고 이동은 같은 회사·이동 식별자의 full 대응 행이 유일하고, 알려진 네 열(`record_id`, `lineage_id`, `movement_date`, `to_location_id`) 외 모든 열이 같을 때만 후보를 만든다. 알 수 없는 추가 업무 열도 대조한다. 미래 참조, 시작일 아닌 OPENING, 수량·단위·품목·조직 불일치는 보류한다.
4. **full과 창고가 다르다는 이유만으로 변경하지 않는다.** 원래 목적지가 quick에서 누락됐고, 기존 목적지가 옛 생성기의 `활성 창고[품목 순번 % 창고 수]`와 일치하는지도 확인한다. 현재 유효한 다른 창고 선택이나 일반 거래의 조직/목적지를 추정해 수정하지 않는다.
5. 수정 행의 수량·단위·조직·기준일은 보존하고, 창고 연결과 후보 품질 상태만 변경한다. full의 2023-08-01 기초 수량을 quick의 2026-02-01 이월 실적으로 가져오는 기능이 아니다. 합성 생성 규칙의 연결 오류를 고치는 검토 후보다. 실제 자료와 공개자료에는 이 보정 규칙을 적용하지 않는다.
6. 각 변경에 이전/이후 행, 참조 행 지문, 참조 파일 지문, 기존 fallback 목록/순번, 보존한 날짜를 남긴다. 추가·변경 행은 `PENDING_VALIDATION / UNVERIFIED_CANDIDATE`다.
7. 창고 연결을 바꾸면 월말 재고도 재산출해야 한다. 기존 INV-01을 조용히 옮기거나 값을 맞추지 않고 `warehouse_rebuild_required`에 남긴다. 다른 진단이 모두 초록이어도 이 재산출 요구가 있으면 최종 후보는 FAIL이다.

수정 파일: `core/data_preparation/kit_sample_revision.py`, `scripts/plan_business_kit_initial_revision.py`, `scripts/generate_sample_company_starter_kit.py`. 신규 회귀: `tests/test_kit_warehouse_revision.py`.

### 기존 보고의 모수 보완과 실제 결과

종전 검사에서 발견한 조직 불일치는 8건이다. full 대응 기초재고와 생성기 fallback을 대조하니 **오연결은 총 11건**이었다. 8건은 다른 공장, 나머지 3건은 같은 공장의 원료창고에 들어가 있어서 조직 검사만으로는 발견되지 않았다. 세 건도 같은 생성 오류라는 근거가 확인돼 후보에 포함했다.

| 구분 | 전 | 이번 후보 |
|---|---:|---:|
| quick 창고 종류 | 3 | 4 (full에 있는 제련 완제품 창고 1종 추가) |
| 기초재고 연결 변경 | 0 | 11행 |
| 미등록 창고 참조 진단 | 159 | 0 |
| 창고/조직 불일치 진단 | 8 | 0 |
| 단위 진단 | 1,098 | 1,098 (수량·단위 보정은 다음 단계) |
| 음수 재고 진단 | 16 | 16 (원본 스냅숏은 유지) |
| quick 수량 진단 합계 | 1,281 | **1,114** |

이전 조직/BOM 후보 3+5+500행 변경에 **MDM-04 1행 추가, INV-02 11행 변경**이 더해졌다. 창고 보정 자체의 보류는 0건이나, 파생 재고 재산출 요구는 12건(기초재고 이동 11건 + 추가 창고 범위 1건)이다. 수량 진단은 행 수가 아니며 동일 행이 여러 진단에 포함될 수 있다.

quick의 유효 입출고는 3,969 → 4,136행, 일치한 생산 불출/입고·판매 출고 합계 항목은 1,225 → 1,384개다. 구매 입고 120개 합계 항목은 계속 일치한다. 540개 재고 스냅숏은 단위 오류 126개 입력 거부 및 무효 이동에 따른 414개 대사 보류 상태이며, 아직 기말재고 대사 완료가 아니다.

full에는 창고/기초재고 연결 변경을 만들지 않았다. 기존 MFG-01 소요량 후보 5,157행만 있고, 과거 유효 BOM 부재 843행 및 수량 진단 29,158건은 유지했다. 두 프로필 모두 `REVIEW_ONLY / FAIL`이며 자동 등록 대상이 아니다.

### 재발 방지

`generate_locations(quick)`는 이제 생산·판매에서 쓰는 `LOC-P1-FG`를 포함한 4창고를 생성한다. 기초재고 생성은 예정 창고가 없거나 비활성·다른 회사·다른 조직·중복이면 `ValueError`로 중단한다. 임의 창고 fallback은 삭제했다.

회귀에서는 순수 생성 함수만 메모리 안에서 호출했다. **`build()`와 전체 재생성은 실행하지 않았다.** 기존 CSV, manifest, Excel 산출물은 변경하지 않았다. 생성기의 다른 문제(단위 고정, 실제 투입 모델, 전체 build의 산출물 삭제/상태 변경)는 별도로 남아 있다.

### 검증·증적·보존

- 창고 신규 회귀 **36건**: 실제 quick/full 후보, 회사/범위/활성/중복/시점/수량/추가 열 차이, legacy fallback 확인, 합성/공개 출처 구분, 정상 거래 비변경, 날짜·조직·수량 보존, 반복 실행 동일, CLI 배선, 전체 starter 파일 SHA 보존.
- 핵심 변이 **5/5 포착**: full 업무내용 대조 제거, 창고 범위 검사 제거, fallback 증명 제거, 재산출 관문 제거, 생성기 fallback 복원. 별도 프로세스 메모리에서 앵커 1곳 적용을 확인한 후 해당 회귀 실패(exit 1)를 확인했다. 작업 파일을 변이하지 않았다.
- 변이 이후 정상 코드의 관련 회귀 **229 passed / 90.84초 / exit 0**. 229는 기존 193 + 신규 36이며 T1과 중복 합산하지 않았다. `git diff --check` 통과.
- 전체 T3는 하루 예산 및 기존 통합 결과 귀속 HOLD를 유지해 재실행하지 않았다. 이 결과를 T3 통과나 최종 통합 완료로 표현하지 않는다.
- 운영 DB·화면 데이터·인증 상태·권한 변경 없음. 기존 상호작용 로그와 다른 작업의 변경을 보존했다. Git 스테이징·커밋·fetch·pull·push 없음.

보고서(새 파일, 기존 보고서는 보존):

- `tmp/kit-k2-warehouse-20260908/quick-candidate.json` — 지문 `9b720ac9594e04306969adc54f873a02c7534ef96c13064ac2c60991b34cb8eb`.
- `tmp/kit-k2-warehouse-20260908/full-candidate.json` — 지문 `670f9ed875371c20458e9ee818648eef0bf4977efdb10807077873b51ee5ebcc`.
- 두 CLI exit 1은 아직 단위/재고 결함이 있는 후보의 정상 진단 결과다.

```powershell
venv/Scripts/python.exe -m scripts.plan_business_kit_initial_revision --profile quick --resolve-exact-bom-copies --repair-warehouses --report tmp/warehouse-candidate-next.json
venv/Scripts/python.exe -m pytest -x -p no:warnings tests/test_kit_warehouse_revision.py tests/test_kit_quantity_audit.py tests/test_kit_sample_audit.py tests/test_kit_sample_revision.py tests/test_business_kit_catalog.py tests/test_starter_package_catalog.py tests/test_kit_app_contract.py tests/test_kit_app_builder.py tests/test_kit_app_api.py
```

### 다음 — 단위 정리와 재고 재산출

1. 품목별 TON/KG/EA와 입출고·BOM·생산 실적의 단위 계약을 맞춘다. 기존 수치에 단위 이름만 바꾸거나 EA→TON을 임의 환산하지 않는다. 합성 상세 수량을 재생성해야 하는 항목과 근거가 있는 변환을 구분한다.
2. 질량 기반 생산 모델에 EA 소모품을 투입재로 넣는 현재 생성 규칙과, 배치에 입출고 단위 하나만 있는 계약을 함께 검토한다. 실제 다중 원료 투입·수율을 반영할 수 있어야 한다.
3. 위 단위/투입 모델을 확정한 후보 이동 원장으로 이번 `warehouse_rebuild_required`의 월말 재고를 새로 산출한다. 기초·구매 입고·생산 불출/입고·판매 출고가 기간별로 보존되는지 확인한다. 음수 재고를 0으로 자르거나 근거 없는 조정 입고로 숨기지 않는다.
4. 새 후보의 대사가 끝나면 기존 제품의 입력 미리보기·새 판 등록 경로와 실제 브라우저 실행을 진행한다. 이번 창고 후보를 그대로 등록하지 않는다. 외부 수집기 UI/갱신 정책은 기존 별도 계획을 유지한다.

## 12. 단위 환산·재고 재산출 후보와 부족 원인 추적 — 2026-09-08

작성: Codex · 사용자 지시: 다음 단계 계속 · 관문: G2-B/G2-D의 수량 정합성 준비. 공통 기반 + 8개 업무영역/35개 계약이라는 전체 모수는 바뀌지 않았다. **이번에는 화면·DB 등록이 아니라, 원본을 보존하는 검토 후보 생성 경로를 전진시켰다. 단위·생산 모델 전체 보정 완료가 아니다.**

### 구현 및 보존 범위

- 신규 `core/data_preparation/kit_stock_revision.py`: `rebuild_stock_candidate()`가 입력 사본에서 수량 환산, 재고 재계산, 최초 부족 날짜와 관련 이동을 산출한다.
- 기존 `plan_package_revision()`과 CLI에 `--rebuild-stock`을 연결했다. 옵션을 주지 않으면 이전 조직/BOM/창고 후보 동작을 유지한다. 종합 규칙 판은 `organization-bom-candidate/4`다.
- 변경/추가 행만 `PENDING_VALIDATION / UNVERIFIED_CANDIDATE`로 남긴다. 새 인증, 승인 원장, 실제 등록, 운영 DB 쓰기 기능은 추가하지 않았다.
- 원본 35개 CSV·manifest·계약·Excel 산출물 보존을 파일 SHA 회귀로 확인했다. 생성기의 `build()`와 전체 재생성을 실행하지 않았다. 생성기 수정도 이번 단계에서는 하지 않았다.
- 외부 수집기 UI 및 주기 갱신 정책은 기존 별도 계획을 유지한다. 이번 데이터 후보 구현이 해당 UI의 완료를 뜻하지 않는다.

### 단위·수량 처리 계약

1. **독립적인 재고조정 이동만** 같은 회사 FND-03의 시점상 유효한 질량 환산 근거로 바꾼다. TON/KG/G만 대상이며 통화·부피·EA를 섞지 않는다. 수량과 단위를 함께 바꾼다.
2. 후보 이동의 발생일을 기준으로 최신 유효 참조 1건을 선택한다. 참조 없음, 미래 참조, 같은 시점 중복, 다른 회사, 0/NaN 계수, 표준 질량 비율과 충돌, 저장 정밀도 손실은 보류한다. 역방향 직접 참조는 허용하되 간접 경로 환산은 하지 않는다.
3. 구매·생산·판매 이동을 개별적으로 환산하지 않는다. 그 경우에는 원천 업무 행과 수량·단위를 함께 정비해야 하기 때문이다. EA 소모품이 TON으로 들어간 자료도 이름만 바꾸지 않는다.
4. 기존 재고 합계에는 이미 단위가 섞였으므로 그 합계에 1,000을 곱하지 않는다. 검증 가능한 입출고를 회사·조직·품목·창고·단위별로 누적해 해당 기준일의 재고를 다시 산출한다.
5. 잘못된 이동이나 원천 합계가 하나라도 있는 품목은 해당 회사의 그 품목 재산출을 보류한다. 품목 자체를 특정할 수 없는 결함은 전체 보류한다. 남은 일부 이동 합계로 초록을 만들지 않는다. 기존 전역 수량 검사의 보류 규칙도 완화하지 않았다.
6. 질적 상태별 배분 근거가 없으므로 품질/보류 재고가 0이 아닌 행은 자동 재분류하지 않는다. 안전재고는 정책량으로 따로 보존/환산하며 물리 재고 합계에 넣지 않는다. 예를 들어 기존 정책이 TON으로 표시돼 있으면 그 수량도 명시적 계수로 환산한다. 이 정책량이 업무상 적절하다고 검증한 것은 아니다.
7. 빠진 창고의 재고 행은 같은 회사·조직의 기존 월말 달력 안에서만 만든다. 그 창고·품목·날짜에 정확히 대응하는 full 행이 있어야 정책과 식별자를 재사용한다. **다른 창고의 안전재고 정책을 빌리지 않고, full의 과거 재고 수량도 가져오지 않는다.** 수량은 현재 프로필의 이동에서 다시 계산한다.
8. 숫자가 같고 소수점 표기만 다른 행은 변경 명세에서 제외한다. 계산한 행 수와 실제 변경 행 수를 별도로 기록한다.

### 실제 모수와 결과

아래 수량 진단은 같은 행에 중복될 수 있으며 생산 투입 계약 진단과 별도 집계다.

| 항목 | quick | full |
|---|---:|---:|
| 원본 이동 / 생산 배치 | 5,000 / 650 | 30,000 / 8,000 |
| 질량 환산 후보 이동 | 108 | 46 |
| 재산출한 재고 행 (변경 없는 행 포함) | 462 | 34,524 |
| 기존 재고 값/단위 변경 | 60 | 252 |
| 누락 창고 재고 행 추가 | 48 | 0 |
| 최종 후보 재고 행 수 | 588 (원본 540) | 55,440 (원본과 같음) |
| 재산출 보류 기존 재고 행 | 126 | 20,916 |
| 단위 진단 전 → 후 | 1,098 → 972 | 28,076 → 27,778 |
| 음수 재고 스냅숏 진단 | 16 → 16 | 1,082 → 1,082 |
| 수량 진단 합계 전 → 후 | 1,114 → 988 | 29,158 → 28,860 |
| 이동 기록상 부족 발생 품목·창고 조합 | 5 | 24 |

quick은 기존 414행을 재계산한 뒤 48행을 추가했다. 원천 단위/합계 결함의 영향을 받는 7개 품목(잘못된 원료와 그 생산품 포함)을 보수적으로 격리했다. full은 83개 품목이 영향을 받는다. 이는 권한 격리·DB 상태 변경이 아니라 메모리 후보의 계산 보류다.

변경 없는 행을 제외하기 전 중간 명세에는 quick 414행/full 34,524행이 변경으로 표시됐다. 값과 단위가 같은 행을 제외한 **최종 근거는 `*-candidate-final.json`**이며 이전 파일은 경과 증적으로 보존했다. 초기 보고서의 변경 행 수를 최종 결과로 인용하지 않는다.

최종 held 명세는 quick 774건(환산 불가 648 + 재고 보류 126), full 24,965건(기존 MFG-01 보류 843 + 환산 불가 3,206 + 재고 보류 20,916)이다. 이 역시 '서로 다른 불량 업무 객체 수'로 확대하지 않는다.

**두 프로필 모두 `REVIEW_ONLY / FAIL`이다.** quick의 이전 창고 재산출 요구 12건은 전체 완료로 닫지 않았다. 전역 검사도 무효 이동이 남아 재고 합계 대사를 계속 보류한다. 부분적인 재고 산술 일치를 인증 가능·시뮬레이션 준비 완료로 보고하지 않는다.

### 음수 재고는 지우지 않고 원인을 보존

월말 사이에 부족했다가 회복되는 경우도 일별 누적으로 탐지한다. 보고서에는 최초 부족일, 그날 직전/직후 잔량, 관련 이동 ID·유형·수량, 기간 중 최소 일말 잔량이 들어간다. **같은 날 안의 선후관계는 원천 시각이 없으므로 판단하지 않는다.** 추가 기초재고·입고·보정 수량은 0이며, 음수도 그대로 재산출한다.

quick 사례: 동정광 `RM-CU-CONC / LOC-P1-RAW`는 기록상 2026-06-13에 18.536톤에서 생산 불출 3건 후 -104.612톤으로 내려간다. 이는 저장된 이동원장의 사실이며, 아래 투입 모델이 아직 검증되지 않았으므로 '실제 공정에서 정확히 이 물량이 필요하다'는 의미가 아니다. 보충 구매를 만들거나 생산일을 임의 변경하지 않았다.

### 새로 구체화한 생산 입력 구조의 공백

기존 생성기의 배치는 `input_material_id/input_quantity/quantity_uom` 한 쌍으로 투입을 표현한다. 그런데 NISO4·LIOH 계열 BOM은 여러 투입 자재가 필요하다. 또한 일반 제품의 BOM 생성에서 EA 소모품을 선택하면서 `input_uom=TON`으로 적는 경로가 있다.

`inspect_batch_input_contract()`는 유효 BOM을 기준으로 이를 별도 노출한다. 한 배치에서 먼저 확인된 차단 사유 1개를 집계하므로 전체 가능한 결함을 중복해서 세지는 않는다.

| 배치 입력 구조 진단 | quick | full |
|---|---:|---:|
| 여러 원료 투입을 현행 단일 투입 행으로 표현 불가 | 217 | 459 |
| BOM 투입 단위와 품목 기준단위 불일치 | 108 | 2,975 |
| 배치 날짜에 유효한 투입 BOM 없음 | 0 | 1,131 |
| 합계 | 325 | 4,565 |

나머지 배치의 실제 투입 수량·수율까지 맞는다는 뜻은 아니다. 생성기는 아직 `output_quantity / actual_yield`로 첫 원료량을 만들며 BOM 계수를 반영하지 않는다. 따라서 **실제 투입량·배치별 계획 배분·생산/판매 연결·재고 금액·전표 연결은 이번 대사 범위 밖**이다. full의 생산계획 843행 유효 BOM 부재도 유지했으며 유효일을 소급하지 않았다.

### 회귀·변이·실측 증적

- 신규 회귀: `tests/test_kit_stock_revision.py` **34 passed / 26.15초**. 독립 작은 원장, TON/KG 수량·정책 환산, EA 비변환, 시점/회사/중복/계수/정밀도, 음수·월중 부족·미래 이동, 원천 합계/품질 상태/신규 창고 정책, 실제 quick/full, CLI 배선, 결정론 및 원본 전체 SHA 보존을 검사했다.
- 핵심 행동 변이 **4/4 포착**: 환산계수 미적용, 음수 0 처리, 미해결 품목 보류 제거, EA를 질량 목록/계수에 넣어 실제 변환 허용. 별도 프로세스의 메모리 소스에만 적용했고 각 시험의 수량/행 불변 단언 실패를 확인했다. 디스크 소스는 변이하지 않았다.
- EA 관문 한 줄만 제거한 최초 탐침은 다음 관문이 여전히 막아 차단 사유 차이만 검출했다. 이것을 실제 환산 우회 검출로 세지 않고, EA 허용 목록과 계수 2곳에 변이를 적용해 **행이 실제로 바뀌는 실패**로 다시 확인했다.
- 최종 정상 코드의 영향 회귀 **263 passed / 131.97초 / exit 0** (기존 229 + 신규 34). T1과 중복 합산하지 않았다. T3는 일일 예산 및 기존 통합 귀속 HOLD를 유지하며 추가 실행하지 않았다. 이 결과를 전체 통합 T3 통과로 표현하지 않는다.

최종 검토 명세:

- `tmp/kit-k2-stock-20260908/quick-candidate-final.json` — `ad332f24693b9cb259932c4fa36d39f7775916cb62c15aa4f93ac81bb9ae1900`.
- `tmp/kit-k2-stock-20260908/full-candidate-final.json` — `5c71333055263348486d5003f7fb3c8cf16609d0fe9462fb54137608592cab8d`.
- CLI exit 1은 남은 데이터 결함을 보고한 정상 진단 종료다. 예외 종료나 설치 실패가 아니다. 적용/쓰기 옵션은 제공하지 않는다.

```powershell
venv/Scripts/python.exe -m scripts.plan_business_kit_initial_revision --profile quick --resolve-exact-bom-copies --repair-warehouses --rebuild-stock --report tmp/stock-candidate-next.json
venv/Scripts/python.exe -m pytest -x -p no:warnings tests/test_kit_stock_revision.py tests/test_kit_warehouse_revision.py tests/test_kit_quantity_audit.py tests/test_kit_sample_audit.py tests/test_kit_sample_revision.py tests/test_business_kit_catalog.py tests/test_starter_package_catalog.py tests/test_kit_app_contract.py tests/test_kit_app_builder.py tests/test_kit_app_api.py
```

### 다음 수행 묶음 — 재검사 반복이 아니라 생산·수량 모델 보정

담당 Codex. 다음 순서로 원래 업무키트 입력 흐름의 준비도를 높인다.

1. MFG-02의 기존 배치 식별자·품질/입출고 참조는 보존하면서, 배치별 **원료 투입 명세**를 표현할 하위 구조와 각 원료의 수량·단위를 확정한다. 한 배치를 여러 행으로 복제해 생산량을 중복 집계하지 않는다. 관련 소비자 목록을 먼저 확인하고 기존 단일 투입과의 호환 범위를 명시한다.
2. BOM 계수와 수율을 반영해 투입량을 재생성한다. 동일 원료 BOM은 먼저 합산하고, 질량 투입과 EA 소모품은 차원을 분리한다. EA를 TON으로 환산하거나 다른 원료로 바꾸는 임의 보정은 하지 않는다. 이는 새로운 합성 상세 시나리오이며 실적 복구라고 표현하지 않는다.
3. 생산 계획·배치 배분·판매 출고의 시점과 수량을 함께 정렬한다. 부족을 없애려고 입고를 끼워 넣는 대신 구매/도착/재고/생산/출고가 설명되는 합성 시나리오를 구성하고 가정을 기록한다. 정상 흐름과 실제로 부족이 드러나는 부정 사례를 분리해 검증한다.
4. 그 후보에 이번 재고 재산출/진단을 다시 적용하고, 기간별 수량·재고를 대사한다. 유효일을 소급하지 않으며 기준기간 밖 자료는 별도 모델 판단으로 남긴다.
5. 대사 완료 후 같은 제품의 미리보기·새 판 등록·브라우저 업무 실행으로 연결한다. 그 전까지 이 후보를 운영 DB에 적재하지 않는다. PostgreSQL·서비스 배포·외부 수집기 UI는 별도 기존 계획과 혼동하지 않는다.

Git 스테이징·커밋·fetch·pull·push 없음. 기존 미커밋 변경과 상호작용 로그는 유지했다. 공식 문서와 사용자 화면은 수정하지 않았다.

## 13. 다중 원료 투입 후보 및 릴리즈 정리 요청으로의 전환

기록일 2026-09-08 · 작업 Codex. 사용자가 미완성 CRM 릴리즈 정리를 요청해 그 작업을 우선했다. 최초 보류 후 사용자 승인으로 릴리즈 17건 사용 중단을 적용했으며, 상세 증적은 `RELEASE_CATALOG_CLEANUP_2026-09-08.md` §7에 기록했다. 생산 후보는 여전히 원본 샘플·운영 인증판·DB에 적용하지 않았다.

### 생산 투입 구조 후보

- `batch-inputs/1`은 배치를 복제하지 않고 `input_lines`에 원료·수량·단위·Lot·BOM 참조를 보관한다. 생산 입고는 기존 배치당 한 번만 유지한다.
- 같은 원료의 BOM을 합산한 뒤 수율과 계수를 적용한다. 서로 다른 원료 수량을 더해서 합계가 같다고 통과시키지 않는다.
- EA는 원래 EA 단위와 명시적인 `PER_GOOD_OUTPUT / CEILING` 정책이 있어야 하며 질량과 환산하지 않는다. 모델로 만든 투입량은 측정 실적으로 주장하지 않는다.
- 기존 명세의 불출·입고 수량, 창고, 일자, 참조와 제공된 Lot까지 대조한 뒤 한 배치 단위로 후보를 만든다. 잘못된 새 입력을 옛 단일 투입 필드로 조용히 되돌리지 않는다.
- 원료별 불출 참조와 실제 수량 검사기·재고 후보 소비자·기존 샘플 검증기·후보 CLI를 연결했다. 원본 생산량과 식별자를 유지하며 스크랩은 다른 단위 수량의 단순 차감으로 만들어내지 않는다.

### quick 실측 — 운영 등록이 아닌 검토 명세

`tmp/kit-k3-production-20260908/quick-candidate.json` / SHA256 `606c4f22c183f0aa44860007a0516206c5dc58ed1caf63d67000fc50414e58db`.

| 항목 | 결과 |
|---|---:|
| 배치 전체 | 650 |
| 다중 투입 계약으로 재작성 가능한 합성 배치 | 542 |
| 추가 원료 불출 후보 | 325 |
| BOM 기준단위 불일치로 보류한 배치 | 108 |
| 후보에 남은 단위 진단 | 972 |
| 후보에 남은 소수 EA 입출고 진단 | 19 |
| 후보에 남은 음수 재고 진단 | 20 |
| 수량 진단 합계 | 1,011 |

**REVIEW_ONLY / FAIL, 계약은 DRAFT_NOT_INSTALLED**다. 기존 재고 후보의 음수 재고 16건보다 20건으로 늘어난 것은 모델 투입량을 반영했을 때 드러나는 부족이다. 없애려고 기초재고/입고를 추가하지 않았다. 실제 투입 관측·Lot 계보·계획 배분·판매 연결·금액 및 전표는 아직 검증 범위 밖이다. full 생산 후보는 이번에 실측하지 않았다.

### 회귀 과정의 정정

1. 불량 원료 하나 때문에 모든 재고 행이 그대로여야 한다던 시험은 과도했다. 결함 원료는 보류하지만 독립적으로 유효한 다른 품목은 계산한다는 실제 계약에 맞춰 영향 품목을 확인하게 고쳤다.
2. 새 EA 정수 검사가 기존 full 260건/quick 19건의 소수 EA 입출고를 잡았다. 이전 재고 회귀의 기대값이 이 신규 진단을 포함하지 않아 순서대로 실패했다. 독립적인 원본 CSV 개수 대조에서도 260/19건을 확인한 뒤 기대값을 확장했다. 수량을 반올림하거나 진단을 끄지 않았다.
3. 최종 영향 회귀는 릴리즈 관련 회귀를 포함해 **176 passed / 46.13초**다. 순차 실행했으며 이전 실패 실행과 합산하지 않았다. 전체 T3는 실행하지 않았다. CLI exit 1은 잔여 결함을 보고한 정상 진단 종료다.

다음 데이터 작업은 108건의 단위·EA 소모 정책을 명시한 합성 모델 확정, 생산계획/배치/판매출고 정렬, 그 이후 재고·금액 대사와 제품 등록이다. 이번 후보만으로 시연 준비 완료라고 보고하지 않는다.
