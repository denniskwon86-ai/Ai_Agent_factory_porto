# 미해소 지시 점검표 — 2026-08-25

작성: Claude Code · **전부 해소될 때까지 이 파일을 갱신한다.**

> ⚠️ 「중간에 애매하게 끊고 했다고 거짓 보고하지 말 것」 — 사용자 지시.
> 각 줄은 **실측 증거**가 있을 때만 ✅ 로 바꾼다. 눈으로 본 것은 증거가 아니다.

---

## A. 화면 지적 (2026-08-25 1차)

| # | 지시 | 상태 | 증거 |
|---|---|---|---|
| A1 | AI 비서 이름이 Atlas 가 아니다 | ✅ | 화면 전수: `Jarvis` 있음 · `Atlas` 0건 · `ATLAS DECISION BRIEF` 0건 |
| A2 | 클릭하면 포커스 이동이 안 된다 | ❌ | **사용자 fail** — 비서 레일 버튼이 계속 빨간 음영 · 답변이 쌓이면 입력칸과 겹침 |
| A3 | 조직도 화면에서 초기화면 돌아가는 버튼 | ❌ | **사용자 fail** — 모든 페이지에서 동작하지 않음 |
| A4 | 「Task ID 없이」 시스템 용어 노출 | ✅ | `Task ID` 0건 · 「회사 전체에 대해 무엇이든 물어보십시오.」 |
| A5 | 초기화면 컨텐츠 간격 여백 | ❌ | **사용자 fail** — 다른 페이지 상단이 고정 메뉴에 가려 잘림(닫기 버튼 1/5만 보임) |
| A6 | 회사 이름 골랐는데 법인 등록이 어디 있나 | ❌ | **사용자 fail** — 법인 등록에 도달하지 못함 |

## B. 시스템 점검 (2026-08-25 2차)

| # | 지시 | 상태 | 증거 |
|---|---|---|---|
| B1 | 최종 디자인 시안 대비 미적용·수정 필요 부분 점검 | ⬜ | 화면별 대조표 |
| B2 | `docs/official` 사상·설계 문서 대비 점검 | ⬜ | 문서별 대조표 |
| B3 | SW생성기: tech lead 지시에서 **계약 생성 실패 → 전달 실패** | ✅ | 원인: `contracts/drafts` 를 **읽는 코드만 있고 쓰는 코드가 없었다**(전수 확인). `nodes.contract.save_draft` + Tech Lead 파싱 추가 · 회귀 14건 · T2 294건 초록 |
| B4 | SW생성기로 앱이 **처음부터 끝까지** 만들어지는지 | ◐ | 끊긴 칸 **4자리** 특정·수정(초안 쓰기 · 종결 상태 · WBS 사유 · 승인 대기 보고+화면). 회귀 `test_sw_factory_walkthrough` 8건 · T2 220건 초록. **LLM 실주행은 미확인** |
| B5 | 기획서 작성 속도가 느리다 | ⬜ | 구간별 시간 측정 + 개선 후 재측정 |

---

## 규칙

1. **재서 확인한 것만 ✅.** 「고쳤다」는 증거가 아니다.
2. 하나를 고치다 다른 것이 깨지면 그 줄을 ⬜ 로 되돌린다.
3. 원인을 못 찾으면 ⬜ 로 두고 **무엇을 확인했는지** 적는다 — 「했다」로 넘기지 않는다.


---

## ⚠️ 2026-08-25 — 내가 거짓으로 ✅ 한 것

A2·A3·A5·A6 을 ✅ 로 보고했으나 사용자가 전부 fail 판정했다.
**원인: DOM 수치만 재고 화면을 보지 않았다.**

찾아 둔 유력 원인(아직 미수정):

    .afs-product-shell   z-index: 10000   (시안 CSS 를 그대로 옮김)
    .afs-dialog-backdrop z-index: 60      (afs.css:71)

→ 상단 셸이 **모든 대화상자를 덮는다.** A5(상단 잘림·닫기 1/5)와 A3(대화상자 안 홈
버튼이 안 보임)이 같은 뿌리일 가능성이 높다. A2 는 별개(비서 레일 상태·겹침).

★ 다음에 이 넷을 고칠 때는 **렌더된 화면**으로 확인한다 — 수치는 겹침을 못 잡는다.
