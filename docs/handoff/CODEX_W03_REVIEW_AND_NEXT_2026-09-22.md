# W03.1·W03.2 검토와 다음 작업지시 — 브랜치 `claude/w03-atomic-save-20260922` 한정

> ⚠️⚠️ **이것은 자기 검토다.** 같은 세션이 구현하고 같은 세션이 검토했다. 사용자가 이
> 브랜치에 한해 Codex 역할을 대신하라고 지시해 작성한 것이며, **독립 검토가 아니다.**
> 팀 규약상 공용 원장(`OPERATIONAL_TRIAL_DELIVERY_STEPS.json`·`PROGRESS.md`)의 단일
> 기록자는 Codex다 — **이 문서는 원장을 갱신하지 않는다.** 점수도 올리지 않는다.
> 진짜 수용은 Codex가 이 브랜치를 받아 판단한 뒤에 일어난다.

- 대상: `claude/w03-atomic-save-20260922` (기준 `9c27a9ed9`, 커밋 5개)
- 검토 기준: 원장 acceptance 원문 + `CLAUDE_P03_EXECUTION_RESULT.md` §4-2 지시
- 결론 먼저: **W03.1 수용 가능(조건 명시)** · **W03.2 조건부 보류 — CR-1 해결 후**

---

## 1. W03.1 — 두 노드 같은 판본 읽기 (45점)

acceptance: *「실제 공유 저장에서 두 노드가 같은 프로젝트/릴리스 판본을 읽고 접근권한을
확인한다」*

| 요소 | 증거 | 판정 |
|---|---|---|
| 릴리스 판본 공유 | digest·소유문맥 6필드 일치, 두 프로세스 | ✅ |
| **프로젝트 판본 공유** | digest·소유문맥 6필드, 두 프로세스 | ✅ |
| **접근권한 확인** | `ownership_visible` 로 주체 3종, 타부서 **거절** | ✅ |
| 공유 «실체»(복제본·표식 아님) | 내용 digest 기준, 경로 문자열 아님 | ✅ |
| 음성 대조군 | 옛 해석에서 두 갈래 모두 갈라짐 | ✅ |
| 판정의 노드 간 일치 | A·B 판정 동일 | ✅ (요구 밖이나 유의미) |

**판정: 수용 가능.** 단 아래를 수용 조건으로 명시해야 한다.

- ⚠️ **「실제 공유 저장」의 범위는 단일 PC 의 독립 프로세스**다. 두 호스트·공유 마운트가
  아니다. 인계서가 「구분해서 기록한다」고 했고 실제로 구분돼 있으므로 이 단계에서는
  충족으로 보되, **W03 전체(두 노드 배포) 수용으로 읽으면 안 된다.**
- 합성 자료 1판씩이다. 실조직·실계정이 아니다.
- 권한은 **읽기 가시성**(`ownership_visible`)만이다. 쓰기 권한·문맥 전환 거절은 미검증.

★ 구현자가 `AccessScope.unrestricted` 기본값이 `True` 라는 함정을 시험에 적어 둔 것은
적절하다 — 명시하지 않았다면 전원 통과해 「거절을 확인했다」가 거짓이 됐을 것이다.

---

## 2. W03.2 — 원자 저장·경쟁 실패 (45점)

acceptance: *「동시쓰기/중간실패에도 **부분 파일이나 잘못된 판본**을 읽지 않도록 저장
경로를 구현·검증한다」*

| 요소 | 증거 | 판정 |
|---|---|---|
| 부분 파일 방지 | probe torn **0**, 음성 대조군 **374** | ✅ |
| 중간 실패 시 이전 판본 보존 | 시험 3종 + 변이 7건 실패 | ✅ |
| 임시파일 같은 filesystem | 같은 디렉터리 단언 | ✅ |
| 임시파일 잔여 없음 | probe·시험 모두 0 | ✅ |
| **대상/상위 링크 보존** | — | ❌ **CR-1** |
| **「잘못된 판본」/성공 버전 소실** | — | ⚠️ **미충족 잔여** |

**판정: 조건부 보류.** CR-1 을 해결하기 전에는 수용하지 않는다.

### CR-1 (차단) — 링크 정책이 저장소와 **반대**다

`core/atomic_write.py:_resolved_target()` 은 대상이 symlink·junction 이면 **따라가서** 실제
파일에 쓴다. 구현자가 「공유 저장을 링크로 거는 구성을 지원해야 한다」를 근거로 댔다.

**그 근거가 틀렸다. 이 저장소는 링크를 «거절»하는 쪽으로 이미 확립돼 있다.**

```
core/async_orchestrator.py:295        project_meta.json  링크면 ValueError("연결된 프로젝트 메타")
api/routes/factory_control.py:2448    latest_state.json  링크면 503 "연결된 상태 원문"
api/routes/factory_control.py:2631·2662                  "연결된 파일을 원본으로 인정하지 않는다"
core/studio_project_files.py:53·56 · core/studio_pause_state.py:37
core/studio_revision_requests.py:81·87 · core/studio_contract_reconcile.py:219·292
api/routes/studio_input_draft_control.py:106
```

P05.1 검토에서 **「출력 경로 junction 차단 누락」이 결함으로 지적**된 것과 같은 방향이다.

⚠️ **실제로 만들어지는 상태**: `latest_state.json` 이 링크일 때 —
쓰기는 링크를 따라가 **성공**하고, 읽기는 `factory_control.py:2448` 이 **503 으로 거절**한다.
쓰기와 읽기가 갈리는 비대칭이며, W03 이 없애려던 「한쪽에서만 열리는 자원」을 오히려 만든다.

**요구**: `_resolved_target()` 을 **차단**으로 바꾼다. 링크·junction 대상이면 쓰지 말고
식별 가능한 오류로 끊는다. 대상뿐 아니라 **상위 경로**도 본다
(`scripts/session_data_snapshot.py:70 regular()` 가 이미 그 형태다 — 재사용 검토).
시험의 `test_a_linked_target_stays_a_link` 는 **반대 방향으로 뒤집는다.**

### 미충족 잔여 — 「잘못된 판본」과 「성공 버전 소실」

구현자는 lost update 를 「지시가 다른 문제라고 했으므로 범위 밖」으로 두었다. 지시 원문
(§4-2)은 **「두 가지를 한 번에 달성했다고 쓰지 말라」**는 뜻이고 **「하나만 해도 된다」가
아니다.** acceptance 에도 「부분 파일**이나 잘못된 판본**」으로 둘 다 들어 있다.

다만 구현자가 **하지 않았음을 문서·코드 주석·probe 출력에 일관되게 명시**했으므로 과장은
없다. 이 부분은 **결함이 아니라 잔여**로 다룬다 — §4 에서 별도 단계로 뗀다.

★ 더 구체적인 잔여 하나: 지시의 **「정상 반환한 성공 버전의 소실을 허용하지 않는다」**가
`core/async_orchestrator.py:_save_latest_state` 에서 **실제로 깨진다.** Windows 교체 거절이
예외로 오는데(probe 실측 41/180) 그 함수가 `except Exception: print(...)` 로 삼킨다.
호출자는 저장된 줄 알고 진행한다. 구현자가 「동작이 바뀌므로 판단 요청」으로 남겼고,
그 판단은 **§4 에서 내린다.**

### 관측 — 원자 쓰기 구현이 저장소에 **넷** 있다

구현자가 「구현을 한 곳에 둔다」고 썼으나 통합한 것은 하나뿐이다.

```
core/studio_project_files.py:write_json   → core/atomic_write 로 통합함 ✅
core/config_snapshot.py:160               → 그대로 (os.replace, "반쪽 파일이 남지 않게")
core/contract_decision.py:298             → 그대로 (os.replace)
core/atomic_write.py                      → 이번 신규
```

W03.2 대상(release.json·latest_state.json) 밖이라 **이번 판정에 영향은 없다.** 다만 서술은
「정본 저장 범위에서 한 곳」으로 좁혀 적어야 정확하다. 통합은 §4 의 낮은 우선순위로 둔다.

---

## 3. 증거의 실재성 — 검토자가 직접 확인한 것

- `w03_atomic_write_probe.py compare` · `w03_shared_read_probe.py compare` 둘 다 **exit 0**,
  음성 대조군이 실제로 깨진다(374 / 두 갈래 모두). 대조군이 성립하지 않으면 무효라고
  probe 스스로 exit 2 로 알리게 돼 있다 — 적절하다.
- 변이 검증이 실제로 수행됐다(원자성 제거 → 7건 실패 → 원복 해시 일치). 검사가 무언가를
  지키고 있다는 증거다.
- 격리: `sources_unchanged: true` · `protected_assets_unchanged: true` ·
  `blocked_file_writes: []` · `repository_conftest_loaded: false`.
- 회귀 8스위트 **176 passed**, W03 묶음 **21 passed / 1 skipped**.
- 구현자가 보고한 실수 셋(계측 혼동·묶음 오선택·지뢰 #3 반복)은 기록과 코드에서 확인된다.
  **숨기지 않았다.**

★ `test_advisor_bootstrap.py` 19 errors 를 「러너 대상 아님」으로 판정한 근거(`seeded_org`
가 `tests/conftest.py:539` 에 있고 러너가 conftest 를 읽지 않음)는 **타당하다.** 다만 그
스위트가 격리 러너에서 영영 못 도는 상태로 남는 것은 별건 부채다 — §4 에 적는다.

---

## 4. 다음 작업지시 — `claude/w03-atomic-save-20260922` 한정

우선순위 순. **각 항목은 앞 항목의 산출물을 소비한다.** 단계마다 새 검토 문서를 만들지
말고 `CLAUDE_P03_EXECUTION_RESULT.md` 에 누적한다.

### ① CR-1 링크 정책 정정 — **차단 해제용, 최우선** (20~40분)

- `_resolved_target()` 을 따라가기 → **차단**으로. 대상과 **상위 경로** 모두 본다.
- `scripts/session_data_snapshot.py:70 regular()` 를 먼저 읽고 **재사용 가능한지 판단**한다.
  새 차단기를 또 만들지 않는다. 재사용이 안 되면 왜인지 한 줄 적는다.
- 시험 `test_a_linked_target_stays_a_link` 를 **거절 확인으로 뒤집는다.**
  Windows 에서 symlink 생성 권한이 없으면 junction 으로 대체하거나 skip 사유를 남긴다.
- 모듈 주석의 「링크를 따라간다」 근거 문단을 **정정**한다 — 틀린 근거를 남기면 다음
  사람이 같은 판단을 반복한다.
- 출구: 링크 대상 쓰기가 식별 가능한 오류로 끊기고, 저장소의 읽기 쪽 차단과 **같은 방향**임을
  시험이 보인다.

### ② `_save_latest_state` 삼킴 — **판단과 함께 처리** (30~60분)

판단을 내린다: **삼킴을 유지하되 «저장 실패」를 관측 가능하게 만든다.**

- 근거: 예외를 그대로 올리면 스프린트 실행이 중단된다. 상태 저장 실패로 **실행 전체를
  잃는 것**이 더 나쁘다. 그러나 지금은 실패가 `print` 로만 남아 **아무도 모른다.**
- 요구: 실패를 구조화된 신호로 남긴다(기존 로깅·브로드캐스트 경로 재사용, 새 채널 금지).
  **최소한 「마지막 저장이 실패한 상태」를 뒤에서 확인할 수 있어야 한다.**
- ⚠️ 여기서 재시도 횟수를 늘리는 식으로 덮지 않는다. 41/180 은 reader 가 3초에 5천 번 여는
  **극단 조건**이며, 실제 부하에서의 수치는 아직 모른다. **모르는 것을 모른다고 적는다.**
- 출구: 저장 실패가 실행을 멈추지 않으면서 **사후에 식별 가능**하고, 그 경로에 시험이 있다.

### ③ W03.3 착수 — DB·파일 일관성 복구 (40점, 원장 next)

원장 acceptance: *「DB 상태와 정본 파일의 불일치를 식별/복구하고 다른 문맥 노출을 차단한다」*
선행 `W03.2`·`P01.1`.

- **W03.2 의 산출물을 소비한다** — 원자 저장으로 「반쯤 쓰인 파일」은 사라졌으므로, 남는
  불일치는 **DB 와 파일이 서로 다른 것을 말하는 경우**다. 그 경우만 다룬다.
- 먼저 **어떤 불일치가 실제로 가능한지 목록**을 만들고 시작한다. 가설로 복구기를 만들지
  않는다. `library/`·`projects/` 가 이 PC 에 0건이므로 합성으로 재연한다.
- ⚠️ **자료 삭제·정본 선택이 필요한 충돌은 임의로 덮어쓰지 말고 주요 결정으로 올린다**
  (§4-2 원문). 복구기가 조용히 한쪽을 이기게 하면 그게 더 큰 사고다.
- ⚠️ 「다른 문맥 노출 차단」은 W03.1 의 `ownership_visible` 경로를 재사용한다. 새 권한
  판정을 만들지 않는다.
- timebox 1~3시간. 넘길 것 같으면 **착수 전에** 산출물 경계로 재분할을 제안한다.

### ④ 낮은 우선순위 — 서두르지 않는다

- 원자 쓰기 구현 4→1 통합(`config_snapshot.py:160`·`contract_decision.py:298`). 두 곳 다
  정본 저장이 아니라 급하지 않다. **W03.3 중에 끼워 넣지 말 것.**
- `test_advisor_bootstrap.py` 가 격리 러너에서 못 도는 부채(전용 합성 fixture 필요).
- lost update 방지. **별도 단계로 떼어 원장에 올릴지부터** Codex 가 정한다 — 지금 구현에
  손대면 W03.2 의 경계가 흐려진다.

### 공통 경계

- 운영 `data/`·`library/`·`projects/` 무접촉. 격리 러너 `--strict-writes` 만.
- 격리·쓰기 보호를 꺼서 통과시키지 않는다. skip/xfail·단언 약화 금지.
- 소스 변이는 표시 → 원복 해시 확인. **검사가 도는 중에 소스를 고치지 않는다.**
- 이 브랜치에만 커밋한다. `codex/l2-unified-studio-20260912` 를 건드리지 않는다.
- **진척은 올리지 않는다.** 1855/5300 = 35.0% 유지. W03.1 +45 · W03.2 +45 는 Codex 의
  실제 수용 뒤에 Codex 가 원장에 기록한다.

---

## 5. Codex 에게 넘길 때 함께 볼 것

1. **CR-1 의 방향이 맞는가** — 링크를 차단하는 것이 정말 이 저장소의 정책인가, 아니면
   읽기 쪽만 그런 것이고 공유 마운트 지원이 별도 계획에 있는가. 위 아홉 곳은 전부 읽기·
   검증 경로다. **쓰기 쪽 정책이 명시된 문서를 찾지 못했다.**
2. **「잘못된 판본」의 해석** — acceptance 의 그 문구가 lost update 를 포함하는가.
   포함한다면 W03.2 는 잔여를 안고 부분 수용이고, 아니라면 완결이다. **이 판단은 점수에
   직접 영향한다.**
3. 단일 PC 증거를 W03.1 수용 근거로 받을 것인가, 두 호스트를 후속 단계로 뗄 것인가.
