# 데이터 판본·키트 수명주기 실행계획

- 기준일: 2026-09-10
- 상태: `P4_DONE` — P1~P4 완료 · 1.1.0 생성(2026-09-11) · 제련 드라이버는 1.2.0 으로 밀림
- 기준 브랜치/커밋: `integration/g2-vertical-loop-20260821` / `7a10af750`
- 발단: 검증이 끝난 `KIT-MFG-NONFERROUS-PROCUREMENT 1.0.0` 을 in-place 로 수정한 사고
  (`fddf13d59` → `7a10af750` 으로 되돌림)

---

## 1. 무엇이 일어났나

제련 특성화 작업 중 드라이버 5 개 추가와 `industry_code` 정정을 **1.0.0 판본에 직접**
적용했다. `manifest.json` 의 `status` 는 `VALIDATED_FOR_DEMO` 였고 `generated_at` 은
`2026-08-11` 이었는데, 재생성 스크립트 한 번으로 `2026-09-10` 이 됐다. 초안 문서 제목은
「1.1.0」이라 써 놓고 구현은 1.0.0 을 덮어썼다.

부수적으로 Excel 템플릿 35 개도 삭제됐다가 재생성됐는데, 재계산에 필요한 LibreOffice 가
없어 **수식 셀이 빈 채로** 남았다. 커밋 상태는 검증 PASS 였으므로 되돌렸다.

---

## 2. 진단 — 확인한 사실

추측을 빼고 코드·파일에서 확인한 것만 적는다.

| # | 사실 | 근거 |
|---|---|---|
| 1 | **DB 계층은 구현돼 있다.** 테이블 9 개(`kit_registry_versions`·`kit_instances`·`source_bindings`·`dataset_snapshots` 등), 상태기계(DRAFT→VALIDATED→APPROVED→ACTIVE→RETIRED), ACTIVE 유일성을 DB 부분 유일 인덱스로 보장 | `core/data_preparation/store.py` · `models.py` |
| 2 | **API 가 그 계층을 쓴다.** 데이터준비·기준선·계산승인 라우트가 `data_preparation_store` 를 import 하고, `register_all(store)` 로 키트를 등록한다 | `api/routes/data_preparation_control.py:189` 외 |
| 3 | **로컬에 `data_preparation.db` 가 없다.** 다른 DB 9 개는 오늘 09:02 에 갱신됐다 — 서버는 떴지만 **데이터 준비 엔드포인트를 호출한 적이 없다** | `data/` 디렉터리 |
| 4 | **DB 가 있었어도 이번 사고는 막히지 않는다.** `upsert_kit_version` 은 같은 `(kit_id, version)` 이면 **내용으로 덮는다.** 주석도 「문서가 고쳐지면 지문이 달라지고, 그 사실이 보여야 한다」이지 「막는다」가 아니다 | `store.py:363-394` |
| 5 | **생성기가 판본을 하나만 만든다.** `KIT_VERSION = "1.0.0"` 하드코딩이라, 1.1.0 을 내려면 생성기를 고쳐야 하고 **고치는 순간 1.0.0 을 재현할 능력을 잃는다** | `scripts/generate_sample_company_starter_kit.py:31` |
| 6 | **분류 데이터에 판본 개념이 없다.** `docs/business-taxonomy/samples/` 의 CSV 20 개는 파이프라인이 매번 덮어쓴다. 파일명의 「2026」은 데이터 기준연도이지 판본이 아니다 | `dump_datasets.py` · `apply_segments.py` |

### 2.1 그래서 진짜 문제는 「CSV 냐 DB 냐」가 아니다

**확정된 것을 확정된 채로 두는 장치가 어느 층에도 없다.**

| 층 | 덮어쓰기를 막는가 |
|---|---|
| 파일 | ✗ 누구나 쓴다 |
| 생성기 | ✗ 재실행하면 같은 경로에 덮어쓴다 |
| DB 등록부 | ✗ `upsert` 가 내용으로 덮는다 (사실 4) |
| git | △ 이력은 남지만 **실수를 막지는 못한다** |

DB 로 옮기는 것은 **질의·동시성·참조무결성**을 얻는 일이고, 판본 불변성은 **별도로
설계해야 얻는다.** 둘을 한 문장으로 묶으면 「DB 로 옮겼으니 안전하다」는 오해가 남는다.

---

## 3. 목적과 전체 흐름

### L0 — 목적

**AX 의사결정 플랫폼의 산업 커버리지를 정하고, 대상별 업무키트를 만들어 적용한다.**
판본 관리는 그 자체가 목적이 아니라, 3·4 단계에서 같은 사고가 반복되는 것을 막는 수단이다.

### L1 — 전체 흐름

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | **분류** — 어떤 사업들이 있나 | 거의 완료. 모수 21,613 · 인스턴스 20,478 · 셀 101 · 오판정 약 3% |
| 2 | **대상 선정** — 어디에 키트를 만드나 | 1 차 확정. 비철 제련 11 개사 30.6 조 (D-47) |
| 3 | **키트 제작** — 무엇을 담나 | **막힘.** 도메인 검토 대기 + 판본 체계 없음 |
| 4 | **적용** — 실제 회사에 붙인다 | 미착수 |

3 단계가 두 가지로 막혀 있다. **도메인 검토**(제련 드라이버가 맞는가)는 사람이 답할
일이고, **판본 체계**는 이 계획이 다룬다.

---

## 4. 단계별 계획

### P1. 확정 판본을 불변으로 만든다 — 최우선

사고 재발을 막는 최소 조치다. P2 이후를 안 하더라도 이것만은 있어야 한다.

| | 작업 | 상태 |
|---|---|---|
| P1-1 | 생성기가 **이미 있는 판본 디렉터리에 쓰기를 거부**. `--force` 를 줘야 덮어쓴다 | ✅ `build()` 앞에 `kit_freeze.guard()` |
| P1-2 | `manifest.status` 가 `VALIDATED_*`·`APPROVED_*`·`CERTIFIED_*`·`RELEASED_*` 면 거부 | ✅ 표식을 깜빡해도 막히는 이중 안전장치 |
| P1-3 | `.frozen` 표식과 `fingerprint.json`(파일별 sha256) | ✅ `scripts/freeze_starter_kit.py` |
| P1-4 | `validate_*` 에 지문 대조 | ✅ **맨 앞에서** 대조한다 — 검증기가 뒤에서 manifest·보고서를 쓰므로 |

### P1 결과 (2026-09-10)

- `core/data_preparation/kit_freeze.py` 신설 · `scripts/freeze_starter_kit.py` 신설
- **1.0.0 동결 완료** — 175 개 파일 지문, `reason: 검증 PASS 406건`
- **사고 재현 시험 통과** — 생성기를 그냥 돌리면 `FrozenKitError` + 종료코드 1,
  `generated_at` 은 `2026-08-11` 그대로, 지문 대조 PASS
- 검증 406/406 PASS(지문 대조 1 건 추가) · 시험 14 건 신설 · 키트 시험 95 건 통과

**동결 판본에서는 검증기가 보고서·manifest 를 쓰지 않는다.** `validated_at` 한 줄만
바뀌어도 지문이 달라져, 검증을 돌릴 때마다 그 판본이 「변경됨」이 되기 때문이다.
확정 판본의 재검증은 **대조가 목적**이지 기록 갱신이 아니다.

### P2. 생성기가 판본을 여럿 만들 수 있게 한다

1.1.0 을 내기 위한 전제다. 지금은 1.0.0 을 재현할 수 없게 되므로 1.1.0 을 낼 수 없다.

| | 작업 | 상태 |
|---|---|---|
| P2-1 | `KIT_VERSION` 을 **인자**로 (`--version`) · `--out` 으로 다른 경로에 생성 | ✅ `use_version()` |
| P2-2 | 판본별 차이를 **데이터로 분리** | ✅ `scripts/kit_defs/` — **오버레이** 방식 |
| P2-3 | 1.0.0 을 새 구조로 재생성해 **지문이 같은지** 확인 | ✅ **데이터 137 개 전부 일치** |
| P2-4 | 1.1.0 생성 | ✅ **2026-09-11** — 단 **내용이 바뀌었다**(아래) |
| P2-5 | 제련 드라이버 5 · `C2421` · 시나리오 4 → **1.2.0 으로 밀림** | ⏸ **도메인 검토 대기** |

#### ⚠️ 1.1.0 이 다른 내용을 담게 됐다 (2026-09-11)

계획의 1.1.0 은 「제련 드라이버」였는데, **머지 사고 수리가 그 번호를 먼저 썼다.**
2026-09-05 갈래가 1.0.0 을 in-place 로 고친 것이 머지 39ed6e6af 로 들어왔고
(1.0.0 은 0ca2a58ad 에서 되돌렸다), 그 작업을 판본으로 다시 내야 했다.

1.1.0 = APP-06·07 등재 · `MDM-07` 원가센터명 · quick 프로필 `LOC-P1-FG` ·
**로직 버그 3 건**(`active` 문자열 판정 · BOM 대체판 충돌 · 범위 밖 창고 바꿔치기).
제련 드라이버는 도메인 검토가 끝나면 **1.2.0** 으로 낸다.

#### ★ 1.0.0 은 이제 재생성되지 않는다

버그 셋이 **로직 변경**이라 값 오버레이로 되돌릴 수 없다. `if l["active"]` 를
`company_profile_patches` 로 복원할 방법은 없다. 그래서 P2 가 세운 「생성기 기본 =
1.0.0」 구조를 유지할 수 없었고, **기본을 1.1.0 으로 옮기고 `load("1.0.0")` 이
거부**하게 했다.

이것이 P2-3 의 전제를 바꾼다. 1.0.0 을 보증하는 것은 이제 **재현 가능성이 아니라
파일과 지문**이다 — `kit_freeze.verify()` 가 175 개 파일을 대조하고 PASS 한다.
「돌렸더니 1.0.0 이 나왔는데 내용이 다른」 것이 가장 나쁘므로, 만들 수 없으면 만들 수
없다고 말한다.

#### 1.1.0 은 아직 확정이 아니다

Excel 템플릿 36 개가 없다 — 생성에 LibreOffice 재계산이 필요한데 이 환경에 없다.
지문 대장도 만들지 않았으므로 카탈로그가 `integrity=UNVERIFIED` 로 보여 준다.
Excel 까지 갖춘 뒤 `python scripts/freeze_starter_kit.py` 로 동결한다.

### P2 결과 (2026-09-10)

**왜 「완전 분리」가 아니라 「오버레이」인가.** 1.0.0 은 동결 판본이라 새 구조로
재생성했을 때 지문이 한 글자도 달라지면 안 된다. 1,300 줄 생성기에서 정의를 전부
뽑아내면 그 과정에서 순서·기본값이 미묘하게 달라지고 지문이 그것을 잡는다. 그래서
**기본 정의는 생성기에 두고 판본별 「차이」만 얹는다** — 1.0.0 의 오버레이는 비어
있으므로 결과가 같다. 판본이 셋 이상으로 늘어 「기본이 무엇인지」가 흐려지면 그때
완전 분리로 옮긴다.

**왜 JSON 이 아니라 파이썬인가.** 이 정의는 사람이 손으로 고치고, **왜 그런지가 값보다
중요하다.** 「제련사는 금속 가격으로 벌지 않는다」 같은 설명이 JSON 에서는 살지 못한다.

**P2-3 대조 결과** — 원본 175 · 재생성 137. 차이는 전부 설명된다.

| 차이 | 이유 |
|---|---|
| Excel 36 개 | 별도 스크립트 산출물 — 생성기가 만들지 않는다 |
| `validations/*.json` 2 개 | 검증기 산출물 |
| `manifest.json` | `generated_at`·`status`·`validation`·`excel_templates` — 생성 시각과 나중 단계가 채우는 값 |
| **데이터 파일 137 개** | **한 글자도 다르지 않다** |

로더가 막는 것 셋 — 없는 판본(오타로 새 판본이 생기는 것) · `OVERLAY` 누락 ·
**판본 불일치**(복사해서 만들 때 `version` 을 안 고치는 실수). 시험 12 건으로 고정했고,
그중 하나가 재생성 대조 자체다.

### P3. 등록부의 불변 판본 보호

**⚠️ P3-1 의 전제가 틀렸다.** 착수해 보니 「키트」가 두 종류이고 등록 경로가 다르다.

| | 무엇 | 경로 | 등록부에 들어가나 |
|---|---|---|---|
| 운영 템플릿 Profile | 조직에 적용할 데이터 계약 | `docs/data-kits/*.kit.json` (현재 1 개) | **예** — `register_all` 대상 |
| Starter Package | 샘플 회사·합성 데이터·앱·보고서 묶음 | `starter_kits/{id}/{version}/` | **아니오** — `starter_package_catalog` 은 읽기 전용 |

「운영 등록과 분리한다」는 의도된 설계다(`kit_registry.starter_package_catalog` 주석).
그래서 **우리가 동결한 1.0.0 은 `register_all` 로 등록할 수 없다** — P3-1 은 성립하지 않는다.

| | 작업 | 상태 |
|---|---|---|
| P3-1 | ~~`data_preparation.db` 생성하고 1.0.0 등록~~ | ❌ **불가** — Starter Package 는 등록부 대상이 아니다 |
| P3-2 | `upsert_kit_version` 에 불변 판본 보호 | ✅ `frozen` 열 + `StateConflict` |
| P3-3 | 판본 상태·지문을 드러낸다 | ✅ 카탈로그에 `frozen`·`integrity` · CLI 는 P1 의 `--list` |

### P3 결과 (2026-09-10)

**등록부 보호(P3-2)는 지금 당장 쓰이지 않지만 넣어 둔다.** `docs/data-kits` 의 Profile 도
확정되면 동결해야 하고, 나중에 Starter Package 를 등록부에 넣기로 하면 그때 필요하다.
막는 경우 넷을 시험으로 고정했다 — 미동결은 덮어쓰고 · 같은 지문이면 멱등이고 ·
**동결 판본의 지문이 달라지면 `StateConflict`** 이고 · **`frozen=False` 로 재등록해도
풀리지 않는다**(풀린다면 보호가 통째로 무력해진다).

**카탈로그가 동결 상태를 드러낸다(P3-3).** 화면에 「확정」이라고만 쓰고 대조 결과를
숨기면 파일이 바뀐 판본을 확정된 것으로 보여 주게 된다. 그래서 셋을 함께 낸다 —
`frozen` · `fingerprint_ledger` · `integrity`(`PASS`/`FAIL`/**`UNVERIFIED`**).
대장이 없으면 「검사하지 않았다」이지 「통과했다」가 아니다.

```
KIT-MFG-NONFERROUS-PROCUREMENT 1.0.0  AVAILABLE_FOR_DEMO  frozen=True   integrity=PASS
KIT-MFG-BATTERY-CHEMICAL-...   1.0.0  PREPARING           frozen=False  integrity=UNVERIFIED
```

**남은 결정** — Starter Package 를 등록부에 넣을 것인가. 넣으면 판본·지문·상태가 한
곳에서 관리되지만, 「운영 템플릿과 체험 패키지를 섞지 않는다」는 기존 설계와 충돌한다.

### P4. 분류 데이터에 판본을 준다 — **DB 이관**(2026-09-11 결정)

모수 21,613 건이 「전체 대상 산정」의 근거인데 지금은 덮어쓰기만 된다.

**P4-2 는 「CSV 유지」에서 「DB 이관」으로 뒤집혔다**(2026-09-11 사용자 지시). 앞선
판단은 「읽는 주체가 대부분 사람」이라는 이유였는데, 실제로 문제가 된 것은 읽기가 아니라
**쓰기**였다 — `reclassify.py` 가 `ftc-all-2026-classified.csv` 를 제자리에서 고치고,
`apply_segments.py` 가 `instances-2026.csv` 를 덮어쓴다. 사람이 열어 보는 편의는
export 로 얻을 수 있지만, **덮어쓰기를 막는 일은 파일로는 못 한다.**

#### 무엇을 옮기고 무엇을 남기나

| 층 | 어디에 | git 에 | 왜 |
|---|---|---|---|
| **정본** | `taxonomy.db` (SQLite) | ✗ (`*.db` 는 이미 gitignore) | 질의·불변성 강제·원천/결과 분리 |
| **보존** | `samples/snapshots/{id}/*.csv` | ✓ | DB 가 없어도 복원된다 — 왕복 동일성을 시험으로 고정 |
| 편의 | `samples/*.csv` (평면) | 제거 대상 | 덮어쓰기의 원인이었던 자리 |

⚠️ **DB 로 옮기는 것만으로는 불변성이 생기지 않는다**(2.1 절). 스냅샷 동결을 함께
넣어야 의미가 있다 — P3-2 에서 키트에 한 것과 같은 장치를 분류 데이터에도 둔다.

| | 작업 | 결과 |
|---|---|---|
| P4-T1 | **스키마 + 저장소 계층** `engine/taxonomy_db.py` — 원천 8 표 · 결과 5 표 · 스냅샷 대장 | ✅ 동결을 **DB 트리거**로 강제 — `sqlite3` 프롬프트에서도 막힌다 |
| P4-T2 | **이관** — CSV 를 첫 스냅샷으로. 원본은 건드리지 않는다 | ✅ 131,000 행이 한 글자도 다르지 않다. 끝난 뒤 스크립트는 `restore_db.py` 로 대체 |
| P4-T3 | **내보내기·복원** `export_snapshot.py` · `restore_db.py` | ✅ 사본 → DB 복원이 원천 지문·206,531 행·동결 상태까지 재현 |
| P4-T4 | **파이프라인 전환** — 11 개 스크립트의 읽기·쓰기를 DB 로 | ✅ 재실행 결과가 기준 판본과 **완전히 일치**(47,766 행) |
| P4-T5 | **롱리스트를 판본과 함께** `make_longlist.py` | ✅ 「2026-09-11-2 판 · 규칙 지문 04c02216 · 제련 11 개사 30.6 조」 |

**평면 `samples/*.csv` 8 개를 지웠다.** 덮어쓰기의 원인이던 자리다. 진단·시험
산출물은 `samples/reports/` 로 옮겼다.

시험 `tests/test_taxonomy_db.py` 23 건 — 동결을 우회하는 네 경로(파이썬 · UPDATE ·
DELETE · INSERT)와 해제 금지를 각각 고정했다.

---

## 5. 순서와 이유

```
P1 (불변화)  →  P2 (판본 분리)  →  P2-4 (1.1.0 생성)
                                        ↑
                              도메인 검토 통과가 전제
P3 (DB 등록) 는 P1 뒤 언제든 · P4 (분류 판본) 는 독립
```

- **P1 이 먼저인 이유**: P2 를 하려면 생성기를 고쳐야 하는데, 보호 장치 없이 고치면
  또 1.0.0 을 건드린다.
- **P2-4 가 도메인 검토 뒤인 이유**: 드라이버가 틀리면 1.1.0 을 다시 내야 하고,
  그러면 판본이 의미를 잃는다.
- **P3 를 나중에 두는 이유**: DB 등록은 사고를 막지 못한다(사실 4). 급한 것은 P1 이다.

---

## 6. 이번 계획에 포함하지 않는 것

- PostgreSQL 전환 (기존 계획서에서 이미 완료선 밖으로 둔 항목)
- 실제 회사 데이터 적재 — 롱리스트는 **영업 대상**이지 데이터 원천이 아니다
- Excel 재계산 파이프라인 복구 (LibreOffice 의존) — 필요할 때 별도로 다룬다
- 제련 업무 내용의 정합성 — 도메인 검토 사안이지 이 계획의 범위가 아니다

---

## 7. 승인이 필요한 것

1. **P1~P4 의 범위와 순서**가 맞는지.
2. ~~**P4-2**: 분류 데이터를 CSV 로 둘지 DB 로 옮길지~~ → **DB 이관으로 결정**
   (2026-09-11). 근거는 P4 절에 적었다 — 문제는 읽기가 아니라 제자리 쓰기였다.
3. **P2-2 의 형태**: 판본별 차이를 파이썬 모듈로 둘지 JSON 으로 둘지.
