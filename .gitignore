# ===== Secrets =====
.env
.env.*
!.env.example
# 점 없는 변형(env, env.example 등)도 실키가 담길 수 있으므로 전부 제외.
# 버전관리되는 정본 템플릿은 오직 .env.example(점 있음) 하나뿐이다.
/env
/env.*

# ===== Python =====
__pycache__/
*.py[cod]
.venv/
venv/

# ===== Node / Frontend =====
node_modules/
.vite/
dist/

# ===== LangGraph runtime (generated checkpoints / store / vectors) =====
.langgraph/
.langgraph_api/

# ===== Runtime data (generated per run) =====
outputs/
projects/
workspace/
library/
# 실패 번들(진단 자료). 워크스페이스 롤백(git clean -fd)에 지워지지 않도록 여기에 쓰지만,
# 저장소에 커밋할 성격은 아니다(pytest 도 tmp 워크스페이스 이름으로 하위 폴더를 만든다).
data/failures/

# ===== Databases / local state =====
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal

# ===== IDE / Editor =====
.vscode/
.idea/
*.swp
*.swo

# ===== OS =====
Thumbs.db
.DS_Store

# ===== Test / Cache =====
.pytest_cache/
.coverage
htmlcov/

# ===== Claude / AI agent =====
.claude/

# 지식 허브 런타임 데이터 (벡터 인덱스/등록 자료 원본)
data/chroma_db/
data/knowledge_packs/

# LLM 호출 텔레메트리(런타임 계측 데이터 — 커밋 금지)
data/llm_call_log.jsonl
# [§10.3] 품질 결과 텔레메트리(게이트 판정·실패 원인·사람 수용 — 런타임 계측)
data/quality_outcomes.jsonl

# [M1] 기준정보 저장소 런타임 DB (골든 레코드 — 런타임 데이터, 커밋 금지)
data/master/
# [§12] 외부 인텔리전스 런타임 저장소(§12.3 상 별도 DB)
data/external_intelligence.db*
# [§7.3] Shadow Mode 런타임 저장소
data/shadow_runs.db*
# [§9 M3] 부서 워크스페이스(공유·복제·승격) 런타임 저장소
data/workspace.db*
data/external_raw/

# 골든 벤치마크 스코어카드/사람점수 (런타임 계측). 골든 기준(golden/)은 A-1 완주 후 커밋 여부 재결정.
data/benchmark/

# [M3] MCP 브로커 온디맨드 조회 캐시 (복제 아님 — 단기 TTL 캐시, 런타임)
data/mcp_cache.db

# ★★ [2026-07-31] **운영 상태 파일은 커밋하지 않는다.** 아래 세 개는 추적돼 있었고 실제로
#   사고를 냈다: 두 폴더(원본·워크트리)에서 각각 갱신되다가 `git pull` 이 "로컬 변경이
#   덮어써진다"며 중단됐고, 하마터면 승인 17건·감사 34줄이 커밋된 옛 파일로 되돌아갈 뻔했다.
#   ⚠️ 더 위험한 것은 `scope_policy.json` 이다 — 이 파일은 `ORG_ENFORCE` 를 담고 코드
#     기본값보다 먼저 읽힌다. 저장소에 실려 다니면 **다른 환경의 강제 설정이 이 환경에 적용된다**
#     (실측: 테스트 9건이 로컬 정책 파일 때문에 깨졌다). 권한 설정은 환경의 상태이고 코드가 아니다.
#   등록 원본(docs/reference/)은 계속 커밋한다 — 판정 결과(레지스트리)만 런타임으로 뺀다.
data/reference_registry.json
data/access_audit.jsonl
data/scope_policy.json

# ★ 워크플로우 레지스트리(= "default" 템플릿)의 **런타임 저장본**. 한 번도 추적된 적이 없고,
#   `core/agent_registry.load_registry()` 가 파일이 없으면 `DEFAULT_REGISTRY` 로 폴백하므로
#   커밋하지 않아도 부팅한다. ⚠️ 오히려 커밋하면 위 세 파일과 같은 사고가 난다 — 한 환경에서
#   에이전트 순서·HOTL 을 바꾼 것이 다른 환경의 파이프라인 정의를 덮어쓴다.
agents_registry.json

# 백필·이관 스크립트가 남기는 DB 스냅샷. 원본은 `data/*.db` 이고 이것은 되돌리기용 사본이다 —
# 사본을 저장소에 실으면 「어느 쪽이 정본인가」가 흐려진다.
data/*.bak_backfill_*

*- ���纻.*

*���纻*

# 화면 캡처 산출물 — scripts/capture_screens.py 로 언제든 재생성한다(약 90초).
# 이미지를 커밋하면 금방 낡아서 «지금 화면» 이 아닌 것을 근거로 쓰게 된다.
docs/uiux/screenshots/

# 로그인 자격·세션 저장소(런타임). ⚠️ 비밀번호 해시와 활성 세션이 들어 있다 — 저장소에 실리면
# 어느 환경의 세션이 다른 환경에서 살아난다.
data/auth.db*
